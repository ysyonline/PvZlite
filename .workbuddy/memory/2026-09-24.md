# 2026-09-24

## 05:1x–05:5x 原开发机 weixufeng · v2.0.0 M2 开工：T-201/T-202/T-203 三单元闭环 ✅（4 刀未推送）

- **接手核验**：HEAD=e173e65=远端零偏差，工作树净；M1 已闭环，按 plan 进入 M2
- **T-201 `30966bc`**：MENU_BTN={x:380,y:400,w:240,h:56}+SELECT_GEOM={TAB 页签 4×192+3×8=792 居中/CELL 2行5列 5×140+4×20=780 居中/BACK 220×50}落常量区（END_BTN 惯例）；自证=new Function 编译零报错。**★code-map 刷新欠账发现：HEAD 源码实为 3435 行（M1 后），code-map 头部仍 3289（v1.9）——T-102~T-106 期间未重跑，本次重跑把 M1 结构变化一并刷入（3443 行/71 常量/97 函数/20 分区），提交信息已注明**
- **T-202 `b51c6dd`**：state 枚举+select；render 分流 drawSelectPlaceholder 临时占位（防落入 drawGameWorld 崩）；setState/updateBGM 钩子零改动实证（BGM 仅认 play/select 天然停止）；v20-state-select.js 11/11 PASS（harness 顶层 function 经 g.sandbox.setState 直达，probe().state 可断言）
- **★ 施工毛刺复踩铁律**：同文件并行两笔 Edit 互相覆盖丢改动（L495 注释+脚本 sbSetState 首笔丢失，Edit 报成功但内容不在）——「禁同文件并行 Edit」再次实证，已串行重做；grep 源码实测才定位
- **T-203 前设计缺口第五批拍板（AskUserQuestion）**：**Q-17 难度入口=选关页顶部三态钮**（主菜单保持纯静态）/ **Q-18 选卡=点关后先进 deck 再开局**（menu→select→点关→deck→对局，保留默认卡组）；已回写 decisions.md 第五批+plan §9+T-204/T-205 描述，提交 `d41a8cf`
- **T-203 `c5da641`**：drawMenu 重写（标题/副标语/进入游戏钮=MENU_BTN/积分/最高分/版本角标；删旧关卡钮/难度行/开始钮/操作提示两行）；onClickMenu 重写（仅 MENU_BTN→uiClick+setState('select')）；空格 menu 态 startGame→setState('select')
- **自证 v20-menu-verify.js（CDP 端口 9363，新脚本 151 行）**：新源 PASS（行428按钮色 131>120/点击进 select/占位底色占比 0.9943）；旧源 v1.9.0 冻结副本三判据全红（按钮色 0/点击不动）=判别力成立；四 png+追加式账本入库
- **源码行数**：3452→3394（重绘净删 58 行，4500 红线压力缓解）
- **终态**：4 刀本地未推送（30966bc/b51c6dd/c5da641/d41a8cf）；工作树净；**下一单元 T-204 选关页绘制（1.5h：4 页签+40 格三态+难度钮 Q-17+返回钮，SELECT_GEOM 落位）**；T-204 时注意：旧菜单删除的操作提示文案（1-9 选卡/X 铲子/空格暂停等）与世界规则文案需在选关页承接；M2 收尾 T-206 才跑批量门控

## 05:3x–06:2x 原开发机 · T-204 选关页绘制闭环 ✅（`03f85f6`，5 刀未推送）

- **drawSelect 新写**：页签 4 世界（WORLD_THEMES.name + Q-6a 昼夜标注常量 SEL_TAB_TIME，泳池特殊三段）/难度行 3 态钮（Q-17，SELECT_GEOM 增补 DIFF:{x0:296,y:139,w:120,h:30,gap:24}）/关卡格 2×5（可玩绿/已通关暗金+金框+✓/锁定暗+通关前一关解锁/占位灰+敬请期待）/返回钮/操作提示文案承接/积分最高分角标
- **selTab 进页缺省**：setState('select') 唯一入口钳 1..4=levelKey 所在世界；背景渐变随页签换 WORLD_THEMES 昼板色（视觉区分四世界）
- **★ 占位判定设计修正**：初版 placeholder=CARD_AWARD==PLACEHOLDER 使 1-4（corn 真卡）在锁定态显示锁定暗色——自证 6 轮红根因=采样目标选错+判定歧义；以格带颜色直方探针（CDP getImageData 分桶统计）定位；终版 placeholder=award==null||'PLACEHOLDER' 与可玩性解耦恒显示敬请期待，采样目标改 1-7（真占位）后全绿
- **自证 v20-select-draw.js**：新源六判据 PASS（页签×4/主标白字/1-1 暗金/1-7 灰 #4a4a42 精确单像素/难度金底/返回钮）；旧源 v1.9.0 FAIL（saveCleared ReferenceError=语义性必红）；v20-state-select 断言同步 drawSelect（占位函数退役）11/11
- **源码**：3394→3482 行（+88）/73 常量；v20-menu-verify 不受影响（menu 态未动）
- **终态**：5 刀本地未推送（30966bc/b51c6dd/c5da641/d41a8cf/03f85f6）；**下一单元 T-205 选关页命中（1h：页签切换/难度切换/1-1 点击→deck→开局/占位 toast/返回 menu；v20-menu-nav.js 全链路）**

## 06:1x–06:5x 原开发机 · T-205 选关页命中闭环 ✅（`38fb9df`，8 刀未推送）+ 判别力警报复盘

- **onClickSelect 新写**（几何全与 drawSelect 共用 SELECT_GEOM）：页签切换/难度切换/关卡格四分支（占位→toast「该关卡即将开放」+deny｜锁定→deny｜可玩→levelKey+level 切换→setState('deck')（Q-18）｜）/返回钮回 menu；onClick 主分流增 select 分支；deck 返回钮 menu→select（Q-18 回程闭环）
- **harness probe 增 selTab/toastMsg 桥**（typeof 守卫 v1 源 null）——let 声明不挂 sandbox 对象（铁律再现：顶层 function 才直达），断言必须走 probe
- **★ 判别力警报（重要教训）**：v20-menu-nav 首跑旧源 v1.9.0 也 20/20 全绿——根因=`loadGame(HTML位置参数)` 被静默忽略（正确签名 `loadGame({htmlPath})`），两轮全在加载缺省新源；改对象传参后旧源 15 红判别力归位。**新规则：跨源对照脚本首跑，必须先确认「旧源确实红」，只看新源绿不算数**（与 v1.9「假绿两形态」同族：静默降级型假绿）
- **自证终态**：新源 20/20（menu→select→页签→难度→占位 toast→锁定拒绝→1-1→deck→返回 select→再进→开局 play→返回钮 menu 全链路）；旧源 5/20
- **终态**：8 刀本地未推送（…03f85f6/38fb9df 含 memory 两刀）；源码 3516 行/99 函数；**下一单元 T-206 M2 收尾门控：run-gates 一键批量全绿 + v20 五脚本 + UI 三件套截图归档（tests/playtests/png/）**

## 06:5x–07:0x 原开发机 weixufeng · T-206 M2 终门 ✅（`9433181`，9 刀未推送）→ **M2 里程碑闭环**

- **FULL 门控 1 红定位=SMOKE-023 B8**：旧契约「菜单四钮直点关卡（V12 几何 190+(i-1)*160, y=270）」在 T-203/T-204/T-205 后已退役（菜单只剩 MENU_BTN 一钮，点关链路迁选关页）→ 契约迁移（T-106 同款先例），非产品 bug
- **B8 迁移要点**：新链路=点 MENU_BTN(500,428) 进 select → 页签 uiClick（重复点同签不发声）→ **切回页签 1 再测格子**（首跑踩坑：测完页签 4 后点「1-4/1-1」实际命中世界 4 的占位格 → deny 不是 uiClick）→ 锁定 1-4(col3 中心 660,228) deny → 可玩 1-1(col0 中心 180,228) uiClick 进 deck；几何与 SELECT_GEOM/MENU_BTN 同源
- **教训浓缩**：①run-all 失败行用 ❌ 字符 grep（"FAIL"字样只在汇总行）②契约迁移型断言必须带「selTab 世界基准复位」③页签测试会污染后续格子测试的世界上下文
- **menu-verify 结构判据**（T-206 前置已修）：PROBE_SELECT 旧占位色比判据被 T-204 淘汰 → selTabHits>=3 + backBtnPx>200；新源 PASS/旧源 FAIL 恢复
- **bench 回填判读**：perf-profile §7 日期+CPU 型号变（i3-10110U 低配机）ms 整体偏大属机器噪声——**draw calls 数完全一致+判级 PASS=无真实漂移** → checkout 还原
- **CRLF 警告甄别**：git add 后 "CRLF will be replaced by LF" 只是工作副本提示；`git show HEAD:file | node -e "CR计数"` 实测 CR=0 干净（od -c grep 误报 142，node 精确口径为准）
- **终态**：FULL 87/87 · BUS 56/56 · BENCH 五场景 PASS（run-gates 全绿）；v20 五脚本全绿；png 三截图归档；源码 3516 行未动；**9 刀本地未推送（用户未授权）——M2 六单元（T-201~T-206）全部闭环，下一里程碑 M3：T-301 起战局端到端**
