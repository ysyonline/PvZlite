# 2026-09-18

## 发布后跟踪收官 + 归档（00:55 授权提交提交推送，04:2x 归档）

- **用户授权「是」后两刀提交 + 推送完成**：`daeb276`（发布后跟踪产物 14 文件 +806/-26）/ `85241af`（记忆归档）/ `bb27c86`（补记推送结果）。`git ls-remote` 权威核验一致；packed-refs 坑两次复发均按既定 sed 修复法处理，最终 `## main...origin/main` 干净同步
- **发布后跟踪四项终态**：N4/#8 量化复核 ✅（Edge headless+CDP，S2 场景 C expert 4× 530 帧 p95=18.5ms / jank 0.57%，GPU 轮定案）/ E5 Edge ✅（真实点击全流程 0 错 0 帧异常）/ N2 README 改名 ✅ / ~~T6 时点跟踪 ⏳~~ → **✅ 已闭环（05:4x，见下节）**
- 报告与证据已入库：`tests/e5n4-post-release-report.md` + `tests/e5n4-evidence-backup/`
- **源码零改动**，冻结产物未触碰；v1.1.0 分发件不受影响

## T6 样本 #2 回传定案（05:4x）——发布后跟踪四项全部闭环

- 用户回传核心观测：**3:00 整时点打到第 5 波**（原话「打到第5关」，按 L1 口径记第 5 波）。已回填 `round-2-notes-2.md`（单值样本，其余栏位缺项不补并已披露）
- **T6 双样本定案**：#1 未过线（09-17，未到第 3 波）+ #2 大幅过线（第 5 波）→ 判定公式项「≥1 新手 3 分钟内到第 3 波」满足；原 CONCERNS 成因消除，22:06 豁免被事后数据支持，**历史判定不改写**（CONCERNS→豁免→事后验证通过）
- 已更新：`round-2-report.md` 升 v3（新增第十二节，R-A 风险关闭）+ `KNOWN-ISSUES.md` N3 残留段闭环。**发布后跟踪四项 N4/E5/N2/T6 全部闭环，无遗留**
- 样本 #2 数据解读：个体上手节奏差异，非难度缺陷信号；T6 不再是难度调整依据

## v1.2 开工 + T6 样本 #2 场次启动（04:41 用户双授权）

- 用户指令两条：① 新人正在玩 T6 跟踪场，结束后用户填表回传 → ② v1.2 开工（走七阶段诊断）
- 已建 `tests/playtests/round-2-notes-2.md`（样本 #2 专用记录表，核心观测 = **03:00 整时到达波次**；原 `round-2-notes.md` 是 09-17 样本 #1 已填成稿，只读不覆盖）
- 用户 04:51 拍板 **方案 A（泳池地形线）** → `production/v1.2-plan.md` 落盘 → 团队 `pvz-v1.2` 建立 → V12-01 派文策渊
- **V12-01 完成（G1 评审通过）**：`design/gdd/level-4.md` 落盘。要点：8 波 38 只（n16/c10/f8/b4），大波 W3/W6/W8，两幕结构（W1-W5 无桶、W6 桶回归、W8 双桶压轴 2480hp 系列之最）；startSun=150（刻意非单调，拍板项①）；睡莲 25 费唯一新卡；水域按**行级常量**降维（WATER_ROWS=[1,3]，粗估 60-120 行，推翻 L3 时代 C 档 >150 行判断）。数值三口径主理人独立复算一致：38=38、9640=9640、640<1600<2480
- **5 项用户拍板项待决**（GDD 附录表）：①startSun=150 ②垫语义甲（实体，推荐）/乙（布尔）——乙需 V12-02 背书 ③睡莲 CD=5s ④波次表终稿 ⑤W8 双桶
- **V12-02 完成（G2 评审 PASS）**：`docs/architecture/v1.2-pool-terrain.md` 落盘。关键结论：
  - **垫语义推荐甲案**（垫为真实植物实体，走 plants 全管道零新状态载体）；唯一硬成本 = updateZombies 啃食目标选择改**倒序遍历**（+1 行，L1006），已证明对陆地格严格等价（嘴前窗口 36px < CELL_W 90）
  - **总改动核心 ≈76 行 / 含注释可选 ≈90**（GDD 粗估 60-120 ✓）；测试侧 3 文件 ≈150 行
  - **E1/E2 时序确认**：现状 L652 占用校验在前、L653 扣款在后，天然零副作用；水轴校验插 L652 后纯 +2 行
  - **字段名定稿**：`WATER_ROWS=[1,3]`（L37 后）、`CARDS[6]={type:'lilypad',name:'睡莲',cost:25,cd:5,dur:300}`（dur 不入契约）
  - **补充发现**：GDD 漏了菜单按钮 x 基准 270→190 ×2 + 菜单文案 2 处（"6 植物"/"1-6 选卡"）
  - **E4 规格矛盾**：GDD 原文"垫上有植物禁止铲 + toast 先移除上方植物"是死路（无处移除）；程基岩建议改"铲子作用于最上层实体"。已转文策渊裁决（主理人倾向 A 采纳修正）
  - SFX 零新增确认（plant 绑事件绑类型）；SMOKE-025 进 run-smoke、REG-END-02 平移 6 处，两 runner 零改动
  - V12-03 六步施工顺序已给出（数据→校验→行为→渲染→测试→收尾，验证驱动 T16 先行）
- **会签完成（GDD v1.0→v1.1）**：文策渊回写 3 处事实修正 + E4 裁决 **A 案**（铲子作用于最上层实体，垫+植物→移植物留垫；空垫→移垫无返还；设计理由已写入 E4 格）。主理人 grep 核验落点无残留矛盾文本。**GDD/架构两文档现在零矛盾、零悬置**
- 等待中：**6 项拍板项打包用户终审** → V12-03 实现派单

## 提交推送收尾（07:59，用户授权「推」）

- **两刀提交 + 推送完成**：`48ba89d`（test(T6) 样本 #2 定案 3 文件）/ `57bed4e`（docs(V12) Phase 1-2 产物 + 记忆归档 5 文件）。`git ls-remote` 权威核验一致（`57bed4e1…bb815`）；packed-refs 坑复发一次，按既定 sed 法修复，`## main...origin/main` 干净同步，工作树全净
- **归档彻底完成**：新电脑可直接 clone 拿到全部最新进度（clone 后哈希核验记得走 `git show HEAD:plants-vs-zombies.html | sha256sum`）

---

# 📌 新会话接手指南（2026-09-18 05:55 归档 · 取代本文件上方 04:25 旧版与 2026-09-17.md 末尾版）

## 一、当前状态（一句话）
**v1.1 发布后跟踪四项全部闭环**（T6 样本 #2 定案：3:00 时点第 5 波，双样本过线）；**v1.2 方案 A「泳池」Phase 1-2 完成**——GDD v1.1 + 架构清单双文档定稿零矛盾，**卡在 6 项拍板项等用户终审**（回「全部按建议」即派 V12-03 实现）。
**⚠️ 归档时点工作树有 7 个未提交文件**（3 改 4 新：T6 定案 2 文档 + KNOWN-ISSUES N3 闭环 + v1.2 三文档 + 记忆归档），**未提交未推送**——新会话开场先向用户确认是否提交。

## 二、剩余待办（按优先级）
| # | 事项 | 说明 |
|---|---|---|
| 1 | **工作树 7 文件提交推送**（需用户授权） | 内容：T6 样本 #2 定案（notes-2/report v3/KNOWN-ISSUES N3）+ v1.2 Phase 1-2 产物（v1.2-plan/GDD level-4/架构 pool-terrain）+ 记忆归档。建议拆 2 commit：①`test(T6)` 双样本定案 ②`docs(V12)` Phase 1-2 产物 |
| 2 | **v1.2 六项拍板项终审** | ①startSun=150 ②垫语义甲案 ③睡莲 CD=5s ④波次表照抄 ⑤W8 双桶 ⑥E4 铲子 A 案（最上层实体）——全有推荐值，GDD 附录表 + v1.2-pool-terrain.md §6 |
| 3 | **V12-03 实现派单**（拍板后） | 按程基岩六步施工顺序（数据→校验→行为→渲染→测试→收尾，验证驱动 SMOKE-025 T16 先行）；改动面核心 ≈76 行 + 测试侧 ≈150 行；SFX 零新增 |
| 4 | v1.2 后续冲刺 | S2 实现 → S3 门控全绿 + bench L4 场景 + Playtest + 定版发布（v1.2-plan.md §二） |

## 三、硬规则（继续有效）
1. **v1.1.0 为线上版本基线**：改源码须走新版本流程，不得直接改后覆盖分发；`production/release/v1.0.0/` 与 `v1.1/` 两处冻结产物只读
2. 改代码：查 `docs/code-map.md` → 只读目标区块 → 精确编辑 → 重跑 `node tools/gen-code-map.mjs`；禁止对同一文件并行 Edit，改完 grep 核对
3. 高影响动作（提交/推送/打标签/分发/热修）一律先获用户授权
4. 测试基线：烟雾 24/24 · 回归 30/30（--all 54/54）· 总线 55/55 · bench PASS；改源码后必须全绿

## 四、关键命令（照抄）
- Node 绝对路径：`C:/Users/weixufeng/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`（带 `-3` 后缀）
- Bash 每命令开头：`export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY`
- 推送（helper 覆盖，否则挂死）+ 推送后 packed-refs 修复 + 哈希核验：见 `2026-09-17.md` 末尾接手指南第五/七节（仍有效）
- **浏览器自动化（已实证，别装 agent-browser）**：Edge headless + CDP，方法与坑全记录在 `2026-09-17.md`「发布后跟踪执行完成」第三节

## 五、教训与状态备注
- 性能结论必须用 GPU 轮（`--disable-gpu` 软渲染 jank 13.7% 是假象，GPU 轮 0.57%）；注入稳态压力时僵尸 spd 置 0
- **团队 `pvz-v1.2` 未解散**（design-strategist / engineering-lead 两个成员在线待命）；新会话若继续 v1.2，SendMessage 按 name 唤醒即可；若开新团队则先 TeamDelete（需先 shutdown 两成员）
- T6 双样本方法论：单值样本也可定案（判定只依赖一个观测点），但缺项必须披露、不外推其它结论
- v1.2 关键设计资产速查：`WATER_ROWS=[1,3]` / `CARDS[6] lilypad 25费5sCD dur300(不入契约)` / 啃食倒序遍历 L1006(+1行) / 水轴校验 onClick L652 后(+2行) / 菜单基准 270→190×2 + 文案 2 处

## V12-03 实现完成（10:13 用户「确认」六项拍板全按推荐 → 直接派单施工）

- **源码 plants-vs-zombies.html 1985 → 2076 行（+91，落架构估算 ≈76-90 区间上沿）**，VERSION v1.1.0 → v1.2.0
- 数据层：`WATER_ROWS=[1,3]`（L38）+ LEVELS[4] 泳池关 8 波 38 只照抄 GDD §4.1 + CARDS[6] lilypad 25费5sCD + **实现期新增 `water:true` 关卡标志（L108）**
- **⚠️ 架构文档 §3/§4 的水轴校验原案有全局生效漏洞（实现期发现并修正）**：原案 `WATER_ROWS.includes(g.y)` 不看关卡 → L1-L3 全陆地关的水行格位（如 (0,3)）种植被误拦，SMOKE-010 当场拦截。修正 = LEVELS[4] 加 `water:true`，校验（L675-676）/水面渲染（L1291）/僵尸半浸（L1613）三处全部 `level.water` 门控，L1-L3 零感知。**此为对架构文档的规格 deviation，须回写 v1.2-pool-terrain.md**
- 行为层：啃食倒序遍历（L1030，甲案）；E4 铲子 A 案 = 铲子循环改倒序取最上层实体（L655）
- 占用检查改 `(p.type!=='lilypad'||c.type==='lilypad')` 条件：垫不算占用；**并堵住架构文档插入代码的边界漏洞——「水格有垫再点睡莲」现在正确走占用拒绝**（GDD §3.2 矩阵"有垫+睡莲=❌已占用"）
- 渲染层：水面渐变+相位波纹（gt 驱动暂停冻结）+水陆交界亮边；睡莲场景绘制（无落地投影）+卡面；僵尸水行下半浸色偏；菜单 4 钮基准 190 + 上界 4 ×2 处；文案 6→7 植物/1-7 选卡 ×3 处（HUD 那处架构清单也没列，顺手改了）
- 测试侧：SMOKE-025 新建（T1-T16 全覆盖 + L1 陆地关回归锁 + 真帧断言）；REG-END-02 平移锁 LEVELS[5]；harness index.js +2 桥（`__CARDS` / `__consts.WATER_ROWS` 带 typeof 守卫）；SMOKE-023 菜单按钮坐标更新（360→260、520→580，B8 语义等价：点已解锁钮 uiClick + 点未解锁钮 deny）
- **测试侧坑**：①harness `selectShovel` 后 clickGrid 铲子用后即清（selected=null），连续铲两格须各重选铲子（SMOKE-018 同款惯例）②豌豆是 CARDS 索引 1 不是 0
- **四道门控全绿**：烟雾 25/25（新增 025）· 回归 30/30（--all 55/55，REG-END-02 平移后过）· 总线 55/55 · bench PASS（已回填 perf-profile §7，幂等正常）
- code-map 已重跑（2076 行/45 常量）；**工作树 6 文件改动未提交**（源码+code-map+perf-profile+3 测试文件+新 SMOKE-025），待用户授权提交
- 待办：M1-M5 人工验收（真机）→ bench L4 末波场景（M7，v1.2-plan S3）→ 提交推送（需授权）

## V12-03b 真机反馈修复轮（10:0x-10:3x）——M1/M2 通过，5 项反馈全部修复

- **用户真机反馈**：M1 ✅ M2 ✅；M3a 半浸不明显；M4 铲豌豆无返还；新报 3 项 = 僵尸下水无水花、无下水音效、4x 进关失效
- **4x 根因定位（唯一 bug 级）**：`startGame` 里 `gameSpeed=1` 重置 → 菜单/上一局设的档位进新关被静默清回 1x，但按钮 UI 仍显示旧档（状态错位）。**修复 = 声明处 `let gameSpeed=1` 定基 1 次，startGame 不再触碰**；档位跨局保留、只随按钮变（SMOKE-026 新建锁死该契约：按钮 stub 真实路径 3 档循环 + 进关不断档 + 跨局保留）
- **M3a 半浸加强**：叠层 0.55→**0.78** + 腰线亮带 `rgba(200,235,255,0.5)` 3px 横条
- **M4 铲除返还（偏离 GDD §6.2「无返还」，用户真机反馈后的事实拍板）**：返卡价一半向下取 25 的倍数、保底 25（豌豆返 50 / 睡莲返 25 / 向日葵·坚果返 25 / 双发返 100 / 西瓜返 75），toast `+N 阳光` 提示；SMOKE-025 T16⑤/⑤b 追加 ⑤a/⑤b-a 阳光断言（874+50 / 924+25）
- **入水水花（新）**：updateZombies 内 `z.splashed` 一次性标记，僵尸过草坪右缘（`x<=GRID_X+COLS*CELL_W+8`）触发 14 粒白蓝粒子 + `shockwave` 环（新 `water:true` 字段走水色渲染 `rgba(190,230,252,·)` 2.5px）；跋涉涟漪按 `R(0,1)<dt*1.2` dt 概率触发（帧率/倍速无关，约 0.8s 一次，无声防糊音）
- **入水音效 SFX.splash（新，偏离 GDD §7.1 零新音频）**：`noise(0.3,vol .14,lp1200,hp250)+tone(520→170)`，路由 `splash:'battle'`；AUDIO_ROUTES 与 verify-bus expect 表同步 +1 键（56 断言）；gate 节流 0.3s
- **四道门控全绿（数字全部 +1）**：烟雾 **26/26** · 回归 --all **56/56** · 总线 **56/56** · bench PASS；code-map 重跑（**2111 行**/45 常量）
- 验收表 `v12-acceptance.md` 已升级：新增 M3b（入水反馈 4 项）/ M3c（4x 3 项）章节，M4 返还预期更新，M6 门控数字刷新
- **工作树 12 文件未提交**（6 旧 + 新增 verify-bus/SMOKE-026/验收表/2 记忆文件），待用户授权提交；待办不变：M3b/M3c 真机复验 → 提交推送

## 📌 新会话接手指南（12:39 归档版，以此为准）

**当前状态一句话**：v1.2 V12-03（泳池关实现）+ V12-03b（真机反馈修复轮）全部完成，四道门控全绿（**烟雾 26/26 · 回归 --all 56/56 · 总线 56/56 · bench PASS**），源码 **2111 行**（code-map 已重跑同步），**工作树 12 文件未提交**，等用户两件事：① M3b/M3c 真机复验结果 ② 提交推送授权。

**12 个未提交文件**（`git status -s` 核实）：
- 源码：`plants-vs-zombies.html`（V12-03 主体 + V12-03b 五项修复）
- 文档：`docs/code-map.md`（重跑）、`docs/architecture/perf-profile.md`（bench 幂等回填，非源码改动）
- 测试：`tests/harness/cases/SMOKE-025.js`、`SMOKE-026.js`（新增）、`REG-END-02.js`、`SMOKE-023.js`、`tests/harness/index.js`（+2 桥）、`verify-bus.js`（+splash 路由）
- 产物：`tests/playtests/v12-acceptance.md`（新增，含 M3b/M3c）
- 记忆：`.workbuddy/memory/2026-09-18.md`、`MEMORY.md`

**提交建议（两刀，待授权）**：
1. 功能刀：源码 + code-map + 测试 6 文件 + 验收表（`feat(v1.2): 泳池关 V12-03+03b`，信息里写明 4x 修复根因与两处 GDD 偏离）
2. 记忆刀：`.workbuddy/memory/` 2 文件
- **git add 别带不存在的路径**；提交后必看 `git show --stat`；推送用 helper 覆盖命令（本文件上方「推送命令」节，照抄勿改）

**新会话开场动作**：
1. 问用户 M3b（入水水花/音效）/ M3c（4x 跨局保留）真机复验结果
2. 若复验通过 + 授权 → 按上面两刀提交推送 → `git ls-remote origin main` 核验
3. **遗留待办（不阻塞提交）**：① GDD 两处规格偏离须回写 `design/gdd/level-4.md`（§6.2 铲除返还、§7.1 splash 音频）② 架构文档两处漏洞修正回写 `docs/architecture/v1.2-pool-terrain.md`（water:true 门控 + 有垫拒铺垫）③ M5/M7（整局时长 / bench L4 末波场景）仍空着
4. 之后剩余流程见 `production/v1.2-plan.md`（版本号 v1.2.0 → 发布 → tag 推远端）

## 文档回写完成（12:44 接手会话，遗留待办①②清账）

- **GDD `design/gdd/level-4.md` v1.1→v1.2**：①版本头增补 V12-03b 回写记录 ②§6.2 E4 增补铲除返还规则（卡价一半向下取 25 倍数保底 25，豌豆 50/睡莲·向日葵·坚果 25/双发 100/西瓜 75，toast 提示，偏离 v1.0「无返还」原文并注明）③§7.1 结论改写「零新音频→唯新增 SFX.splash」（noise+tone 参数、路由 battle、gate 0.3s、verify-bus 55→56）+ 原「明确不做入水音」删除线保留历史判断 + §7.2 备忘同步 56/56
- **架构 `docs/architecture/v1.2-pool-terrain.md`**：①状态行 Delivered→Implemented ②§3 两处 ⚠️ 实现期修正注：水轴校验原案全局生效漏洞（water:true 门控三处 L687-688/L1319/L1640）+ 占用检查 lilypad 分支补齐（堵「有垫再点睡莲」，L686）③§4 清单 #4/#5 行同步修正注
- 回写事实全部与源码实况 grep 核对（返还公式 L670、splash 路由 L331、三处 water 门控）；未动源码与测试
- **工作树 14 文件未提交**（原 12 + 本轮 2 文档回写）；提交建议升级为三刀：①feat 功能刀 ②docs 规格回写刀（GDD+架构）③记忆刀
- 仍待用户：M3b/M3c 真机复验结果 + 提交推送授权
- **12:5x 用户答复：尚未真机复验，暂不提交**——工作树 14 文件保持现状；下次推进 = 用户回传 M3b/M3c 复验结果 + 提交授权
- **12:56 用户复验回传（M3b/M3c 定案）**：
  - **M3c ✅ 通过**（4x 跨局保留三行全过）——4x 修复闭环
  - **M3b 功能可用判通过，沉淀 3 项优化转下次迭代（用户明确本轮不改）**：①入水声改「噗通」②游动途中「哗哗哗」水声（现为刻意无声）③水中僵尸应看不见腿（现半浸仅色偏，腿仍可见）
  - 已回填 `tests/playtests/v12-acceptance.md`（M3b 4 行逐项 + 结论注 + M3c 三行全 ✅）
  - 已登记优化池 `production/v1.2-plan.md` §六（O1 噗通 / O2 哗哗哗 / O3 腿部遮挡，含现状→期望与涉及面）
- **仍挂起：提交推送授权**——工作树 **15 文件**（git status 实核：13 M + 2 ??，新增 production/v1.2-plan.md 优化池改动；v12-acceptance.md 本就是 ?? 新文件）下次开场直接问授权即可，三刀建议不变（功能 / 文档回写 / 记忆）
