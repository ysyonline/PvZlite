# 2026-09-18

## 发布后跟踪收官 + 归档（00:55 授权提交推送，04:2x 归档）

- **用户授权「是」后两刀提交 + 推送完成**：`daeb276`（发布后跟踪产物 14 文件 +806/-26）/ `85241af`（记忆归档）/ `bb27c86`（补记推送结果）。`git ls-remote` 权威核验一致；packed-refs 坑两次复发均按既定 sed 修复法处理，最终 `## main...origin/main` 干净同步
- **发布后跟踪四项终态**：N4/#8 量化复核 ✅（Edge headless+CDP，S2 场景 C expert 4× 530 帧 p95=18.5ms / jank 0.57%，GPU 轮定案）/ E5 Edge ✅（真实点击全流程 0 错 0 帧异常）/ N2 README 改名 ✅ / **T6 时点跟踪 ⏳ 待用户安排真人新手场次**（记录表 `tests/playtests/round-2-execution-pack.md` 第六节）
- 报告与证据已入库：`tests/e5n4-post-release-report.md` + `tests/e5n4-evidence-backup/`
- **源码零改动**，冻结产物未触碰；v1.1.0 分发件不受影响

---

# 📌 新会话接手指南（2026-09-18 04:25 归档 · 取代 2026-09-17.md 末尾 v1.2 开工版）

## 一、当前状态（一句话）
**v1.1.0 已发布，发布后跟踪四项中三项闭环并全部提交推送远端**（HEAD = origin/main = `bb27c86`，工作树干净）；剩余 = **T6 时点跟踪一场**（用户安排）+ **v1.2 规划**（主线下一步）。

## 二、剩余待办（按优先级）
| # | 事项 | 说明 |
|---|---|---|
| 1 | **T6 时点跟踪一场** | 真人新手观察「3 分钟内是否到第 3 波」，记录表 `tests/playtests/round-2-execution-pack.md` 第六节；记录到手后由主理人更新 round-2-report 并闭合 KNOWN-ISSUES N3 残留 |
| 2 | **v1.2 规划** | 候选池：触屏/移动端适配、地瓜 AOE 激活（`nearCell` 死代码，v1.0.0 起遗留）、新关卡（泳池/屋顶地形方向）、新植物/僵尸（樱桃/寒冰/旗帜/气球等）。**开工前先走七阶段诊断**，出 A/B/C 方案供用户拍板，别直接写码 |
| 3 | 分发跟进 | 分发件就绪（`production/release/v1.1/plants-vs-zombies.v1.1.html`，改名分发）；玩家反馈走 `tests/bug-taxonomy.md` 分级入 `tests/bugs/` |

## 三、硬规则（继续有效）
1. **v1.1.0 为线上版本基线**：改源码须走新版本流程，不得直接改后覆盖分发；`production/release/v1.0.0/` 与 `v1.1/` 两处冻结产物只读
2. 改代码：查 `docs/code-map.md` → 只读目标区块 → 精确编辑 → 重跑 `node tools/gen-code-map.mjs`；禁止对同一文件并行 Edit，改完 grep 核对
3. 高影响动作（提交/推送/打标签/分发/热修）一律先获用户授权
4. 测试基线：烟雾 24/24 · 回归 30/30（--all 54/54）· 总线 55/55 · bench PASS；改源码后必须全绿

## 四、关键命令（照抄）
- Node 绝对路径：`C:/Users/weixufeng/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`（带 `-3` 后缀）
- Bash 每命令开头：`export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY`
- 推送（helper 覆盖，否则挂死）+ 推送后 packed-refs 修复 + 哈希核验：见 `2026-09-17.md` 末尾接手指南第五/七节（仍有效）
- **浏览器自动化（已实证，别装 agent-browser）**：Edge headless + CDP，方法与坑全记录在 `2026-09-17.md`「发布后跟踪执行完成」第三节；可复现 runner = `tests/e5n4-evidence-backup/e5n4-runner.mjs`

## 五、上一会话教训（延续）
- 性能结论必须用 GPU 轮（`--disable-gpu` 软渲染 jank 13.7% 是假象，GPU 轮 0.57%）
- PowerShell 起的 Edge 子进程随 PS 会话结束被清——bash 单命令内启动+跑 runner 一气呵成
- 注入稳态压力时僵尸 spd 必须置 0（否则采样中途进屋触发 end 稀释数据）
