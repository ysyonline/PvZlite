# 2026-09-18

## 发布后跟踪收官 + 归档（00:55 授权提交提交推送，04:2x 归档）

- **用户授权「是」后两刀提交 + 推送完成**：`daeb276`（发布后跟踪产物 14 文件 +806/-26）/ `85241af`（记忆归档）/ `bb27c86`（补记推送结果）。`git ls-remote` 权威核验一致；packed-refs 坑两次复发均按既定 sed 修复法处理，最终 `## main...origin/main` 干净同步
- **发布后跟踪四项终态**：N4/#8 量化复核 ✅（Edge headless+CDP，S2 场景 C expert 4× 530 帧 p95=18.5ms / jank 0.57%，GPU 轮定案）/ E5 Edge ✅（真实点击全流程 0 错 0 帧异常）/ N2 README 改名 ✅ / ~~T6 时点跟踪 ⏳~~ → **✅ 已闭环（05:4x，见下节）**
- 报告与证据已入库：`tests/e5n4-post-release-report.md` + `tests/e5n4-evidence-backup/`
- **源码零改动**，冻结产物未触碰；v1.1.0 分发件不受影响

## T6 样本 #2 回传定案（05:4x）——发布后跟踪四项全部闭环

- 用户回传核心观测：**3:00 整时点打到第 5 波**（原话「打到第5关」，按 L1 口径记第 5 波）。已回填 `round-2-notes-2.md`（单值样本，其余栏位缺项不补并已披露）
- **T6 双样本定案**：#1 未过线（09-17，未到第 3 波）+ #2 大幅过线（第 5 波）→ 判定公式项「≥1 新手 3 分钟内到第 3 波」满足；原 CONCERNS 成因消除，22:06 豁免被事后数据支持，**历史判定不改写**（CONCERNS→豁免→事后验证通过）
- 已更新：`round-2-report.md` 升 v3（新增第十二节，R-A 风险关闭）+ `KNOWN-ISSUES.md` N3 残留段闭环。**发布后跟踪四项 N4/E5/N2/T6 全部闭环，无遗留**
- 样本 #2 数据解读：个体上手节奏差异，非难度缺陷信号；T6 不再是难度调整依据

## v1.2 开工 + T6 样本 #2 场次启动（04:41 用户双授权）

- 用户指令两条：① 新人正在玩 T6 跟踪场，结束后用户填表回传 → ② v1.2 开工（走七阶段诊断）
- 已建 `tests/playtests/round-2-notes-2.md`（样本 #2 专用记录表，核心观测 = **03:00 整时到达波次**；原 `round-2-notes.md` 是 09-17 样本 #1 已填成稿，只读不覆盖）
- 用户 04:51 拍板 **方案 A（泳池地形线）** → `production/v1.2-plan.md` 落盘 → 团队 `pvz-v1.2` 建立 → V12-01 派文策渊
- **V12-01 完成（G1 评审通过）**：`design/gdd/level-4.md` 落盘。要点：8 波 38 只（n16/c10/f8/b4），大波 W3/W6/W8，两幕结构（W1-W5 无桶、W6 桶回归、W8 双桶压轴 2480hp 系列之最）；startSun=150（刻意非单调，拍板项①）；睡莲 25 费唯一新卡；水域按**行级常量**降维（WATER_ROWS=[1,3]，粗估 60-120 行，推翻 L3 时代 C 档 >150 行判断）。数值三口径主理人独立复算一致：38=38、9640=9640、640<1600<2480
- **5 项用户拍板项待决**（GDD 附录表）：①startSun=150 ②垫语义甲（实体，推荐）/乙（布尔）——乙需 V12-02 背书 ③睡莲 CD=5s ④波次表终稿 ⑤W8 双桶
- **V12-02 完成（G2 评审 PASS）**：`docs/architecture/v1.2-pool-terrain.md` 落盘。关键结论：
  - **垫语义推荐甲案**（垫为真实植物实体，走 plants 全管道零新状态载体）；唯一硬成本 = updateZombies 啃食目标选择改**倒序遍历**（+1 行，L1006），已证明对陆地格严格等价（嘴前窗口 36px < CELL_W 90）
  - **总改动核心 ≈76 行 / 含注释可选 ≈90**（GDD 粗估 60-120 ✓）；测试侧 3 文件 ≈150 行
  - **E1/E2 时序确认**：现状 L652 占用校验在前、L653 扣款在后，天然零副作用；水轴校验插 L652 后纯 +2 行
  - **字段名定稿**：`WATER_ROWS=[1,3]`（L37 后）、`CARDS[6]={type:'lilypad',name:'睡莲',cost:25,cd:5,dur:300}`（dur 不入契约）
  - **补充发现**：GDD 漏了菜单按钮 x 基准 270→190 ×2 + 菜单文案 2 处（"6 植物"/"1-6 选卡"）
  - **E4 规格矛盾**：GDD 原文"垫上有植物禁止铲 + toast 先移除上方植物"是死路（无处移除）；程基岩建议改"铲子作用于最上层实体"。已转文策渊裁决（主理人倾向 A 采纳修正）
  - SFX 零新增确认（plant 绑事件绑类型）；SMOKE-025 进 run-smoke、REG-END-02 平移 6 处，两 runner 零改动
  - V12-03 六步施工顺序已给出（数据→校验→行为→渲染→测试→收尾，验证驱动 T16 先行）
- **会签完成（GDD v1.0→v1.1）**：文策渊回写 3 处事实修正 + E4 裁决 **A 案**（铲子作用于最上层实体，垫+植物→移植物留垫；空垫→移垫无返还；设计理由已写入 E4 格）。主理人 grep 核验落点无残留矛盾文本。**GDD/架构两文档现在零矛盾、零悬置**
- 等待中：**6 项拍板项打包用户终审** → V12-03 实现派单

## 提交推送收尾（07:59，用户授权「推」）

- **两刀提交 + 推送完成**：`48ba89d`（test(T6) 样本 #2 定案 3 文件）/ `57bed4e`（docs(V12) Phase 1-2 产物 + 记忆归档 5 文件）。`git ls-remote` 权威核验一致（`57bed4e1…bb815`）；packed-refs 坑复发一次，按既定 sed 法修复，`## main...origin/main` 干净同步，工作树全净
- **归档彻底完成**：新电脑可直接 clone 拿到全部最新进度（clone 后哈希核验记得走 `git show HEAD:plants-vs-zombies.html | sha256sum`）

---

# 📌 新会话接手指南（2026-09-18 05:55 归档 · 取代本文件上方 04:25 旧版与 2026-09-17.md 末尾版）

## 一、当前状态（一句话）
**v1.1 发布后跟踪四项全部闭环**（T6 样本 #2 定案：3:00 时点第 5 波，双样本过线）；**v1.2 方案 A「泳池」Phase 1-2 完成**——GDD v1.1 + 架构清单双文档定稿零矛盾，**卡在 6 项拍板项等用户终审**（回「全部按建议」即派 V12-03 实现）。
**⚠️ 归档时点工作树有 7 个未提交文件**（3 改 4 新：T6 定案 2 文档 + KNOWN-ISSUES N3 闭环 + v1.2 三文档 + 记忆归档），**未提交未推送**——新会话开场先向用户确认是否提交。

## 二、剩余待办（按优先级）
| # | 事项 | 说明 |
|---|---|---|
| 1 | **工作树 7 文件提交推送**（需用户授权） | 内容：T6 样本 #2 定案（notes-2/report v3/KNOWN-ISSUES N3）+ v1.2 Phase 1-2 产物（v1.2-plan/GDD level-4/架构 pool-terrain）+ 记忆归档。建议拆 2 commit：①`test(T6)` 双样本定案 ②`docs(V12)` Phase 1-2 产物 |
| 2 | **v1.2 六项拍板项终审** | ①startSun=150 ②垫语义甲案 ③睡莲 CD=5s ④波次表照抄 ⑤W8 双桶 ⑥E4 铲子 A 案（最上层实体）——全有推荐值，GDD 附录表 + v1.2-pool-terrain.md §6 |
| 3 | **V12-03 实现派单**（拍板后） | 按程基岩六步施工顺序（数据→校验→行为→渲染→测试→收尾，验证驱动 SMOKE-025 T16 先行）；改动面核心 ≈76 行 + 测试侧 ≈150 行；SFX 零新增 |
| 4 | v1.2 后续冲刺 | S2 实现 → S3 门控全绿 + bench L4 场景 + Playtest + 定版发布（v1.2-plan.md §二） |

## 三、硬规则（继续有效）
1. **v1.1.0 为线上版本基线**：改源码须走新版本流程，不得直接改后覆盖分发；`production/release/v1.0.0/` 与 `v1.1/` 两处冻结产物只读
2. 改代码：查 `docs/code-map.md` → 只读目标区块 → 精确编辑 → 重跑 `node tools/gen-code-map.mjs`；禁止对同一文件并行 Edit，改完 grep 核对
3. 高影响动作（提交/推送/打标签/分发/热修）一律先获用户授权
4. 测试基线：烟雾 24/24 · 回归 30/30（--all 54/54）· 总线 55/55 · bench PASS；改源码后必须全绿

## 四、关键命令（照抄）
- Node 绝对路径：`C:/Users/weixufeng/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`（带 `-3` 后缀）
- Bash 每命令开头：`export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY`
- 推送（helper 覆盖，否则挂死）+ 推送后 packed-refs 修复 + 哈希核验：见 `2026-09-17.md` 末尾接手指南第五/七节（仍有效）
- **浏览器自动化（已实证，别装 agent-browser）**：Edge headless + CDP，方法与坑全记录在 `2026-09-17.md`「发布后跟踪执行完成」第三节

## 五、教训与状态备注
- 性能结论必须用 GPU 轮（`--disable-gpu` 软渲染 jank 13.7% 是假象，GPU 轮 0.57%）；注入稳态压力时僵尸 spd 置 0
- **团队 `pvz-v1.2` 未解散**（design-strategist / engineering-lead 两个成员在线待命）；新会话若继续 v1.2，SendMessage 按 name 唤醒即可；若开新团队则先 TeamDelete（需先 shutdown 两成员）
- T6 双样本方法论：单值样本也可定案（判定只依赖一个观测点），但缺项必须披露、不外推其它结论
- v1.2 关键设计资产速查：`WATER_ROWS=[1,3]` / `CARDS[6] lilypad 25费5sCD dur300(不入契约)` / 啃食倒序遍历 L1006(+1行) / 水轴校验 onClick L652 后(+2行) / 菜单基准 270→190×2 + 文案 2 处

## V12-03 实现完成（10:13 用户「确认」六项拍板全按推荐 → 直接派单施工）

- **源码 plants-vs-zombies.html 1985 → 2076 行（+91，落架构估算 ≈76-90 区间上沿）**，VERSION v1.1.0 → v1.2.0
- 数据层：`WATER_ROWS=[1,3]`（L38）+ LEVELS[4] 泳池关 8 波 38 只照抄 GDD §4.1 + CARDS[6] lilypad 25费5sCD + **实现期新增 `water:true` 关卡标志（L108）**
- **⚠️ 架构文档 §3/§4 的水轴校验原案有全局生效漏洞（实现期发现并修正）**：原案 `WATER_ROWS.includes(g.y)` 不看关卡 → L1-L3 全陆地关的水行格位（如 (0,3)）种植被误拦，SMOKE-010 当场拦截。修正 = LEVELS[4] 加 `water:true`，校验（L675-676）/水面渲染（L1291）/僵尸半浸（L1613）三处全部 `level.water` 门控，L1-L3 零感知。**此为对架构文档的规格 deviation，须回写 v1.2-pool-terrain.md**
- 行为层：啃食倒序遍历（L1030，甲案）；E4 铲子 A 案 = 铲子循环改倒序取最上层实体（L655）
- 占用检查改 `(p.type!=='lilypad'||c.type==='lilypad')` 条件：垫不算占用；**并堵住架构文档插入代码的边界漏洞——「水格有垫再点睡莲」现在正确走占用拒绝**（GDD §3.2 矩阵"有垫+睡莲=❌已占用"）
- 渲染层：水面渐变+相位波纹（gt 驱动暂停冻结）+水陆交界亮边；睡莲场景绘制（无落地投影）+卡面；僵尸水行下半浸色偏；菜单 4 钮基准 190 + 上界 4 ×2 处；文案 6→7 植物/1-7 选卡 ×3 处（HUD 那处架构清单也没列，顺手改了）
- 测试侧：SMOKE-025 新建（T1-T16 全覆盖 + L1 陆地关回归锁 + 真帧断言）；REG-END-02 平移锁 LEVELS[5]；harness index.js +2 桥（`__CARDS` / `__consts.WATER_ROWS` 带 typeof 守卫）；SMOKE-023 菜单按钮坐标更新（360→260、520→580，B8 语义等价：点已解锁钮 uiClick + 点未解锁钮 deny）
- **测试侧坑**：①harness `selectShovel` 后 clickGrid 铲子用后即清（selected=null），连续铲两格须各重选铲子（SMOKE-018 同款惯例）②豌豆是 CARDS 索引 1 不是 0
- **四道门控全绿**：烟雾 25/25（新增 025）· 回归 30/30（--all 55/55，REG-END-02 平移后过）· 总线 55/55 · bench PASS（已回填 perf-profile §7，幂等正常）
- code-map 已重跑（2076 行/45 常量）；**工作树 6 文件改动未提交**（源码+code-map+perf-profile+3 测试文件+新 SMOKE-025），待用户授权提交
- 待办：M1-M5 人工验收（真机）→ bench L4 末波场景（M7，v1.2-plan S3）→ 提交推送（需授权）

## V12-03b 真机反馈修复轮（10:0x-10:3x）——M1/M2 通过，5 项反馈全部修复

- **用户真机反馈**：M1 ✅ M2 ✅；M3a 半浸不明显；M4 铲豌豆无返还；新报 3 项 = 僵尸下水无水花、无下水音效、4x 进关失效
- **4x 根因定位（唯一 bug 级）**：`startGame` 里 `gameSpeed=1` 重置 → 菜单/上一局设的档位进新关被静默清回 1x，但按钮 UI 仍显示旧档（状态错位）。**修复 = 声明处 `let gameSpeed=1` 定基 1 次，startGame 不再触碰**；档位跨局保留、只随按钮变（SMOKE-026 新建锁死该契约：按钮 stub 真实路径 3 档循环 + 进关不断档 + 跨局保留）
- **M3a 半浸加强**：叠层 0.55→**0.78** + 腰线亮带 `rgba(200,235,255,0.5)` 3px 横条
- **M4 铲除返还（偏离 GDD §6.2「无返还」，用户真机反馈后的事实拍板）**：返卡价一半向下取 25 的倍数、保底 25（豌豆返 50 / 睡莲返 25 / 向日葵·坚果返 25 / 双发返 100 / 西瓜返 75），toast `+N 阳光` 提示；SMOKE-025 T16⑤/⑤b 追加 ⑤a/⑤b-a 阳光断言（874+50 / 924+25）
- **入水水花（新）**：updateZombies 内 `z.splashed` 一次性标记，僵尸过草坪右缘（`x<=GRID_X+COLS*CELL_W+8`）触发 14 粒白蓝粒子 + `shockwave` 环（新 `water:true` 字段走水色渲染 `rgba(190,230,252,·)` 2.5px）；跋涉涟漪按 `R(0,1)<dt*1.2` dt 概率触发（帧率/倍速无关，约 0.8s 一次，无声防糊音）
- **入水音效 SFX.splash（新，偏离 GDD §7.1 零新音频）**：`noise(0.3,vol .14,lp1200,hp250)+tone(520→170)`，路由 `splash:'battle'`；AUDIO_ROUTES 与 verify-bus expect 表同步 +1 键（56 断言）；gate 节流 0.3s
- **四道门控全绿（数字全部 +1）**：烟雾 **26/26** · 回归 --all **56/56** · 总线 **56/56** · bench PASS；code-map 重跑（**2111 行**/45 常量）
- 验收表 `v12-acceptance.md` 已升级：新增 M3b（入水反馈 4 项）/ M3c（4x 3 项）章节，M4 返还预期更新，M6 门控数字刷新
- **工作树 12 文件未提交**（6 旧 + 新增 verify-bus/SMOKE-026/验收表/2 记忆文件），待用户授权提交；待办不变：M3b/M3c 真机复验 → 提交推送

## 📌 新会话接手指南（12:39 归档版，以此为准）

**当前状态一句话**：v1.2 V12-03（泳池关实现）+ V12-03b（真机反馈修复轮）全部完成，四道门控全绿（**烟雾 26/26 · 回归 --all 56/56 · 总线 56/56 · bench PASS**），源码 **2111 行**（code-map 已重跑同步），**工作树 12 文件未提交**，等用户两件事：① M3b/M3c 真机复验结果 ② 提交推送授权。

**12 个未提交文件**（`git status -s` 核实）：
- 源码：`plants-vs-zombies.html`（V12-03 主体 + V12-03b 五项修复）
- 文档：`docs/code-map.md`（重跑）、`docs/architecture/perf-profile.md`（bench 幂等回填，非源码改动）
- 测试：`tests/harness/cases/SMOKE-025.js`、`SMOKE-026.js`（新增）、`REG-END-02.js`、`SMOKE-023.js`、`tests/harness/index.js`（+2 桥）、`verify-bus.js`（+splash 路由）
- 产物：`tests/playtests/v12-acceptance.md`（新增，含 M3b/M3c）
- 记忆：`.workbuddy/memory/2026-09-18.md`、`MEMORY.md`

**提交建议（两刀，待授权）**：
1. 功能刀：源码 + code-map + 测试 6 文件 + 验收表（`feat(v1.2): 泳池关 V12-03+03b`，信息里写明 4x 修复根因与两处 GDD 偏离）
2. 记忆刀：`.workbuddy/memory/` 2 文件
- **git add 别带不存在的路径**；提交后必看 `git show --stat`；推送用 helper 覆盖命令（本文件上方「推送命令」节，照抄勿改）

**新会话开场动作**：
1. 问用户 M3b（入水水花/音效）/ M3c（4x 跨局保留）真机复验结果
2. 若复验通过 + 授权 → 按上面两刀提交推送 → `git ls-remote origin main` 核验
3. **遗留待办（不阻塞提交）**：① GDD 两处规格偏离须回写 `design/gdd/level-4.md`（§6.2 铲除返还、§7.1 splash 音频）② 架构文档两处漏洞修正回写 `docs/architecture/v1.2-pool-terrain.md`（water:true 门控 + 有垫拒铺垫）③ M5/M7（整局时长 / bench L4 末波场景）仍空着
4. 之后剩余流程见 `production/v1.2-plan.md`（版本号 v1.2.0 → 发布 → tag 推远端）

## 文档回写完成（12:44 接手会话，遗留待办①②清账）

- **GDD `design/gdd/level-4.md` v1.1→v1.2**：①版本头增补 V12-03b 回写记录 ②§6.2 E4 增补铲除返还规则（卡价一半向下取 25 倍数保底 25，豌豆 50/睡莲·向日葵·坚果 25/双发 100/西瓜 75，toast 提示，偏离 v1.0「无返还」原文并注明）③§7.1 结论改写「零新音频→唯新增 SFX.splash」（noise+tone 参数、路由 battle、gate 0.3s、verify-bus 55→56）+ 原「明确不做入水音」删除线保留历史判断 + §7.2 备忘同步 56/56
- **架构 `docs/architecture/v1.2-pool-terrain.md`**：①状态行 Delivered→Implemented ②§3 两处 ⚠️ 实现期修正注：水轴校验原案全局生效漏洞（water:true 门控三处 L687-688/L1319/L1640）+ 占用检查 lilypad 分支补齐（堵「有垫再点睡莲」，L686）③§4 清单 #4/#5 行同步修正注
- 回写事实全部与源码实况 grep 核对（返还公式 L670、splash 路由 L331、三处 water 门控）；未动源码与测试
- **工作树 14 文件未提交**（原 12 + 本轮 2 文档回写）；提交建议升级为三刀：①feat 功能刀 ②docs 规格回写刀（GDD+架构）③记忆刀
- 仍待用户：M3b/M3c 真机复验结果 + 提交推送授权
- **12:5x 用户答复：尚未真机复验，暂不提交**——工作树 14 文件保持现状；下次推进 = 用户回传 M3b/M3c 复验结果 + 提交授权
- **12:56 用户复验回传（M3b/M3c 定案）**：
  - **M3c ✅ 通过**（4x 跨局保留三行全过）——4x 修复闭环
  - **M3b 功能可用判通过，沉淀 3 项优化转下次迭代（用户明确本轮不改）**：①入水声改「噗通」②游动途中「哗哗哗」水声（现为刻意无声）③水中僵尸应看不见腿（现半浸仅色偏，腿仍可见）
  - 已回填 `tests/playtests/v12-acceptance.md`（M3b 4 行逐项 + 结论注 + M3c 三行全 ✅）
  - 已登记优化池 `production/v1.2-plan.md` §六（O1 噗通 / O2 哗哗哗 / O3 腿部遮挡，含现状→期望与涉及面）
- **仍挂起：提交推送授权**——工作树 **15 文件**（git status 实核：13 M + 2 ??，新增 production/v1.2-plan.md 优化池改动；v12-acceptance.md 本就是 ?? 新文件）下次开场直接问授权即可，三刀建议不变（功能 / 文档回写 / 记忆）

## 提交推送完成（13:07 用户授权「推」，13:0x 落地）

- **三刀提交**：`2b91552`（feat 功能刀：源码+code-map+perf-profile+测试 6 文件+验收表，10 文件 +694/-176）/ `02162ca`（docs 规格回写刀：GDD+架构+v1.2-plan 优化池，3 文件）/ `44bc8cb`（memory 归档刀，2 文件）
- **推送成功**：`96b1de4..44bc8cb main -> main`，`git ls-remote origin main` 权威核验一致（`44bc8cba…b131`）
- **本次推送无坑**：工作树全净、`## main...origin/main` 无 ahead/gone——packed-refs 修复法本轮未触发
- **v1.2 S2 实现轮彻底收官**：代码+测试+验收+规格回写+优化池全部入库；剩余流程 = M5/M7（整局时长/bench L4 末波）→ 定版发布 v1.2.0 → tag 推远端（v1.2-plan S3）

## M7 bench 场景 D 完成（13:1x 继续推进，提交 `6419034` 已推远端）

- **bench.js 增设场景 D**（v1.2-plan S3 / 验收表 M7 7.2 专项）：setLevel(4) + `setDiff('expert')` + 15 植物（含 2 睡莲垫走真实渲染叠层）+ 30 僵尸（含 2 bucket 地狱 hp 1008），构成对齐 L4 W8 双桶压轴（GDD E10）；加载器补绑 `setDiff`（此前 bench 返回对象漏了它）
- **结果 PASS**：p50 0.34ms / p95 0.86ms（**5% 预算**）/ max 1.55ms，1970 draw calls 全口径 / 1335 估算口径，帧异常 0；较场景 C（1155 估算口径）+15.6% ≈ 实体增幅（僵尸 +20%/植物 +25%）线性吻合 → **水景渲染无超线性成本**（水面波纹/半浸/水花路径全走了一遍）
- **perf-profile.md 两处增补**：§7 自动段（bench 幂等回填，含场景 D 行）+ §1.4 末尾手写「V12 增补」注（静态估算基于 L1-L3，D 实测补上 L4 口径）
- **验收表 M7 7.2 勾 ✅**；7.1 真机体感留给用户打整局时顺带看
- **动态损耗披露**：场景 D 植物终态 14/15（pea@3,4 采样窗尾被啃食，前 95% 窗口满编），诊断脚本实证非 bug
- 三道门控复跑全绿（烟雾 26/26 · 回归 --all 56/56 · 总线 56/56）；推送 `48b1ce1..6419034`，ls-remote 核验一致，工作树全净
- **剩余**：M5（用户真机打整局看时长/体感 + M7 7.1）→ 用户点头 → 定版 v1.2.0 发布（RELEASE-NOTES + 发布包 + tag 推远端）

## 🎉 v1.2.0 定版发布完成（13:24 用户「点头」授权，13:3x 落地）

- **发布包 `production/release/v1.2/`**：RELEASE-NOTES（定稿 v1.0，七节）/ KNOWN-ISSUES（O1-O3 优化池 + N-12a/b 发布后跟踪 + 4 处规格偏离披露）/ RELEASE-CHECKLIST（A-F 六节，Go/No-Go 判定 GO）+ artifacts 冻结副本
- **冻结副本**：`plants-vs-zombies.v1.2.html`，2111 行 / 89,296 字节，**LF SHA-256 `ebbfdb34…4ffb1`**（完整 `ebbfdb34164d50c8473daee8aa01e2edb816443cb5c87b2f2b5e6d0cf264ffb1`），已 cmp 逐字节核验与 git 对象一致；**只读，不得改动**
- **标签 `v1.2.0` 已推远端**（tag 对象 `265f72d`，commit `1398186`）；`git ls-remote` 权威核验一致；packed-refs 坑未复发
- **README 已同步**：第四关泳池 / 7 种植物 / 水行规则 / 铲除返还 / 4x 跨局保留 / 关卡示例 L4→L5 / 菜单基准 190 口径 / 变更记录 v1.2 条目
- 提交链：`6419034`（bench D）→ `dda0375`（记忆）→ `1398186`（release 包 + README）→ tag `v1.2.0`
- **v1.2 正式收官**。版本线现状：v1.0.0 / v1.1.0 / v1.2.0 三条标签均在远端；**线上最新版 = v1.2.0（2111 行，四关七植物）**
- **遗留（不阻塞）**：①M5 真机整局 + M7-7.1 体感（N-12a，用户游玩时顺带）②软验收 3 项（N-12b）③优化池 O1-O3（下次迭代：噗通音/哗哗哗水声/腿部遮挡）

## 📌 新会话接手指南（13:4x 归档版，以此为准）

**当前状态一句话**：**v1.2.0 已定版发布并推远端**（tag `265f72d` / commit `1398186`，冻结副本哈希 `ebbfdb34…4ffb1`），工作树全净，无未授权动作挂起。

**v1.2 全程回顾**：方案 A 泳池 → V12-01 GDD → V12-02 架构 → V12-03 实现（+91 行）→ V12-03b 真机反馈修复（4x/半浸/返还/水花/splash）→ 三处文档回写 → M7 bench 场景 D → 定版发布。全程四道门控全绿（烟雾 26 · --all 56 · 总线 56 · bench PASS）。

**剩余待办（全部不阻塞）**：
| # | 事项 | 触发条件 |
|---|---|---|
| 1 | M5 真机整局（时长 ≈4min？）+ M7-7.1 体感 | 用户下次游玩回传，回填 `v12-acceptance.md` |
| 2 | 软验收 3 项（startSun=150 / 睡莲 CD / W8 双桶可清性） | 随游玩观察，有预设回退阀 |
| 3 | 优化池 O1-O3：入水声改噗通 / 游动哗哗哗 / 腿部遮挡 | 下次迭代立项（`v1.2-plan.md` §六） |

**硬规则（继续有效）**：①冻结产物只读，热修走新版本流程 ②改代码先查 code-map → 精确编辑 → 重跑生成器 ③高影响动作先获授权 ④改源码后四道门控必须全绿 ⑤推送用 helper 覆盖命令（照抄勿改）

**下次迭代候选**：优化池 O1-O3 / 屋顶地形线 / 更多植物（樱桃、寒冰）/ 移动端触屏

## v1.2 发布后跟踪闭环 + 三项长期决策（13:44 用户回传与拍板）

- **M5 ✅**：普通 ≈4 分钟（符合预估 3:55-4:20）+ **地狱 ≈7 分钟**（1.8x 血量拉长战线，新数据点）；M7-7.1 ✅ 通过（高密度无拖帧）；软验收 3 项 = **不空/顺/能**，两处回退阀均不触发。验收表全绿收官，**N-12a/b 闭环 → v1.2 彻底收官（含发布后跟踪）**
- **O3 约束升级**：水中腿部遮挡动手前**必须先出方案（改动面/风险/视觉对比）供用户审核**，通过后方可施工
- **N-13 多语言 / N-14 移动端**：用户拍板**不做、优先级最低、先以 PC 端为主**；仅大版本发布时确认要不要做——已记 KNOWN-ISSUES v1.1（🧊 冻结态）
- 提交待办：验收表 + KNOWN-ISSUES 更新待推送（小改动，与下次动作合并或单独推）

## 优化轮开工（14:39 用户「开始优化轮」）——O1/O2/O3 方案已出待拍板

- 定位侦察完成：SFX.splash（L477-482）/ updateZombies 入水反馈（L1060-1074）/ drawZombie 半浸（L1639-1645）；AUDIO_ROUTES L328-333；verify-bus 只锁「键→分组」（O1 调参零测试改动，O2 新键需 SFX+路由表+verify-bus 三处同步）
- 方案：O1 = 原地重调 splash 参数（低频 300→70 噗通主体+轻噪，键名不动）；O2 = 推荐 A 连续水声 loop（WadeLoop 帧驱动仿 SirenLoop，battle 组低音量）/ 备选 B 间歇轻 tick；O3 = 推荐甲案 clip 裁切（drawZombie 内 +12 行，水线 16px rect 裁切）已出 SVG 对比图供审
- 版本线：本轮 = v1.2.0 后新版本流程（v1.2.1 候选）；O3 按用户 13:44 拍板「先审后工」，等用户选择

## v1.2.1 复验裁决与返工（15:2x 用户真机反馈「音效不对…先不做了先记录，腿部你要改」）

- **裁决**：O1 噗通/O2 涉水声两案真机均不达预期 → **合成路线冻结，全部回退 v1.2.0 原状，换方案前不再做**；O3「腿可见」必须修
- **O1/O2 回退清单**：splash 参数还原（noise+tone 520→170）/ SFX.wade 键删除 / WadeLoop 对象+update tick+startGame 复位+updateZombies 计数全拆 / AUDIO_ROUTES 去 wade / scheduleNoise 去 _wadePad 参数与淡入淡出分支 / verify-bus expect 表回 56
- **O3「见腿」根因剖析（像素探针实证）**：工作树版裁切代码在无头浏览器中真实生效（水面区腿区采样=水面色）。用户看到腿的两因：①很可能开着**旧画面残留标签页**（v1.2.0 无裁切）②真 bug = **inWater 未看 x 坐标**——水行僵尸在右侧路面段（x>865）被误裁成"半截站在路上"，视觉即"腿没了/不对劲"
- **O3 修复**：inWater 补 `z.x<=GRID_X+COLS*CELL_W+8`（与 updateZombies 入水水花同边界）→ 路面段不裁、入水后才裁
- **三态验证 `tests/playtests/o3-final-verify.js`**（Edge headless+CDP 注入三僵尸+像素判读）：A 水面区水行腿区=水面色 ✅被遮 / B 路面段腿区=身体色 ✅不裁 / C 陆行 ✅完整；截图证据 `tests/playtests/o3-zombie-shot.png`（供用户肉眼复核）
- **回退后口径**：源码 **2121 行**（2111+10，仅 O3）/ 45 常量 / VERSION 仍 v1.2.1（版本号保留，内容只含 O3）/ 门控：烟雾 26 · --all 56 · **总线 56**（回 56，与 v1.2.0 持平）· bench PASS；code-map 已重跑
- **文档回写**：GDD §7.1（7.1 结论改回唯 splash 一键+冻结注+水面环境音「否决维持」）§7.2（56/56）；v1.2-plan §六（O1/O2 🧊冻结+换方案候选：预录采样嵌 base64 违背零依赖需拍板 / 物理建模；O3 已修+三态验证）
- **工作树 9 文件**（8 旧 + o3-final-verify.js 新）未提交待授权；**待用户**：①打开游戏文件确认是最新版（标题栏/左下角 VERSION=v1.2.1）再看腿部效果 ②O3 真机复核 → 授权提交

## O3 复验通过 + O4 僵尸形象优化登记（16:07 用户「刚测试了下，没问题了」）

- **O3 真机复核 ✅**——v1.2.1 内容定稿为仅 O3（2121 行 / VERSION v1.2.1）；工作树 9 文件继续待授权提交
- **新登记 O4「僵尸形象精细化」**（用户原话「现在的僵尸比较丑，后期需要优化……能不能像植物大战僵尸一样精美好看」）：登记 `v1.2-plan.md` §七，三案待拍板——
  - 甲 · 程序化精绘（零依赖守住）：canvas 2D 深挖渐变/高光/关节动画/材质，+150~300 行，上限 ★★★☆
  - 乙 · 像素/矢量贴图 base64 内嵌：观感最直接 ★★★★☆，但**破坏零外部资源红线**，需资产管线
  - 丙 · 混合渐进：骨架程序化+头身贴图+装备矢量叠加，复杂度最高 ★★★★
  - 原版是 Spine 骨骼动画+手绘插画，单文件复刻不现实，合理预期 = 精致风格化卡通
  - **决策联动**：O1/O2 音效若走采样 base64 换方案，与乙案共享「资产内嵌」决策，可同批拍板一次定两线
- **O4 路线初步落定（16:14 用户拍板）**：走 **乙案方向 · AI 生成资产**，分工 = WorkBuddy 写提示词 → 用户免费额度 AI 生图 → WorkBuddy 评估（透明通道/风格一致性/帧间检查）→ 通过后 base64 内嵌。资产需求清单+评估流程已写入 `v1.2-plan §七`（透明 PNG / 4 僵尸同 prompt 骨架锁画风 / 朝左 / ≥256px / 装备可拆更佳 / 禁水印）。零资源红线口径变化须在 README/KNOWN-ISSUES 披露（文件仍单文件无外部请求）

## 发版检查清单增设「优化池核对」节（16:26 用户拍板「以防忘记」）

- **`v1.2-plan.md` 新增 §八**：P1-P5 核对表（O1/O2 冻结项、O3 已闭环、O4 待启动、N-13/N-14 大版本复核）——每次定版发布，RELEASE-CHECKLIST 在 Go/No-Go 前必须逐项给出「已处理 / 不处理+理由」，不许空缺
- 执行方式：登记源 = §八（实时更新），各版本发布包内放快照；自下个发布包起生效（v1.2.1 若发正式版即首次执行）
- 长期记忆 MEMORY.md 已同步该流程（★ 标记），新会话接手不会漏

## 📌 新会话接手指南（16:50 归档版，以此为准）

**当前状态一句话**：v1.2.1 优化轮内容定稿（仅 O3 腿部裁切，2121 行 / VERSION=v1.2.1），**O3 已用户真机复核通过（16:07「没问题了」）**，四道门控全绿（烟雾 26 · --all 56 · 总线 56 · bench PASS），**工作树 10 文件未提交，等用户授权提交**——这是唯一挂起动作。

**10 个未提交文件**（`git status -s` 实核）：
- 源码：`plants-vs-zombies.html`（O3 裁切 + inWater x 边界修复 + VERSION v1.2.1；O1/O2 已回退干净，diff 仅 +10 净行）
- 文档：`docs/code-map.md`（重跑 2121 行）、`docs/architecture/perf-profile.md`（bench 幂等回填）、`design/gdd/level-4.md`（§7.1/7.2 回退口径+冻结注）、`production/v1.2-plan.md`（§六复验裁决定稿 + §七 O4 登记 + §八发版核对节）
- 测试：`tests/harness/verify-bus.js`（回 56，仅注释变更）、`tests/playtests/o3-final-verify.js`（新，三态验证脚本）、`tests/playtests/o3-zombie-shot.png`（新，验证截图）
- 记忆：`.workbuddy/memory/2026-09-18.md`、`MEMORY.md`

**提交建议（两刀，待授权）**：
1. 功能刀：源码 + code-map + perf-profile + verify-bus + o3-final-verify.js + 截图 + GDD + v1.2-plan（`fix(v1.2.1): O3 水行僵尸腿部裁切`，信息写明 inWater x 边界修复 + O1/O2 回退裁决）
2. 记忆刀：`.workbuddy/memory/` 2 文件
- git 硬规则照旧：add 别带不存在路径、提交后看 `git show --stat`、推送用 helper 覆盖命令（本文件上方或 MEMORY.md「推送命令」节，照抄勿改）、推完 `git ls-remote` 核验

**v1.2.1 是否发正式版（RELEASE-NOTES+发布包+tag）待用户定**：内容只有 O3 一项；若发，RELEASE-CHECKLIST 首次执行 §八优化池核对节

**后续迭代候选（全部已登记，不阻塞）**：
- **O4 僵尸形象精细化**（用户 16:14 拍板 AI 资产路线）：用户说「出 O4 提示词」→ 按 §七约束清单写首版（建议先出单帧普通僵尸验画风）；随后评估→内嵌
- **O1/O2 音效换方案**：与 O4 共享「资产内嵌」决策；若 O4 管线打通，采样方案随之解锁
- 屋顶地形线 / 新植物（樱桃、寒冰）/ 新关卡（REG-END-02 平移）

**硬规则（继续有效）**：①冻结产物只读 ②code-map 定位→精确编辑→重跑生成器 ③高影响动作先授权 ④门控全绿（烟雾 26/--all 56/总线 56/bench PASS）⑤改源码走新版本流程 ⑥发版前过 §八优化池核对

## 归档（17:5x 换机版）：两刀提交推送闭环 + 发布线挂起

> **📌 后续更正（2026-09-19/20）**：本节「v1.2.1 正式版发布未完成 / tag 未打」已被后续会话推翻——发布包 `production/release/v1.2.1/` 与 tag `v1.2.1`（锚 `f5d660d`）均已落地并推远端。保留原文作历史记录，**以 `2026-09-19.md` 与 MEMORY.md 当前状态节为准**。

- **工作室流程启用**：用户要求按工作室推进；Phase 0 诊断 = 处于 Phase 6 打磨尾声 → Phase 7 发布切换点；烟雾 26/26 实测复跑通过
- **用户三项拍板**：①授权两刀提交 ②提交后即推送 ③v1.2.1 发正式版（发布包+NOTES+tag）
- **两刀已执行并推送成功**：`6da6b2c` 功能刀（源码+文档+测试 8 文件，+228/-108）+ `8e611d2` 记忆刀（2 文件）；`84e9b6d..8e611d2` 远端核验一致，工作树全净
- **推送排障实录（方法论沉淀）**：首推失败 = `~/.gitconfig` 有 `http.https://github.com.proxy=127.0.0.1:7890` 而加速器没开——TCP 443 通但 TLS 握手被重置、ssh.github.com:443 也被掐、7890 端口 Connection refused；**用户开加速器后用原 helper 覆盖命令一次成功**。教训：push 挂先看 7890 活不活，活=网络问题，死=先问用户加速器
- **v1.2.1 发布未完成（唯一挂起，后已闭环）**：release-ops-lead 被会话中断，**零持久产出**（`production/release/v1.2.1/` 不存在、tag 本地/远端均未打，主理人已实证核验）；成员回报确认无中间状态需清理
- **新会话接手 = 重跑发布四步**（用户授权仍在）：①复跑四道门控 ②建发布包四件套（**首次执行 §八优化池核对节**：P1(O1)/P2(O2) 冻结不处理+理由、P3(O3) 已处理、P4(O4) 待启动不处理+理由、P5(N-13/N-14) 非大版本不处理）③tag v1.2.1 打于 HEAD 并推远端核验 ④发布包文件 commit 前再请示
- **发布包规格（照 v1.2 惯例）**：`artifacts/plants-vs-zombies.v1.2.1.html`（源文件冻结副本）/ RELEASE-NOTES.md（O3 修复为主，O1/O2 裁决与 O4 登记入档）/ KNOWN-ISSUES.md（O1/O2 冻结、O4 待启动、N-13/N-14 冻结、v1.2 遗留 M5/M7-7.1 跟踪项继承）/ RELEASE-CHECKLIST.md（A 门控实测 + 优化池核对节 + B 真机验收（O3 16:07 用户通过）+ C 产物 + D 回滚（退 v1.2 副本）+ E sha256）

## 21:14 开工轮（新会话）：O4 提示词首版落盘

- 开场 `git pull`：`96b1de4..8e611d2` fast-forward 同步（v1.2.1 复验归档版 + tag `v1.2.0` 拉到本地）；packed-refs 坑复发一次（虚报 `[ahead 12]`），按既定 sed 法修复，`## main...origin/main` 干净
- **接手指南「10 文件待授权提交」已由上一会话落地**（`6da6b2c` fix 刀 + `8e611d2` memory 刀），工作树全净——记忆文件本节即纠偏记录
- **O4 提示词首版 v1 落盘：`design/assets/o4-zombie-prompts.md`**（用户 16:14 拍板 AI 资产路线后的第一步交付物）。要点：
  - 首版只出 1 张 normal 单帧站立验画风；同批 4 类用同工具同模型只换装备句
  - 硬约束全数内嵌：透明 PNG（不支持则纯白底、勿用绿底防撞肤色）/ 朝左 / 全身不裁边 / ≥1024px / 禁水印 / 头大身小 PvZ 比例
  - 英文主提示词 + 负向词 + 中英四类装备句 + 中文版（即梦/豆包路线）+ 回传清单（原始 PNG 勿截图 + 工具模型名）
  - 画风锁定词从 drawZombie 实色反推：肤色 sage-green `#c5d0a0`、橄榄绿西装 `#5a6a4a`、cone 橙 `#d97b2a`、bucket 灰 `#b0b0b0`、fast 红 `#c04040`
  - 附评估基线（运行时 96×128、腰线裁切 y=16、内嵌预估 +40~80 行）
- **下一动作**：旭峰拿提示词生图 → 回传原图 → 四步评估（透明/配色目检/帧一致性/结论）→ 通过后 base64 内嵌施工

## 21:21 O4 冻结裁决（用户拍板）

- **用户拍板：O4 🧊 冻结，等版本稳定后与 O1/O2 入水音效同批统一解决**（原话大意：「这个先冻结，等后面统一修改。因为现在版本还不稳定。先记录下来，和僵尸入水的声音一起解决到时」）
- 已落盘：`v1.2-plan.md` §七 冻结注 + §八 P4 状态更新（🧊 冻结 + 提示词已备）；`MEMORY.md` 当前状态节重写（工作树全净 + O1/O2/O4 三项捆绑同批）
- **捆绑逻辑**：O1/O2（采样 base64 音效）与 O4（AI 贴图 base64 内嵌）共享同一「资产内嵌」决策，版本稳定后一次拍板定两线
- 提示词资产保留：`design/assets/o4-zombie-prompts.md` 解冻即用，无需重写
- 当前无挂起动作；下一步方向待用户指令（候选：屋顶地形线 / 新植物 / 新关卡）

## 📌 新会话接手指南（21:25 归档版，以此为准）

**当前状态一句话**：O4 提示词首版已落盘并**随冻结登记一并推远端**（commit `8cb8f87`，ls-remote 核验一致），工作树全净，无任何挂起动作。

**今日（09-18）全程回顾**：v1.2.0 定版发布 → 发布后跟踪闭环（M5/M7-7.1/软验收）→ v1.2.1 优化轮（O1/O2 被否回退 + O3 修复通过）→ O4 登记+路线拍板+提示词落盘 → **O4 冻结（21:21，与 O1/O2 捆绑同批）** → 归档推送 `8cb8f87`。版本线：v1.0.0 / v1.1.0 / v1.2.0 三 tag 在远端；v1.2.1 内容定稿在 main（2121 行，未发正式版、未打 tag——是否发正式版待用户定）。

**🧊 冻结项（下次迭代候选，全部登记在 `v1.2-plan.md` §六/§七/§八）**：
| 项 | 状态 | 解冻条件 |
|---|---|---|
| O1 入水「噗通」+ O2「哗哗哗」 | 合成路线已否，回退 v1.2.0 原声 | 版本稳定后与 O4 同批拍板（候选：采样 base64 / 物理建模） |
| O4 僵尸 AI 贴图 | 提示词已备 `design/assets/o4-zombie-prompts.md`，解冻即用 | 同上，三项共享「资产内嵌」决策 |
| N-13 多语言 / N-14 移动端 | 不做、优先级最低 | 大版本（x.0.0）发版时确认 |

**发版固定流程（勿漏）**：定版发布前，RELEASE-CHECKLIST 在 Go/No-Go 前必须过 `v1.2-plan §八` 优化池核对节（P1-P5 逐项给「已处理/不处理+理由」）。

**下一步方向候选（待用户指令）**：屋顶地形线 / 新植物（樱桃、寒冰）/ 新关卡（REG-END-02 锁 LEVELS[5] 需平移）/ v1.2.1 是否发正式版。

**硬规则（继续有效）**：①冻结产物只读 ②code-map 定位→精确编辑→重跑生成器 ③高影响动作先授权 ④门控全绿（烟雾 26/--all 56/总线 56/bench PASS）⑤改源码走新版本流程 ⑥发版前过 §八优化池核对 ⑦推送 helper 覆盖命令 + ls-remote 核验 + packed-refs sed 修复法（本轮又复发一次，已修）

## 屋顶地形线立项（21:33-22:47 新手会话，方案 B 中量版）——已归档

- **用户指令「屋顶地形线」→ 先做可行性/设计讨论 → 拍板「搞下B中量版」，本轮已归档待重开会话后实现**
- 路线：可行性草稿 `design/gdd/roof-process.md`（文策渊）→ 定稿 GDD `design/gdd/level-5.md`（v1.1）+ 技术可行性 `docs/architecture/v1.3-roof-projectile.md`（程基岩），G1 设计+架构评审 PASS
- **关卡定位（LEVELS[5]）**：决战二幕，9 波 / 42 只 / 3 大波（W3/W6/W9），僵尸零新增；差异化 = **弹道轴**（卷心菜投手抛物线）+ **全场地盆税**（避免换皮泳池）
- **核心决策（用户隐含采纳推荐）**：花盆 = 睡莲式实体占位（近逐行复用泳池路径，成本还更低）；投手 = 卷心菜 cabbage-pult（150费 / 2.0s / 30dmg / 纯抛物线单目标，刻意不做西瓜翻版）；坡度 = 轻视觉平面皮肤（不破坏 gridToPos）；卡片 6→8
- **抛物弹道关键规格（§3.4，已吸收程基岩 CONCERN）**：`t0=d0/vx; lead=target.x−spd_row·t0; d=max(50,lead−spawn.x); t=d/vx; vy0=(zy−spawn.y−0.5·g·t²)/t`（vx=260 / g=500）；命中手感「移动目标接近必中」，唯一 miss = 选错行/eating 态切换；§3.5 投手数值零连带
- **威胁发现（程基岩 CONCERN → 已闭环）**：抛物按目标当前 x 解算 vy0 会打不中移动僵尸（normal 2.77s 左移 44px / fast 125px > 命中窗 ±42px）→ 采纳预测提前量修正，T17 升级「移动目标必中」结算锁
- **改动面**：约 130–185 行（不含测试），抛物是唯一引擎核心触点（updateProjectiles L1010-1036 加 vy/g 独立 cabbage 分支，**直线弹路径零改动保老 REG**）；花盆近逐行复用睡莲
- **门控影响**：SMOKE-027（17 条）+ REG-PULT-01/02（新）+ SMOKE-025 T15 卡数 7→8 平移 + REG-END-02 锁 `LEVELS[6]===undefined` 平移；音效复用 melonThrow / SFX.plant 零新键 → verify-bus 维持 **56/56**
- **里程碑版本线**：L5 屋顶 = v1.3 候选（`level-5.md` 头标注 v1.3 Sprint）；待用户定版本号与排期
- **遗留**：①S1-S3 冲刺拆分（仿 V12-* 编 V13-*）未派单 ②源码未动（GDD+架构均已落盘，工作树新增 3 文档未 commit）③v1.2.1 未发正式版（等用户定）
- 团队 `_auto_f9d1212c` 两成员（design-strategist / engineering-lead 系）本轮任务完成，重开会话视需要 TeamDelete 或复用

## 屋顶线 S1 开工会话（22:56 用户「继续屋顶线，先授权提交三份立项文档，然后做 S1 地基与护栏」）

- **授权落地两刀提交**：`1f71aae`（立项三件套：GDD level-5 + roof-process + v1.3-roof-projectile，+1099 行）/ `472443f`（V13-S1 产物：v1.3-plan + v13-s1-foundation-review + GDD/架构/草稿勘误，+352/-14）。未推送（推送须单独授权）
- **v1.3-plan.md 落盘**：S1 地基护栏 / S2 内容实现 / S3 验证发布，任务编号 V13-*；团队 `pvz-v13` 已建（本轮仅 spawn engineering-lead）
- **V13-S1 完成（程基岩 9m14s 回传，G2 护栏评审 PASS）**，产物 `production/v13-s1-foundation-review.md`：
  - **卡数裁决 = 9**（7+2=9；GDD/架构「7→8 / 总数=8 / 热键8」全为 off-by-one 笔误）——主理人汇编时执行 V13-S1b 全量勘误：GDD v1.1→v1.2（§1.1/§3.0/§8.1-T15/§8.2-M4）、架构 §0/§3.2/§4.2/§4.3/§7、草稿加注（B 档口径 9 张）；planter@7、cabbage@8（纠正架构原 cards[6]/[7] 顺位误写）
  - 数据契约：`roof:true` 独立布尔（不引 terrain 枚举，water/roof 互斥前置）；CARDS append 硬约束（前 7 张索引不动）；`CABBAGE_VX=260/CABBAGE_G=500` 声明于基础常量区（CARD_X0 后）
  - 布局验算：9 张右缘 958<1000 余 42px，卡栏在底部 y≈600-678 与顶部 HUD 垂直零重叠 → **零布局改动**
  - 回归加固规格：SMOKE-027（T1-T16 断言表，数值与 GDD §8.1 全部核对一致）+ REG-PULT-01（静止/移动命中，注入式含完整代码）+ REG-PULT-02（cabbage y 变化 / pea、melon y 恒定）+ REG-ROOF-01（主理人拍板独立用例）——**harness 零新桥**（现有 __projectiles/__zombies/__consts 够用）
  - 平移清单：SMOKE-025 T15 `===7`→`===9` + cards[7]/[8] 补断言；REG-END-02 setUnlocked/setLevel 4→5、forceWaves 8→9、锁 LEVELS[5]→[6]
  - S2 六步施工顺序（数据→校验→行为→渲染→测试→收尾）+ 红线（第一生命线 = updateProjectiles 重力分支必须 `if(pr.type==='cabbage')` 前置；CARDS 只 append；测试与实现同批全绿不落红态）
  - **主理人两项裁决**：①卡数=9 ②门控口径回归 56→59（REG-ROOF-01 独立回归用例：种植校验完整点击路径版，SMOKE-027 T16 保留轻量抽检）
- **门控目标**：烟雾 26→27 · 回归 56→59 · 总线 56（零新 SFX 键）· bench 四→五场景（+场景 E：L5 屋顶末波）
- **工作树余 2 记忆文件未提交**（本轮会话日志）；**下一步 = V13-S2 实现派单（等用户指令）**；本地两提交未推送

- **提交授权执行**：docs 两刀（①立项三件套：GDD level-5 + 架构 v1.3-roof-projectile + 草稿 roof-process；②记忆归档）——哈希见 git log；**未推送**（推送未授权，待与 S1 产物合并或单独授权）
- **`production/v1.3-plan.md` 落盘**（主理人汇编）：S1 地基与护栏 / S2 内容实现 / S3 验证与发布（bench 场景 E + 定版 v1.3.0 + 过 v1.2-plan §八优化池核对）；任务编号 V13-*；质量门 G1✅→G2 护栏评审→G3 门控全绿→G4 Playtest→G5 发布签字
- **⚠️ 主理人一致性检查发现「卡数矛盾」**：GDD §1.1 写「8 张（+花盆+投手）」、架构 §3.2 写「卡数 7→8」，但 +2 卡事实为 7+2=**9**；受影响面 = SMOKE-025 T15 断言值 / onKey 热键上界（'1'-'9'?）/ drawCardBar 布局余量（76+9×98=958 vs canvas 1000，HUD 间距）/ 菜单文案处数——V13-S1 派程基岩裁决，定案后 V13-S1b 派文策渊勘误 GDD
- 团队 `pvz-v13` 建立；**V13-S1「地基与护栏」派程基岩**（P0）：①数据契约定稿（roof:true / CARDS append 顺序 planter→cabbage / 常量 CABBAGE_VX=260·CABBAGE_G=500）②卡数矛盾裁决 ③回归加固规格（SMOKE-027 断言表 + REG-PULT-01 静止/移动命中 + REG-PULT-02 分支隔离 + harness 桥需求 + 平移清单）④S2 六步施工顺序 + 红线清单；产物 `production/v13-s1-foundation-review.md`（成员只读回传，主理人落盘）
- S1 决策：**测试骨架不落红态**（守「门控全绿」纪律，仿 V12-03 验证驱动在 S2 内先测后实现）；源码未动
