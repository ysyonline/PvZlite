# PvZ Lite 工作日志 · 2026-09-25（跨午夜，承接 09-24 晚班 v2.1 M3）

## 00:0x–01:0x 原开发机 weixufeng · M3 内容轴闭环 ✅（提交 `556876f`，未推送）

### 编排
- 建团队 `pvz-v21-m3`；主理人（我）做阶段诊断 + 派单 + **独立事实核验**，工程活全部由 engineering-lead 承担
- 三刀连做：T-301 画像 → T-301b 补写 → T-301c 放软，最终**合并为一个提交**（用户令「等 2-4 定了一起提交」）

### T-301 · 20 关难度曲线画像
- 口径（用户拍板）：20 关 × 4 种子 = 80 局；**普通档 bot**；**阳光不锁（真实经济）**
- 产物：`tests/playtests/v21-curve-profile.js` / `-results.json` / `production/v21-curve-profile-report.md`
- **★核心发现 = 1-3 / 1-4 / 1-5 是模板占位关**：`_ANCHORS` 顶层键缺这三关 → `materializeLevels()` 用世界模板深拷贝补齐 ⇒ waves **与 1-1 逐字节相同**；教学串行门又只对 `'1-1'/'1-2'` 生效 ⇒ 这三关连减速门都没有，实测比 1-1 还快（150s vs 165.5s）；世界 1 曲线在 1-2(4760) 后塌回 2660，ramp 0.51/0.41/0.35（门限 0.70）
- 次级：无「明显过难」关；世界 2 曲线健康（建造需求 33~41 株 vs 世界 1 的 15~29 ⇒ 睡莲税+密度阶梯有效）；1-7/1-9/2-4 各 1/4 种子失守但首败在 W1~W4 + 渗透中位仍 225 ⇒ 判种子级方差

### T-301b · 曲线修补 + 加密复核
- **Part A**：`_ANCHORS` 18→21 键，插入 1-3/1-4/1-5（threat 5480/6300/6980，ramp 1.057/0.977/0.905 全 >0.70；限 normal/cone/fast，bucket 教学位仍留 1-7）；世界 1 耗时中位恢复**严格单调**（226.3 → 237.3 → 265.8 → 277.3 → 288.8…）；`git diff --numstat` = **47/0 纯插入零删除**
- **Part B 加密到 8 种子**：1-7 = 1/8 间歇、1-9 = 1/8 间歇 ⇒ 种子方差不动；**2-4 = 3/8（37.5%）稳定失守**（W1/W2/W2，t=54/76/84s）⇒ 需调
- 回归：harness 58/58；`v20-level-registry` PASS+旧源必红；`v20-harness-compat` PASS+旧 harness 必红；`v21-world2-open` PASS；**全 tests/ 无脚本锁定这三关 waves 快照 ⇒ 零迁移**

### T-301c · 2-4 开局放软
- 用户三选一取「放软首波」：`_ANCHORS['2-4']` 的 W1 由 `[normal×2, fast×1]` 改为 **`[normal×2]`**（fast 移出，W2 不动）⇒ threat 10500→10360（−140 / −1.3%），夜段整体难度未动；单调性 2-3(9320) < 2-4(10360) < 2-5(12620) 保持
- **⚠ 指令歧义自纠**：我的派单同时写了「fast 移到 W2」与「threat −140」，二者数学不一致（移=净 0 / 删=−140）；工程侧按明确数值期望实现为「移出」，我认可（在用户选项描述「或改 W1:normal×2」范围内）。**教训：派单给目标数值与给操作方案必须二选一，混写会产生歧义**
- 复跑：2-4 失守率 **37.5% → 12.5%**（≤1/8 达标），首败波次 W1/W2/W2 → **W3**；全 20 关离群判定**四类全空**（过难/偏难/过易/超载 + 稳定失守）；残留单局失守 1-7/1-9/2-4 各 1/8 = 信息项
- **判定器口径细化**：把「失守」从「任意 1 局抵达进屋带」收紧为「**稳定 ≥2 局**」，单局孤立降级为 `singleBreach` 信息项；合成自检扩为 6 用例全 PASS
- code-map 重刷 **3884 → 3931 行**

### 提交
- `556876f`（full `556876f543ba1f02212503ad5f9f1eab3da23b42`）
- 5 文件：`docs/code-map.md` / `plants-vs-zombies.html` / `production/v21-curve-profile-report.md` / `tests/playtests/v21-curve-profile-results.json` / `tests/playtests/v21-curve-profile.js`（5005 insertions / 199 deletions）
- 主理人核验：`git show --stat` 恰好 5 文件无卷带；工作树仅剩 memory 记账；`ahead 1`（**未推送**，等用户令）

### ★ 可复用沉淀（画像/度量类任务）
- **基建**：`tests/playtests/lib/prelude.js` 全套可复用（`loadGame{seed,search:'?test=1'}` / `g.tick(DT)` 外置时钟 / `probe()` 读态 / `selectCard+clickGrid` 真种植路径）；关卡寻址 `g.setLevel('2-6')`
- **★bot 策略坑**：初版「首个可负担」贪婪 ⇒ **80 局全败**（睡莲 25 / 向日葵 50 持续偷走豌豆 100 预算，防线仅建 7~13 株）；须「**严格优先级 + 买不起/冷却中就等**」
- **★锁阳光会毁掉判别力**：锁阳光后 bot 铺满防线、80 局全胜、曲线恒平——经济维度（startSun + 睡莲税）是世界 2 的核心难度
- **★内容缺口检测法**：难度画像能反查出「锚点缺失导致的静默模板化」——`_ANCHORS` 缺键不会有任何报错，关卡照常可玩，只是内容与模板关逐字节相同。**凡新增/改动锚点后，必须断言该关 waves ≠ 模板关**

### 下一入口
- **T-302a 已派**（团队任务 #5）：选关页奖励预览行（v2.1 规划 §5 Q-3 = B，**唯一未实施项**）——三态分叉（真卡→卡名 / 金币→预览 / 占位→不显示），几何走共享常量，需截图+像素判据+旧源对照
- 随后 **T-302b 定版通扫**（VERSION→v2.1.0 + 活默认值通扫 + code-map + 四门控）→ **T-303 发布四件套 + tag**（需用户授权推送）
- 提交节奏：用户已表达「内容刀合一」偏好；T-302a/T-302b 的提交与推送需再请示

## 01:0x–01:2x · T-302a 选关页奖励预览行 ✅（Q-3，v2.1 唯一未实施项落地）

- 实现：`drawSelect` 格内第三行（12px）按 `CARD_AWARD` 三态分叉——真卡→卡名（`CARDS` 查名）/ 金币→「100 金币」/ 占位→不绘制；判定只用 `CARD_AWARD`（与解锁状态、`LEVELS` 解耦，锁定格照样显示）
- 几何：新增共享常量 `SELECT_GEOM.CELL_REWARD_DY:70`，绘制处引用 `G.CELL_REWARD_DY` ⇒ **零硬编码**；位置在时长相 `by+46` 之下、格底 `by+96` 之上，避开 ✓/▶ 角标（`by+12`）⇒ 元素间距 24px 无重叠
- ★**emoji 取舍**：用「100 金币」文字 + 项目金色 `#ffd54a`，**不用 💰**——canvas 字体 `"Microsoft YaHei"` 下彩色 emoji 依赖字体回退、渲染不可靠；单色文字与「✓ 已通关」同强调色，必定可渲染
- 验证：新脚本 `v21-select-reward.js`（CDP 像素探针，奖励带 = 格内 `[bx+20, by+62, w-40, 20]` 计金色像素）——新源 5 判据全绿（真卡 78 / 金币 124 / 占位 0 / 锁定格 82 解耦 / 判别力），**旧源 v2.0.1 同判据 FAIL = 红**（合法红：setup 成功、选关页正常加载，只是无奖励行）
- 截图：`v21-select-reward-new.png`（页签 1：真卡 + 金币格）/ `-new-ph.png`（页签 3：占位格）——世界 1 无占位格、世界 3 无真卡/金币格 ⇒ 三态需两图覆盖
- 回归：`v20-select-draw` PASS · `v20-menu-nav` 21/0 · `v21-world2-open` PASS；code-map 3931 → **3944 行**；源码 **+13/−0 纯插入**
- ★**环境限制（重要）**：本会话模型 **deepseek-v4-flash 不支持读图** ⇒ 主理人与工程侧**均无法目视核对截图排版**；技术证据 = 像素探针 + 代码位置推算，**视觉美观终审只能交用户**

## 01:2x · T-302b 定版通扫已派（团队任务 #6）

- 内容：`VERSION`→v2.1.0 + 活默认值通扫（prelude / v17-acceptance / v18-acceptance / r9a-metric-spec 先例，**以实际 grep 为准**，历史文档保持原貌）+ code-map 重刷 + `run-gates` 四门控 + 更新 v2.1-plan.md 进度标记
- **合并提交一刀**（T-302a + T-302b，用户已表达「内容刀合一」偏好）：message `feat(v2.1): T-302 选关页奖励预览行 + v2.1.0 定版`；**仅提交不推送**（推送等用户单独授权）

## 01:3x · T-302b v2.1.0 定版通扫 ✅（提交 `b4dab8f`）

- `VERSION`（源码 L62）→ `v2.1.0`；启动实测打印 `[PvZ] PvZ Lite v2.1.0 已启动`，`sandbox.__VERSION = v2.1.0` ✓
- **活默认值通扫 4 文件 6 处**：`lib/prelude.js`（L24 注释 + L52 `EXPECT.VERSION`）/ `lib/r9a-metric-spec.md`（L95/L97）/ `v17-acceptance.js` L119 / `v18-acceptance.js`（L26/L53）；**主理人独立复查残留 = 0**（grep 退出码 1）✓
- **历史引用刻意保留**：`production/release/v2.0.1/**` 历史发布包、`v2.1-plan.md` L4/L137 沿革叙述、源码 3 处「v2.0.1：test=1 解锁定闸」代码沿革注释、v21-* 脚本的 `gitShow('v2.0.1')` git tag 参照
- **四门控全绿**：FULL 87/87 · BUS 56/56 · BENCH PASS（`run-gates` 一键入口命中本机 spawnSync EBUSY → 按 KNOWN-ISSUES 逐门直跑等效）
- code-map **3944 行**；提交 `b4dab8f`（13 文件，439 insertions / 219 deletions）；**未推送**（ahead 2）
- **偏差处理（两处，均认同）**：①4 个通扫文件入刀——「通扫零残留」是 DONE 判据，只改不提交会致 v17/v18 版本自证红 ②`docs/architecture/perf-profile.md` bench 幂等回填 7 行已 `git checkout` 还原——符合项目惯例「无漂移还原」
- 工作树仅剩 `.workbuddy/memory/*`（3 项，主理人自理）
- **⚠ 未决**：选关页截图视觉终审（主理人与工程侧均无法读图）待用户确认；**M3 剩余 = T-303 发布四件套 + tag v2.1.0（需授权）**

## 01:4x–02:0x · T-303 v2.1.0 发布 ✅ → 推送完成，全链路闭环 🎉

- 发布包 `production/release/v2.1.0/`（对齐 v2.0.1 结构）：**RELEASE-NOTES**（变更清单全录 11 刀 + 难度画像结论 + 提交链）/ **RELEASE-CHECKLIST**（§A 门控实测 + §B 真机验收 + §E 完整性三重校验 + §F Go/No-Go + §G 挂起）/ **KNOWN-ISSUES**（增量 4 项 + 继承项 + 方法论 5 条）/ **artifacts/** 冻结副本
- 冻结副本：`git show HEAD:…` 字节直通（**不经工作区拷贝**，避 CRLF 污染）；**主理人独立复算** = SHA-256 `38a335df8527a75ca7259c56f409f2f12800d604efbc8e67368c4fae27531a3b` / **213,599 B** / **3944 行** / **CR=0** / `cmp` **identical** ✓
- 提交 `a3ca5f6`（5 文件）；**轻量 tag** `v2.1.0` → a3ca5f6（`objecttype=commit`）
- ★**网络通路探明（重要）**：本机**默认走 `127.0.0.1` 代理不通**（加速器未起：`Failed to connect to github.com:443 over proxy 127.0.0.1`），但**绕代理直连可用** ⇒ 原开发机配方 **`git -c "http.https://github.com.proxy=" …` 复核有效**；并借此拿到远端 tag 列表（最新 `v2.0.1`）⇒ **补上工程侧断网时无法完成的远端确认**
- **推送（用户授权）**：`push origin main` → `b79be05..a3ca5f6`；`push origin v2.1.0` → `[new tag]`
- **四处一致双确认**：远端 main = 远端 tag = 本地 HEAD = 本地 tag = **`a3ca5f6`**；`ahead` 归零 ✓
- 工作树仅剩 `.workbuddy/memory/*`（3 项，待归档刀）

## M3 里程碑闭环小结（T-301 → T-303，6 个任务单元）

| 单元 | 产出 | 关键结论 |
|---|---|---|
| T-301 | 画像脚本 / 数据 / 报告 | ★挖出 1-3/1-4/1-5 模板占位塌陷 |
| T-301b | 补写三关 waves | 世界 1 曲线恢复严格单调 |
| T-301c | 2-4 首波放软 | 失守率 37.5% → 12.5% |
| T-302a | 选关页奖励预览行 | Q-3 落地（v2.1 唯一未实施项） |
| T-302b | v2.1.0 定版通扫 | 通扫零残留 + 四门控全绿 |
| T-303 | 发布四件套 + tag | **推送完成，全链路闭环** |

- **★M3 全程机制代码零改动**：源码改动仅 `_ANCHORS` +3 条目、`2-4` 首波 −1 只 fast、选关页奖励行 +13 行、`VERSION` 一行 —— 全部数据层/表层，**回滚友好**
- **★本期三条最有价值沉淀**（已进 KNOWN-ISSUES）：
  1. **锚点缺键静默模板化**——`_ANCHORS` 缺键不报错、关卡照常可玩，内容却与模板关**逐字节相同**；只有难度画像能发现 ⇒ **凡改锚点必断言该关 waves ≠ 模板关**
  2. **派单铁律**——「目标数值」与「操作方案」必须**二选一**，混写必致歧义（2-4 的「移到 W2」vs「−140」即此）
  3. **bot 校准优先**——读数异常先怀疑 bot 自己（初版贪婪策略 80 局全败，实为策略缺陷非关卡过难）
- **★v2.1.0 终态**：可玩 **6 → 20 关**；源码 3618 → **3944 行**；LF SHA-256 `38a335df…1a3b`；`production/v2.1-plan.md` 全 ✅

## 下一版本入口（v2.2 候选，未立项）
- 世界 3（墓地）/ 世界 4（房屋）开放——`_ANCHORS` 现有 `4-1` 锚点，3-x 尚无
- 墓碑机制实装（Q-7 沿革，`grave` 字段预留位）
- 金币留存钩子（v2.1 全通仅 2000/6400 ≈ 31%，SLOT_PRICE 缺口留 v2.2）
- N-13 多语言 / N-14 移动端——维持冻结至 v3.0.0 复核

## 06:0x 原开发机 weixufeng · v2.1.1 消缺补丁（地瓜爆炸无差别秒杀）✅

### 缺陷与修复
- **缺陷**：`explodeMine` 原用 `z.hp-=DMG(500); if(z.hp<=0)kill` 做耐久结算，铁桶(560)与高难度（hard×1.35 / expert×1.80）下 HP 超 500 的僵尸炸不死（铁桶普通难度剩 60 血、expert 剩 508）。
- **用户拍板**：武装后僵尸碰到就死、不存在耐久结算 ⇒ 命中即 `killZombie`（无差别秒杀，任何僵尸/任何难度一碰即死）。
- **修复**：`explodeMine` 去掉 DMG=500，`if(sameCell||nearCell) killZombie(z)`；范围判定逻辑未动（同排 ±54px）。

### 验证
- 新增 `tests/harness/cases/REG-MINE-03.js`：场景A 普通难度铁桶(560)同格被秒杀；场景B hard 下 normal(243)+bucket(756) 被秒杀、范围外 dx=80 存活。
- **判别力自证**：旧源（git show a3ca5f6）跑 REG-MINE-03 必红——铁桶剩 60 血（`hp:60`），证明旧缺陷真实存在。
- 门控：FULL **88/88**（SMOKE29+REG59，新增 MINE-03）· BUS 56/56 · BENCH PASS（DC 结构量零漂移，仅耗时因 i3-10110U 机器噪声变大→perf-profile 已 checkout 还原）。

### 定版刀（v2.1.1）
- VERSION L62 → v2.1.1；README 地瓜描述改"无差别秒杀"；活默认值通扫：prelude.js L24/L52、v17-acc L119、v18-acc L26/L53、r9a-metric-spec L95/L97。
- code-map 重刷 3945 行。

### 提交
- `5eeab99`（8 文件，138+/80-），未推送（等用户令）。
- ★坑：run-gates 一键入口在本机仍 spawnSync EBUSY（原开发机也中招），逐门直跑等效（FULL/BUS/BENCH 分开跑）。
