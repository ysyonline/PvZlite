# 2026-09-23

## 05:5x 家庭机收尾：R7/R9-b/prelude 修复 四笔提交 + SSH 推送闭环 ✅（用户令「归档，提交，推送，去公司接着干」）

- **接手核验**：工作树 12 项（4 M + 8 ??）与昨日终态一致，HEAD=`f704888` = 远端
- **四笔分刀提交（主理人逐一 show --stat 核验）**：
  · `70768cc` test(v1.8) R7 三件（+51475：脚本 641 行 / results 50709 行 / 报告 125 行）
  · `ce195e6` test(v1.8) R9-b 三件（+7569：脚本 446 行 / results 6952 行 / 标定文档 171 行）
  · `532c1b4` fix(v1.8) prelude 修复五件（+288−16：hitKind 并存 + survivor rate 硬门控 + selftest 断言 + 验证器）
  · `9aa0f68` memory 归档
- **推送**：SSH 直推一把成功 `f704888..9aa0f68 main -> main`；**远端双确认** `refs/heads/main = 9aa0f68` = 本地 HEAD 逐字节一致；fetch 后 `## main...origin/main` 零偏差，工作树干净
- **★ 公司机接续路径（下次会话第一件事）**：①git pull（SSH/代理视公司机环境）②核验 `git log --oneline -4` 见 9aa0f68/532c1b4/ce195e6/70768cc ③读 `production/v1.8-plan.md` 状态 + 本日志 ④**v1.8 最后一棒 = v18-acceptance 真机验收（七道门+三指标+溢杀分栏全量过一遍，端口 9355+）→ 四门控全绿 → 定版刀（L49 VERSION→v1.8.0 + code-map 重跑 + bench 回填）→ 发布包四件套 → 冻结副本（LF Buffer 直通）→ tag v1.8.0（轻量须显式推）→ 推送需用户单独授权**
- **悬置决策（不阻塞）**：GRID 投影式是否采纳进正式口径（建议采纳，全表最优 2.1pp/零拟合参数）
- **契约底线提醒**：同格锁契约 = REG-GRIDLOCK-01（回归 87/87）+ v17-acc 7/7，禁放宽；指纹口径方法论修正（剔 date/runtimeMs/gitHead/gitClean + htmlPath 小写归一 + git 计数归一）已入 09-22 日志

## 05:5x 补注：MEMORY.md 去重 + 推送终态

- 执行毛刺：shell 命令双执行致 MEMORY.md 追加段重复（`6ec815a`/`2cbdc9f` 内容相同）→ 去重提交 `85ce87d`（−7 行）
- **最终提交链（7 笔）**：`70768cc`(R7) → `ce195e6`(R9-b) → `532c1b4`(fix prelude) → `9aa0f68`/`4c75700`(memory) → `6ec815a`/`2cbdc9f`(MEMORY 双写) → `85ce87d`(去重)
- **终态**：远端 main=`85ce87d` 双确认（ls-remote=本地 HEAD），工作树干净零偏差。全部为测试侧+文档，无源码改动，无轻量标签
- ⚠️ 坑位备案：本会话出现两次 shell 命令双执行（push 输出「Everything up-to-date」但实际前一笔已推成功 / MEMORY.md 双追加）——长命令链后必以 `git log` + `ls-remote` 实测为准，勿信单次命令输出的表象
