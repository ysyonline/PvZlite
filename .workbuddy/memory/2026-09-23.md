# 2026-09-23

## 05:5x 家庭机收尾：R7/R9-b/prelude 修复 四笔提交 + SSH 推送闭环 ✅（用户令「归档，提交，推送，去公司接着干」）

- **接手核验**：工作树 12 项（4 M + 8 ??）与昨日终态一致，HEAD=`f704888` = 远端
- **四笔分刀提交（主理人逐一 show --stat 核验）**：
  · `70768cc` test(v1.8) R7 三件（+51475：脚本 641 行 / results 50709 行 / 报告 125 行）
  · `ce195e6` test(v1.8) R9-b 三件（+7569：脚本 446 行 / results 6952 行 / 标定文档 171 行）
  · `532c1b4` fix(v1.8) prelude 修复五件（+288−16：hitKind 并存 + survivor rate 硬门控 + selftest 断言 + 验证器）
  · `9aa0f68` memory 归档
- **推送**：SSH 直推一把成功 `f704888..9aa0f68 main -> main`；**远端双确认** `refs/heads/main = 9aa0f68` = 本地 HEAD 逐字节一致；fetch 后 `## main...origin/main` 零偏差，工作树干净
- **★ 公司机接续路径（下次会话第一件事）**：①git pull（SSH/代理视公司机环境）②核验 `git log --oneline -4` 见 9aa0f68/532c1b4/ce195e6/70768cc ③读 `production/v1.8-plan.md` 状态 + 本日志 ④**v1.8 最后一棒 = v18-acceptance 真机验收（七道门+三指标+溢杀分栏全量过一遍，端口 9355+）→ 四门控全绿 → 定版刀（L49 VERSION→v1.8.0 + code-map 重跑 + bench 回填）→ 发布包四件套 → 冻结副本（LF Buffer 直通）→ tag v1.8.0（轻量须显式推）→ 推送需用户单独授权**
- **悬置决策（不阻塞）**：GRID 投影式是否采纳进正式口径（建议采纳，全表最优 2.1pp/零拟合参数）
- **契约底线提醒**：同格锁契约 = REG-GRIDLOCK-01（回归 87/87）+ v17-acc 7/7，禁放宽；指纹口径方法论修正（剔 date/runtimeMs/gitHead/gitClean + htmlPath 小写归一 + git 计数归一）已入 09-22 日志

## 05:5x 补注：MEMORY.md 去重 + 推送终态

- 执行毛刺：shell 命令双执行致 MEMORY.md 追加段重复（`6ec815a`/`2cbdc9f` 内容相同）→ 去重提交 `85ce87d`（−7 行）
- **最终提交链（7 笔）**：`70768cc`(R7) → `ce195e6`(R9-b) → `532c1b4`(fix prelude) → `9aa0f68`/`4c75700`(memory) → `6ec815a`/`2cbdc9f`(MEMORY 双写) → `85ce87d`(去重)
- **终态**：远端 main=`85ce87d` 双确认（ls-remote=本地 HEAD），工作树干净零偏差。全部为测试侧+文档，无源码改动，无轻量标签
- ⚠️ 坑位备案：本会话出现两次 shell 命令双执行（push 输出「Everything up-to-date」但实际前一笔已推成功 / MEMORY.md 双追加）——长命令链后必以 `git log` + `ls-remote` 实测为准，勿信单次命令输出的表象

## 05:5x 补注：MEMORY.md 去重 + 推送终态

- 执行毛刺：shell 命令双执行致 MEMORY.md 追加段重复（`6ec815a`/`2cbdc9f` 内容相同）→ 去重提交 `85ce87d`（−7 行）
- **最终提交链（7 笔）**：`70768cc`(R7) → `ce195e6`(R9-b) → `532c1b4`(fix prelude) → `9aa0f68`/`4c75700`(memory) → `6ec815a`/`2cbdc9f`(MEMORY 双写) → `85ce87d`(去重)
- **终态**：远端 main=`85ce87d` 双确认（ls-remote=本地 HEAD），工作树干净零偏差。全部为测试侧+文档，无源码改动，无轻量标签
- ⚠️ 坑位备案：本会话出现两次 shell 命令双执行（push 输出「Everything up-to-date」但实际前一笔已推成功 / MEMORY.md 双追加）——长命令链后必以 `git log` + `ls-remote` 实测为准，勿信单次命令输出的表象

## 08:5x–09:1x 公司机 · v1.8 最后一棒：v18-acceptance 真机验收 PASS ✅

- **接续路径第①②步完成**：git pull（105704d→2d1fe56 fast-forward，工作树干净）→ `git log --oneline -4` 见 9aa0f68/532c1b4/ce195e6/70768cc ✅
- **v18-acceptance.js 新写**（plan M4）：三路结构——A 七道门（prelude.bootstrap，gate1-6 硬门全过）+ B 真机 TB1-TB7（Edge headless+CDP 端口 9355：版本自证/同格锁核心断言 C@425 邻格 0 伤/corn 同格溅 6/melon 无锁跨格 35.75/同格溅射确定性 12×12/黄油 27.45% N=4000/视觉证据两张 png）+ C R9-a 口径端到端（三指标受控场景 cov=0.3555/溢杀分栏 direct 非空真 raw=420/已知样本 eff=5/ovf=3/哨兵双域判别力完整 B 域 wastePerRefresh=0.426≈T*-cd）+ **旧源判别力自证**（git show c379872 提取系统 temp，旧源邻格溅 8 → 新源 0，melon 两源同 35.75，跑后清理）
- **施工坑三条（脚本侧，非源码问题）**：①受控溅射靶不能摆在直中窗 |Δx|<42 内（吃直中 20 而非溅 8）——溅射靶须补 A@415 承接直中或摆 |Δx|≥48；②僵尸数组顺序即结算顺序，harness 臂首位须放直中承接者；③melon 自证臂忘推 `__zombies` → hasZombieAhead 不成立不开火返回 null
- **总判定 PASS（两遍独立复跑一致）**：A 门全过 + B 17/17 + C 16/16（14.0s/15.2s）；results.json + v18-cabbage-flight/splash.png 落 tests/playtests/（未 commit，待定版刀分刀）
- **四门控全绿**：烟雾 29/29 · 回归 **87/87**（+REG-GRIDLOCK-01）· 总线 56/56 · bench PASS（E p50 0.45/p95 0.80ms；DC **290/876/1590/1890/2635** 与 v1.6/v1.7 基线逐场景一致 ⇒ 零渲染结构漂移）；perf-profile.md 跑后已 checkout 还原
- **下一步（待用户指令）**：定版刀（L49 VERSION→v1.8.0 + 注释版本标签 grep 通扫 + code-map 重跑 + 门控复跑）→ 发布包四件套 → 冻结副本（LF Buffer 直通）→ tag v1.8.0 → 推送需单独授权。悬置决策不阻塞：GRID 投影式是否采纳进正式口径

## 10:0x 公司机 · v1.8 定版刀 → 发布包四件套 ✅（用户令「定版刀」）

- **定版刀 `c10f4a1`**（6 文件 +13−13）：L49 `VERSION='v1.8.0-wip'`→`'v1.8.0'`（注释「施工中」→「定版」）+ **版本标签通扫 4 处活默认值**（prelude.js `EXPECT.VERSION` / v17-acceptance.js `VER_EXP` / r9a-metric-spec.md 期望表 / v18-r9b-calibration.js 被测源说明）；历史记录（decisions 立项刀条目 / plan / v18-r7-report）**保持原貌不动**
- **为何必须通扫**：验收 TB1 用 `indexOf(VER_EXP)` 判定，期望写 `v1.8.0-wip` 而源码已 `v1.8.0` ⇒ 定版后复跑必假红。改后已实测验证（TB1 detail `VERSION="v1.8.0"（期望含 v1.8.0）`）
- code-map 重跑：3291 行 / 19 分区 / 94 函数 / 62 常量，**与 HEAD 零差异**（结构未漂移，仅 L49 内容变）；bench 幂等回填 perf-profile §7
- **★ 回归口径修正（重要）**：项目口语「回归 87/87」= `run-all.js --all`（SMOKE 29 + REG 58）；**纯 REG 单跑 = `run-all.js` = 58/58**。此前日志只记 87 未记分解，易误判脚本失效
- 四门控全绿：烟雾 **29/29** · 全量 **87/87**（含 REG-GRIDLOCK-01）· 总线 **56/56** · bench 五场景 PASS（**DC 全口径中位 290/876/1590/1890/2635 与 v1.6/v1.7 基线逐场景一致** ⇒ 零渲染结构漂移；帧耗时列略高属负载波动，p95 仍 1%–6% 预算）
- **验收件分刀 `eb961db`**：v18-acceptance 四件（脚本 610 行 / results 430 行 / 2 png）。**定版口径复跑 PASS**（A 七道门 + B 17/17 + C 16/16 + 卫生过）
  · 首次复跑（定版刀未提交时）唯一红项 = 「卫生:跑后工作树无任务外脏文件」——因工作树有 7 项未提交改动 ⇒ **顺序定论：先提交定版刀、再跑验收**，卫生项才归位（v1.7 同序）
- **发布包四件套 `20d33e1`**：RELEASE-NOTES / RELEASE-CHECKLIST / KNOWN-ISSUES / artifacts/plants-vs-zombies.v1.8.html（+3599 行）
- **★ v1.8.0 冻结指纹（LF）**：`889c0cb066145620de6b96064dced3ff34c8897ebaeeebfc54a2458a6f08804f` / **163,493 B** / **3291 行**（hasCR=false）；源码权威提交 `c10f4a1`；三处指纹（源码 blob / 发布包 blob / 磁盘副本）**逐字节一致**
- **未做（待用户单独授权）**：`v1.8.0` 轻量标签 + main 推送（v1.7 治理惯例：打标与推送同属 G4 发布签字）
- **悬置**：GRID 投影式是否采纳进正式口径（技术侧建议采纳：χ²=8.05 / R²=0.9875 全表最优 + 零拟合参数 + T≤cd 严格退化为线性式；maxAbs 2.1pp 未过 2pp 判据）
- **本版方法论沉淀**：①定版刀 = 版本标签通扫（活默认值），不止改 L49 ②假绿两形态（恒等式空真 / 分类值覆盖键）须用非零正样本断言 + 独立验证器封死 ③口径换用必须换并集公式 ④小样本对照须同种子配对

## 10:2x 公司机 · v1.8.0 发布签字：tag + 推送 ✅（用户令「受权」）

- **打标**：轻量标签 `v1.8.0` @ 发布包提交 `20d33e1`——对齐 v1.7.0 惯例（tag 指**发布包提交**而非源码权威提交；`git cat-file -t` 确认为 `commit`，非附注标签）
- **推送通路**：公司机 origin=**HTTPS**（`https://github.com/ysyonline/PvZlite.git`）+ **GCM 凭据**，**直连通畅**（ls-remote 秒回；无 helper-selector 挂起、无失效代理注入干扰）
  · `git push origin main` → `2d1fe56..282a826`（4 笔：定版刀 c10f4a1 / 验收件 eb961db / 发布包 20d33e1 / memory 282a826）
  · `git push origin v1.8.0` → `* [new tag]`（**轻量标签须显式推**，`git push` 默认不带 tag）
- **远端核验（ls-remote 实测，勿信 status 表象）**：`refs/heads/main = 282a826` = 本地 HEAD ✅ · `refs/tags/v1.8.0 = 20d33e1` = 本地 tag ✅ · `v1.7.0` 仍在 ✅；`git fetch --tags` 后 `## main...origin/main` 无 ahead/behind
- **远端 blob 指纹复核**：自 `origin/main` 取冻结副本与源码 blob，均 = `889c0cb0…804f`，与本地 `c10f4a1` **逐字节一致** ⇒ 无 CRLF 污染
- **v1.8.0 正式发布完成**（定版 → 验收 → 发布包 → tag → 推送全链路闭环）。唯一悬置：GRID 投影式是否进正式口径（用户未拍板，不阻塞）
- **跨机记忆点**：公司机（user3667）走 HTTPS+GCM 直连即可；Administrator 家庭机走 SSH。**换机后先 `git remote -v` + `ls-remote` 判通路再推**

## 10:1x–10:4x 公司机 · 结算屏 UI 修复（用户反馈两条，附截图）✅ 已提交（并入 v1.9.0）

- **用户反馈（v1.8.0 发布后首个 bug 报告）**：①通关后「进入第5关」按钮把背后的字遮住了 ②「通关！」周围的光辉效果去掉（遮住下面的字）
- **核实根因（两条同在 `drawEnd`）**：
  · ① `fillRect(390,380,220,50)` 上沿 380 压住 `fillText('历史最高分 '+highScore, y=384)` 行（文字中心 384 落在按钮区 380..430 内）。**深层根因 = 同一几何在绘制（drawEnd）与命中（onClickEnd）两处各自硬编码**，两处不同步即错位
  · ② 所谓「光辉」= L3220-3228 的 **26 个彩带方块环**（椭圆 cx±250 / 220±105）；源内 grep `shadowBlur|shadowColor` **零命中** ⇒ 唯一候选。环下方弧段（i=6/7 两点 y≈324）与积分行文字带 324..340 重叠
- **修法（并入 v1.9.0）**：新增**共享常量** `END_BTN={x:390,w:220,h:50,nextY:410,backGap:62}`（drawEnd 绘制 / onClickEnd 命中共用，结构上封死漂移）；按钮组 380→**410** 下移（脱离 384 行，间距 26px）；删 26 个装饰环；「🎉已通关全部关卡」480→**500**（维持原有 33px 留白）
- **四门控全绿**：烟雾 29/29 · 全量 87/87 · 总线 56/56 · bench 五场景 PASS（**DC 290/876/1590/1890/2635 与 v1.8.0 基线逐场景一致** ⇒ 零渲染结构漂移，改动只落在结算屏）；code-map 重跑 = 3289 行 / 94 函数 / **常量 62→63**（新增 END_BTN）
- **★ 真机截图自证（新脚本 `tests/playtests/ui-end-verify.js`）**：Edge headless + CDP（端口 9361）构造**第四关通关态**（hasNext=true，正是用户截图场景），像素判据三条，**判别力自证 = 旧源必红 / 新源必绿**
  · 旧源 v1.8.0（`git show v1.8.0:plants-vs-zombies.html` 提取复跑）：按钮色@行384 = **2601** · 彩带环 **26/26** · 黄字 34 ⇒ 判据 ①② **FAIL**
  · 当前源码：按钮色@行384 = **23** · 彩带环 **0** · 黄字 **168**（4.9×）⇒ 判据 ①②③ **PASS**
  · 23 的解释：采样区 y376..392 与按钮新几何 410..460 **几何不相交**，且 200×16 采样中 0.7% 属背景暗像素落入描边色容差，非按钮
  · 产物：`ui-end-before.png` / `ui-end-after.png` / `ui-end-verify-results.json`（**追加式证据账本**，两源并存对比）
- **★ 构造结算态的手法（复用 REG-END-02 口径）**：`unlockedLevel=5; levelNo=4; level=LEVELS[4]; DIFF='hard'; startGame(); wave=level.totalWaves; spawnQueue/zombies/projectiles 清空; waveActive=false; lastWaveT=gt-999; screenShake.t=0` ⇒ 下一帧 checkWave 判通关。**harness 的 `setLevel/setUnlocked/setWave` 只是这几行的包装**（源码无同名函数，页面内直调必 ReferenceError `setUnlocked is not defined`）——踩过一次
- **✅ 已提交（用户 10:3x 授权「1.提交 2.下一个版本吧」）**：`ebf352f` fix(ui): 结算屏两处遮挡修复（v1.9.0）——源码 + code-map + ui-end-verify 四件。**版本走向 = 并入下一版 v1.9.0**（非 v1.8.1 补丁路线），源码 L49 `VERSION` 仍 `v1.8.0` 未动（定版时才改），源码/脚本/规格里的注释标记已由 v1.8.1 → v1.9.0
- **★ 提交前拦下一次行尾污染（重要坑，差点入库）**：`core.autocrlf=true`（系统级）下 git 检出的源码磁盘为 CRLF；项目惯用的 `git add -c core.autocrlf=false` **绕过 CRLF→LF 规范化** ⇒ 整文件 CRLF 入库
  · 表征：提交后 `git show --stat` 假报 **6580 行**（实际 40 行 = 19 增 21 删）；HEAD blob 变 `11509011…` / 167,376 B（应 LF 164,087 B）；磁盘源码 CR=3289
  · 发现途径：`git show --stat` 行数 ≠ `git diff --numstat` ⇒ 立刻查 `git show HEAD:file | wc -c` + CR 计数 + `git show :file`（index 版）三处比对
  · 修复：`git reset --mixed HEAD~1`（未推送，安全）→ 磁盘 CRLF→LF 规范化 → 重新 add/commit ⇒ blob `b8c01365…` / 164,087 B / CR=0 / **3289 行**，diff 回归 **19/21** ✅
  · **规则**：提交前确认「磁盘行尾 == 仓库既有行尾」，不一致时先规范化磁盘再入库。（`README.md` / `MEMORY.md` 磁盘与仓库历史皆 CRLF ⇒ 保持 CRLF 口径；日志 `.md` 与生成的 `.js`/`.json` 为 LF）

## 10:5x 公司机 · 推送 + 行尾治理落地 + v1.9 规划建档

- **推送完成**：`git push origin main` → `0dc9faf..26f257a`（2 笔：UI 修复 + memory）；随后 `.gitattributes` 提交 `f20057e` 亦已推（`26f257a..f20057e`）
- **★ `[gone]` 假象排查（新踩）**：`git branch -vv` 报 `[origin/main: gone]` ⇒ 查 `refs/remotes/` **完全为空**（从未 fetch 过），**不是远端缺分支**；`ls-remote` 实测远端 = `26f257a` 正常。`git fetch origin` 后 `origin/main` 跟踪引用恢复、`[origin/main]` 归位。**记：`[gone]` 先看 `refs/remotes/` 是否为空，再看 `ls-remote`**
- **行尾治理（`.gitattributes`）落地 `f20057e`**：`* text=auto eol=lf` + 二进制显式 `binary` 声明
  · **先诊断后动手（关键）**：109 个磁盘 CRLF 的已跟踪文本文件中，**108 个仓库 blob 本就是 LF**（含 v1.5/v1.3/v1.2.1 冻结副本）⇒ renormalize **零 diff**；**唯一 blob 变化 = `.workbuddy/memory/MEMORY.md`**（48 行 CRLF→LF，历史遗留口径）
  · ⇒ 冻结副本指纹**未受影响**（v1.5 冻结副本 blob 153,230 B 未变）；**`renormalize` 前先跑「仓库 blob 行尾诊断」是这次没伤到发布指纹的关键动作**
  · 效果：`.gitattributes` 优先级高于 `core.autocrlf` ⇒ 检出/入库恒为 LF，**与提交命令怎么写无关**（本次刻意不加 `-c core.autocrlf=false` 验证属性确已生效）
  · 诊断手法：`git ls-files -s` 取 blob hash → `git cat-file --batch` 批量读（**注意 `execSync` 有 `input` 时不能同时给 `encoding:'buffer'`**，会 `ERR_UNKNOWN_ENCODING`）
  · 沉淀 skill：`~/.workbuddy/skills/git-lineending-blob-pollution/`
- **v1.9 规划建档**：`production/v1.9-plan.md`（**草案**，范围待用户拍板）
  · §1.1 在途两项（`ebf352f` / `f20057e`）/ §2 候选池（A 决策：GRID 投影式去留；B 测量：R10/R11；C 体验：R5；D 登记备查：距离衰减·双区溅射）/ §3 版本线位置 / §4 流程要求 / §5 三项裁决 / §6 不做清单
  · ★ 位置提醒：**v1.9.0 是 v2.0.0 前最后一个次版本**；N-13 多语言 / N-14 移动端冻结至 x.0.0 ⇒ v1.9 维持冻结，但 **v2.0.0 必须正式复核**
- **待用户裁决（Q-1/2/3）**：本版范围 / GRID 投影式去留（建议采纳）/ 纯收口 vs 玩法增量

## 11:3x–13:1x 公司机 · Task#3 R10 + Task#4 R11 全绿收口 ✅（Q-1=B 执行；只新增文件未 commit）

- **Task#3 R10（自然对局同格堆叠频率采样）PASS**：`tests/playtests/v19-r10-natural-stacking.js`（3 臂×8 种子真实对局，6.6s）
  · 修复三件后全绿：runOnce 返回体补 `firedMinusHits`（NaN 根治）；自证段整体替换为**统计法**（60s+teleport+[800,816]+makeHitClassifier+pend 队列归因）；分类读 `e.w.hitKind`（现行双键）
  · 核心数：stackOp cabbage=0.0171/corn=0.0433/melon=0.0121；P(≥1|n≥2) cabbage=1(52)/corn=1(177)/melon=0.5385(13)；vCross 同格锁臂全 0；自然溢杀率 ≤0.68%；24 局全胜 wavesMed=7；deaths=spawns=264/臂（killZombie 桥）
  · **判别力自证完美成立**：旧源 v1.7.0 same=5/cross=2（带状）vs 新源 same=17/cross=0（同格锁），两源弹着点数均=30（摆位对齐佐证）
  · 报告：`production/v19-r10-report.md`（含 **R7 脚本 `e.w.kind` 潜在回归披露**——今日复跑必红，待 Task#5 拍板是否修）
- **Task#4 R11（溅射价值有效 DPS 口径）PASS**：`tests/playtests/v19-r11-effective-dps.js`（4 臂×8 种子×2 配对局=64 局，17.2s，首跑即全绿）
  · 双口径：**A 反事实配对**（control 溅射禁用=hp 探针回滚 splash 写入；比结局/minX/通关时间）+ **B 直中先行再分配**（effAlloc=min(S,max(0,H−D))，utilization 为上界）
  · 核心数：**utilization cabbage=81.8%/corn=71.5%/melon=64.3%/压力臂=61.5%** vs **clampRate≈0~0.68%**——溢杀 clamp 对战略冗余几乎失明（R7 画像预警定量证实）；**32 对零结局翻转**（肥阵下溅射不救局，价值=提速+补刀，corn 中位快 1s 且 5/8 种子方向一致）
  · 前提⑤同源性实测：前缀一致帧中位 8367~19742 帧，分叉在局末段；control canceled=384~1140 全生效
  · 回归校验：前三肥臂与 R10 同种子同 bot ⇒ win 8/8+wavesMed=7 复现 R10 基线
  · 报告：`production/v19-r11-effective-dps-report.md`（含有效 DPS 叙事口径建议四条）

## 14:0x 公司机 · Task#5 分刀提交推送完成 ✅ + 会话归档

- **五刀全推（用户 13:3x「提交推送」授权）**：立项刀 `f986124` → GRID 采纳 `9cc06f7` → R10 三件 `df4b0e1` → R11 三件 `72ecef6` → plan 定稿+memory `b5a9438`；**远端 main=`b5a9438` ls-remote 双确认**
- 提交前保障：四门控复跑全绿（SMOKE 29/29 · BUS 56/56 · FULL 87/87 · bench PASS）；4 个磁盘 CRLF 文件规范化为 LF（design 校准文档/prelude/r9a-spec/v17-acceptance）后才提交，每刀 `git show --stat` 核对无假 diff；bench 回填 `perf-profile.md`（6+/6-）随定稿刀入库
- v1.9-plan 定稿：标题转「收口+测量版（定稿）」，§2/§5 填裁决结果（Q-1=B/Q-2=A/Q-3=A，R5 本版不做），§1.2 版本定位填实，新增 §8 R10/R11 执行结果摘要
- 长期 MEMORY.md 同步：版本线 + 当前状态（09-23 14:0x）+ L49/GRID 过时表述修正
- **会话收尾状态**：工作树净（仅 3 个不入库运行日志 r10-run3/run5、r11-run1）；**待办**：①R7 脚本 `e.w.kind` 潜在回归待修 ②v1.9 定版刀未做（L49 切 v1.9.0 + 发布四件套 + tag v1.9.0）③R5 人工复核本版不做
- **Task#5（收尾完成 ✅）**：v1.9-plan 定稿（Q-1=B/Q-2=A/Q-3=A 填入 §5，§8 记 R10/R11 结果摘要）+ 四门控复跑全绿（SMOKE 29/29 · BUS 56/56 · FULL 87/87 · bench PASS）+ 分刀提交五刀全推：立项刀 `f986124` / GRID 采纳 `9cc06f7` / R10 三件 `df4b0e1` / R11 三件 `72ecef6` / 定稿+memory 归档（刀5）。提交前 4 个 CRLF 磁盘文件规范化为 LF（design 校准文档/prelude/r9a-spec/v17-acceptance），diff 统计无假 diff；bench 回填 `docs/architecture/perf-profile.md`（6+/6- 幂等覆盖）随定稿刀入库

## 14:0x–15:0x 公司机 · v1.9.0 定版刀 + 发布包 + tag/推送 全链路闭环 ✅（用户一次性授权）

- **授权范围（AskUserQuestion 拍板）**：启动定版刀（推荐）+ 一次性授权全流程（定版→验收→发布→归档→tag+push，中途不再逐刀请示）+ 3 个运行日志删除
- **定版刀 `7d5daf6`**：L52 `VERSION='v1.9.0-wip'→'v1.9.0'` + 版本标签通扫 **5 文件 9 处**活默认值（prelude.js 缺省+注释残留「1.8.0-wip」/ v17-acc / v18-acc 缺省+注释 / r9a-spec 表格 2 处）；v16-acc 缺省 '1.6' 设计保留不动；历史记录保持原貌；提交前 staged blob CR=0 + diff 9+/9- 无行尾污染（`.gitattributes` 兜底生效实证）
- **四门控复跑全绿（先提交定版刀再跑）**：SMOKE 29/29（141ms）· FULL 87/87（412ms）· BUS 56/56 · bench 五场景 PASS（DC 全口径中位 290/876/1590/1890/2635 与 v1.6–v1.8 逐场景一致零漂移）→ bench 回填 `8963ff1`（perf-profile §7 幂等刷新）
- **真机验收定版复跑三件全 PASS**：v18-acceptance 总判定 PASS（A 七道门+B 17/17+C 16/16，34.2s；版本自证对新缺省 v1.9.0 通过=活默认值改后实测可运行）+ v17-acc 7/7 + ui-end-verify 三判据 true；**★新坑：验收日志写 tests/ 内被卫生门判 FAIL（任务外脏文件）→ 日志须写 $TEMP 仓库外**；验收产物 `git checkout --` 还原后工作树净
- **code-map 重跑零 diff**（3289 行/19 分区/94 函数/63 常量）
- **发布包 `c893120`**：production/release/v1.9/ 四件套（RELEASE-NOTES 124 行 / RELEASE-CHECKLIST 89 行含 A1–A10 实测 / KNOWN-ISSUES 60 行含 R7 脚本缺口新登记+方法论沉淀 4 条）+ 冻结副本 `artifacts/plants-vs-zombies.v1.9.html`（Node Buffer 直通导出，Buffer.compare=0/hasCR=false）
- **定版指纹**：LF SHA-256 `213929e6e82cd4b84e7de4173935c6e214e30a465778c280b5cca23fb5b4d868` / **164,063 B / 3,289 行**；release blob=`008899f` 与源码 blob 逐字节同一对象
- **推送与打标（公司机 HTTPS 直连秒回）**：`e23aab3..e433087`（4 笔）+ **轻量标签 v1.9.0 @ `c893120` 显式推送**；ls-remote 双确认 main=`e433087`=本地 HEAD · tag=`c893120`；fetch 后零偏差；**远端冻结副本与源码 blob sha 均=213929e6…d868 与本地一致**；签字归档刀=memory 提交（末笔）
- **终态**：v1.9.0 全链路闭环 ✅；**下一版=v2.0.0 立项（N-13/N-14 冻结复核必过项）**；遗留：①R7 脚本 e.w.kind 回归待拍板修 ②R5 人工复核维持建议

## 14:4x 公司机 · R7 脚本回归修复闭环 ✅（用户拍板「1」= 待办①）

- **修复 `2f3add7`（已推，远端 main=`2f3add7` 双确认）**：`v18-splash-distribution.js` 两处旧读法——①L247 逐弹着点归因分桶 ②L69 `localOverflow` 账本分桶（R10 报告只登记了①，施工中实测挖出②；②致账本路 splash/direct 恒 0 ⇒ 浪费率全 N/A + 双路记账断言红）——均改双键口径：`kind==='hp'` 过滤 + `hitKind` 分桶（镜像 prelude finalize）
- **复跑验证**：39 臂 × 8 种子 **R7 PASS**（32 断言全过 / unknown 全臂=0 / exit 0，10.7s）；关键数值与 v1.8 定版报告**逐数对齐**（P(≥1|stack) cabbage=1/corn=1/melon=0.9538/mix=0.9876；浪费率 0/0.4%/0/0%；旧源自证 victims=58/vCross=22）⇒ 同种子逐字节可复现性跨版本保持，历史结论零改写
- **文档同步**：R10 报告 §5 标题转「已修复」+ 补修复记录；v1.9 KNOWN-ISSUES 登记项 ✅ 闭环；RELEASE-CHECKLIST §G1/G2 转完成态
- **★方法论补强**：读旧账本字段的脚本，分桶键要全文件扫（不只 grep `w.kind`）——`localOverflow` 这类**本地辅助函数**的隐式读法（`ov[e.kind]`）与主流程显式读法（`e.w.kind`）要分开排查；单点修复后复跑仍红即提示还有第二处
- **终态**：v1.9.0 发布 + 遗留待办①闭环；下一版=v2.0.0 立项（N-13/N-14 复核必过）；R5 维持建议

## 15:1x–15:3x 公司机 · v2.0.0 立项：规划初稿完成（文策渊起草，待用户拍板 Q-1～Q-13）

- **触发**：用户口述 v2.0.0 大升级需求（主菜单+选关两级 / 4世界×10关=40关 / 每世界前5昼后5夜 / 每关奖1植物+世界全通金币 / 本期只跑通1-1其余占位 / 解锁功能暂不做 / 新怪物植物后期）
- **执行**：主理人摸底（code-map：LEVELS L107-193 / POINT_CONFIG / CARD_AWARD / state三态）→ spawn design-strategist 起草；期间一次 502 网络故障（open.bigmodel.cn 解析失败）致成员中断 10 分钟，SendMessage 续跑后完整回传
- **产出**：`production/v2.0-plan.md` 初稿已落盘（§1范围 §2流程 §3数据模型'w-l'键+存档v2迁移 §4奖励改造CARD_AWARD+per10→worldClear §5主题昼夜五层 §6里程碑M1-M5 §7风险R1-R7 §8测试v20-*.js §9待裁决）
- **核心方案**：'w-l'字符串主键+WORLD_THEMES主题表+波次模板启动期确定性展开（省~675行，预算+745→~4030行红线4500）；存档pvz_progress_v2旧键保留幂等迁移；奖励改两表现有表（CARD_AWARD 40项28占位 / CLEAR_REWARD per10→worldClear 300金币）不新增并行表
- **旧5关映射**：L1→1-1 / L2→1-2基底 / L3→1-6基底 / L4→2-1 / L5→4-1（波次为锚点原样迁）
- **待办**：用户拍板 Q-1～Q-13（首批 Q-3占位反馈/Q-6昼夜玩法/Q-7墓房机制/Q-10占位显示）→ 拍板记录落 production/v2.0-decisions.md → 定稿 plan → M1 开工需用户授权
- **执行毛刺**：Bash shim 环境异常（dirname/tail/cat not found），shell 部分命令退化，文件操作尽量走 Read/Edit 专用工具

## 15:4x–16:1x · v2.0.0 第一批拍板回写完成（Q-3/Q-6/Q-7/Q-8）

- **Q-3=A** 占位关点击 toast「该关卡即将开放」+轻音效；**Q-6=A+补充** 昼夜纯视觉 + **泳池特殊三段**（2-1~2-2昼/2-3~2-5夜/2-6~2-10浓雾夜：白雾遮罩仅见最左列前半列+最右列半列，原版雾关视野）⇒ `time` 值域 'day'|'night'|'fog'；**Q-7=B（用户主动升级）** 墓地加墓碑机制（本期字段预留+视觉，实装随世界3开放）；**Q-8=A** 分世界页签
- **产出**：`production/v2.0-decisions.md` 新建（决策记录惯例起点）；`v2.0-plan.md` §1.1/§3.1/§9 已回写拍板结论 + 新增 Q-6a/Q-6b 补充确认项
- **注意**：Expert 会话中途被用户取消一次（16:1x 系统通知），后按默认角色继续；第二批拍板待做（Q-1/Q-2/Q-4/Q-5/Q-9/Q-10/Q-11/Q-12/Q-13 + Q-6a/Q-6b）

## 16:1x–16:2x · v2.0.0 全部拍板完成，规划定稿 ✅

- **第二批**：Q-1=A 纯Canvas静态主菜单 / Q-2=A 旧档映射世界1 / Q-4=C 地形卡对位+强度递增（28占位格不发不补偿）/ Q-5=A 300金币每世界
- **第三批**：Q-9=A 旧屋顶关迁世界4（4-1锚点）/ Q-10=A 40格全可见（占位灰调+敬请期待）/ Q-11=A dusk退役二分 / **Q-6b=本期实装浓雾**（M4 白雾遮罩仅见最左列前半列+最右列半列，?level=2-6 视觉验收）
- **自动锁定**：Q-12=仅1-1可玩（原话）/ Q-6a=草墓房前5昼后5夜（原话「后面同理」）/ Q-13=per10→worldClear（由Q-5A推导，防重复发钱）
- **终态**：`v2.0-plan.md` 升**定稿**（§9 决议一览表，残留选项清单已清）；`v2.0-decisions.md` 全量记录（三批+自动锁定三分类）
- **下一步**：M1（数据层与存档迁移）开工需用户授权；施工前重跑 gen-code-map 刷新行号；策划/成员 spawn 的临时团队随会话收尾自动清理

## 16:3x · v2.0.0 任务拆分细化（用户要求：颗粒度细致，不做半天大任务）

- **§6 重写**：5 里程碑 → **18 任务单元**（T-101~106 / T-201~206 / T-301~303 / T-401~405 / T-501~504），每单元 0.5~1.5h、独立 DONE 判据、一刀一提交
- **首任务 T-101 = 旧验收脚本依赖盘点**（纯只读）：grep tests/ 全部 LEVELS/levelNo 引用落清单，结论指导后续改动面（对应风险 R-3）
- 顺序依赖写明：T-102→T-104 强依赖；T-201 先于 T-203/204；T-401 先于 T-402/403；预估总量 ≈17.5h
- **下一步**：待用户授权开工 T-101

## 16:5x–17:1x 公司机 · v2.0.0 T-101 旧验收脚本依赖盘点完成 ✅（严守真执行，主理人抽验）

- **产出**：`tests/playtests/v20-impact-inventory.md`（T-101 唯一写操作，未动任何现有文件）；三分类：断言过时 23 文件 / 逻辑回归 14 文件 / 无影响 ~50 门控用例+历史证据；字符串键样本全库零命中（符合预期）
- **受影响门控**：SMOKE 7/29（005/011/024/025/026/027/028）· REG 15/58 · 独立 playtest ~14（prelude 底座最先改）；总量 ≈40 文件、180±30 处编辑（其中 ~60 处「闭包直改 levelNo / 数字下标读表 / 算术寻址」真红级逻辑改点）
- **主理人抽验三处全对**：harness/index.js L200-205（`setUnlocked n|0` 字符串恒 0→解锁恒回 L1，真红）/ REG-END-02.js L15+L26（`setLevel(5)`+`LEVELS[6]===undefined` 末位关前提）/ ui-end-verify.js L52+L110（`LEVELS[levelNo+1]` 算术寻址 NaN，真红）
- **Top 风险**：①REG-END-02 前提崩塌（L5→'4-1' 非末位，须改锚末位键；'4-10' 波次由 T-102 materializeLevels 确定性展开即供）②harness/index.js 单点收口（建议 T-104 兼容层：字符串键签名+数字 1..5 shim+probe 双暴露 levelKey/levelNo）③存档复合键 `hard:3` 与 pvz_unlocked 数值语义 v2 格式未定
- **新裁决项已拍板（第四批 Q-14/Q-15/Q-16，用户 17:1x 选推荐项，已回写 decisions.md 第四批 + plan.md §9 决议表）**：Q-14 存档复合键=`hard:1-6` 同构拼接（旧档 hard:N→hard:锚点键，单向幂等旧键保留）/ Q-15 END-02 改锚 `'4-10'`（materializeLevels 展开供表，无时序阻塞）/ Q-16 unlocked=最高解锁键（键序比较；T-104 数字 shim+probe 双暴露）
- **开工授权：用户选「暂不开工」**（先消化清单）；下次开工入口 = T-104 harness 兼容层 → T-102（已无时序阻塞）

## 17:2x 公司机 · 用户批评「过于跑脚本验证」→ 批量验证节流规则落地 ✅

- **用户批评（原话）**：每 10 关才跑批量脚本验证，团队过于频繁跑脚本，浪费资源时间
- **落实（production/v2.0-plan.md §6 门控基线重写）**：任务单元收尾**一律不跑批量门控**（一刀一提交照旧）；批量验证只在里程碑收尾单元（T-106/T-206/T-303/T-405/T-503）执行；T-106/T-206/T-303/T-405/T-503 五行 DONE 判据同步改「run-gates.js 一键批量」；§6.末「每个任务单元」条目同步改
- **新建一键批量入口 `tests/harness/run-gates.js`**：FULL（run-all --all 87 项）+ BUS（verify-bus 56）+ BENCH（bench 五场景，回填 perf-profile）；`--quick` 跳 bench；汇总判级 exit 0/1；实测全跑 2.0s、quick 0.9s
- **★ 本机 Node 路径勘误**：22.22.2-**3**（`C:/Users/user3667/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`），记忆里旧写法 `22.22.2` 缺 `-3` 后缀会 No such file
- **★ bench 回填即脏树**：run-gates 全跑会回填 perf-profile.md §7（时间戳+毫秒抖动 6 行）→ 里程碑门控跑完若无实质漂移直接 `git checkout --` 还原，与 v15/v16/v17 results 同类处理；DC 全口径中位 290/876/1590/1890/2635 逐场景零漂移
- 终态：工作树 = 4 个 v2.0 文档/清单/工具新文件 + 2 个 memory 修改，未提交（v2.0 开工时一并处理）；节点省下的验证开销 ≈ 每单元 3-4 次门控 × 17 单元

## 17:3x 公司机 · 提交推送收尾 ✅（用户回家换机续干）

- **两刀提交并推送**：`bd92542`（v2.0.0 立项资产 4 文件 667 行：plan/decisions/run-gates.js/T-101 清单）+ `abfc66b`（memory 归档 56 行）；推送 `60a1e76..abfc66b` 秒回；ls-remote 双确认 remote main=`abfc66b`=本地 HEAD
- **提交前核验**：4 个新文件 CR=0（磁盘 LF=仓库行尾，`.gitattributes` 兜底）；`git show --stat` 行数与 numstat 一致无假 diff；工作树净
- **换机交接要点**：家里机（Administrator）origin=SSH 直推可用；开工入口 = T-104 harness 兼容层 → T-102（Q-14/Q-15/Q-16 已拍板无阻塞）；批量门控只用 run-gates.js 且仅在里程碑收尾
- **bonus**：SMOKE-025 顺带升级全 40 键 materializeLevels 契约；tests/README.md 是唯一须随施工更新的活性文档（regression-plan/playtest-plan 规格源头只增不改）；e5n4-evidence-backup 整体只读归档
- **施工顺序建议（清单 §④-8）**：T-104 harness 兼容层 → T-102 LEVELS 表+锚点迁移（门控分批全绿）→ playtest 底座+脚本 → 存档兼容用例 → v2 验收手册新写

## 22:4x 原开发机 weixufeng · 换机 pull 同步 ✅

- `git pull origin main` 直连成功（无代理/无需配方），fast-forward `656b6d8..e3c5fdc`，90 文件（v1.6~v1.9 全套发布件 + v2.0 T-102/103/104b/105 源码与测试 + .gitattributes）；本地=远端 HEAD，工作树净（仅 MEMORY.md 本地重写）
- 远端已把 MEMORY.md 精简至 11.3k，仍超注入上限 ⇒ 本地二次压缩至 ~3.4k（铁律/版本线+指纹/门控/三机环境/关键坑浓缩），细节全部指向日志与 production/design 文档；**注意此改动未提交，下次提交或 pull 冲突时按本地版为准再处理**

## 23:0x 原开发机 weixufeng · T-106 M1 收尾门控 ✅（v2.0 M1 全里程碑闭环）

- **接手判定**：T-101~T-105 已在家里机落地，但**旧门控断言迁移（盘点 B 组）与 bench 寻址（D1）未做**——run-gates 首跑 FULL 71/87（16 红）+ BENCH 崩（`__LEVELS[levelNo].armTime` 数字寻址），正是 T-106 该收的尾
- **修复面（19 文件，+219/−148，一刀 `db3c985`）**：16 个门控用例数字键迁移（`__LEVELS[n]`→锚点键、dusk 退役改 `time`、`probe().levelNo` 断言改 `levelKey`、REG-END-02 末位改锚 '4-10' 按 Q-15）；REG-CLEAR-01 重写 worldClear 契约；REG-SLOT-03 重写新发卡序列（6 真卡 + PLACEHOLDER 跳过 + 池 10 张）；REG-POINT-04 改 '1-10' 世界通关结算链；REG-META-01 场景 3 修正（cleared 集合推导含 melon/corn，旧 unlock 缺口已修）；REG-TESTMODE-01 九键清单 `pvz_unlocked`→`pvz_progress_v2` + 复合键 `hard:1-6`
- **harness 两处真 bug（家里机收尾漏网）**：①api `computeClearReward` 透传**丢 worldKey 参数**（T-105 新签名未跟上）②无 saveCleared 注入器 → 补 `setSaveCleared`；bench.js armTime 改 levelKey 寻址
- **v20-harness-compat 复核基线改版**：T-102/T-104b 落地后该脚本静默全红——[A] 段 v1.9 语义断言过期（dusk/levelNo 数字）已按 v2 语义重写（21/21）；[B] 段原取 `git show HEAD:harness` 在 T-104a 提交后结构性失效，改钉 v1.9.0 冻结基线（tag 缺失回退全哈希 `c893120`）且与 v1.9 冻结副本配对（旧 harness 裸读 levelNo 对 v2 源会 ReferenceError，禁跨代配对）
- **终态**：run-gates 四门控全绿（FULL 87/87 · BUS 56/56 · BENCH 五场景）+ v20 五脚本全绿；bench 回填脏 perf-profile 已 checkout 还原；入库 blob CR=0（Write 工具 CRLF 由 .gitattributes 规范化，`git show --stat` 行数与 numstat 一致）
- **M1 全闭环（T-101~T-106）**；下一里程碑 M2（T-201 共享几何常量 MENU_BTN/SELECT_GEOM 起步）。**本地领先远端 2 刀未推送**（db3c985 + memory 归档刀），推送待用户授权
