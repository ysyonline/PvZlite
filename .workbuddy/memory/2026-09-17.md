# 2026-09-17

## v1.1 S2 音频补齐完成（B1–B10 + sirenLoop）

### 交付范围（全部落地，无遗留）
| 项 | 实现 | 备注 |
|---|---|---|
| B1 种植落地 | **并入 `SFX.plant`**：上滑主音 + `noise(0.06,{vol:0.09,lp:600,hp:100,delay:0.05})` | 规格名叫 `SFX.plant.settle`，JS 里挂不到函数属性上，故并入 |
| B2 卡片就绪 | 新键 `SFX.cardReady(type)`；`update()` 卡片冷却循环内**转点判定**（`>0` 归零当帧），节流键 `cardReady_<type>` | 原先卡片由灰转彩完全无声 |
| B3 阳光分层 | 新键 `SFX.sunDrop()`（1567→2093，gate 0.3s）；`SFX.sun` 降为 0.10+0.07 + `gate('sunCollect',0.08)`；**掉落两条路径都接线**（checkWave 自然 + updatePlant 向日葵） | 只接一条会被变异测试抓出（本次真被抓出一次） |
| B4 死亡分层 | `SFX.death(type)` **参数分派**（`killZombie` 传 `z.type`）；cone 叠 300→180 triangle，bucket 叠 noise(3500/800)+1200→200 square | 规格写的两个独立键无法与现有函数共存 |
| B5 西瓜抛掷 | 新键 `SFX.melonThrow()`（180→320 triangle，vol 0.14），**与 shoot 同帧叠层**，**独立节流键** | 共用 `gate('shoot')` 会互相吞（先调的赢） |
| B6 失败收束 | 新键 `SFX.loseClimax()`（noise 0.6s + 80→40，delay 0.35），带 2s 节流防同帧多只进屋叠加 | |
| B8 菜单点击 | 新键 `SFX.uiClick()`；菜单的关卡选择/难度选择两处从误用的 `SFX.sun` 改正 | 静音按钮恢复时的 `SFX.sun` 保留（顺带确认能听到声音） |
| B9/B10 铲子 | `SFX.shovelArm()`（点击槽 + `X` 键两条入口，仅"进入"时响）、`SFX.shovelEmpty()`（`dug` 标志区分铲空/挖到） | |
| §C sirenLoop | `SFX.siren()` **拆为单轮** + 新建顶层 `SirenLoop` 对象，**帧驱动**：`start()`/`stop()`/`tick(dt)`，INTERVAL 0.62s、MAXROUND 8 | 见下方设计决策 |

### §C 关键设计决策：不用 setInterval，改帧驱动
- 暂停时 `update` 不执行 → 警报**天然同步暂停**（规格要求的"暂停要停 loop"零额外代码）
- 横幅结束当帧 `SirenLoop.stop()` → 与 `SFX.bigWaveImpact` 收束衔接，无定时器拖尾
- 2s 窗口 ≈ 4 轮（原一次性三连 1.32s 后留 0.68s 空白）
- 200ms 淡出改为「每轮自带指数衰减 + 收束音衔接」，不单独做总线增益斜坡（避免波及其它 event 音效）
- 附带收益：vm 沙箱的 `window.setInterval` 是空 stub，用定时器实现的逻辑**无头测不了**；帧驱动可测

### 测试护栏
- **新增 `SMOKE-022`**（B1/B2/B3）+ **`SMOKE-023`**（B4/B5/B6/B8/B9/B10 + §C 生命周期）
- **适配 `SMOKE-016`**：B1 叠层使单次种植往 `audioQueue` 入 **2 项**（tone + noise），原断言 `===1` 挂 → 改「单调增长 + 基线差值」式；并 `setSunFallT(999)` 隔离自然掉落环境音
- **`verify-bus.js`**：`AUDIO_ROUTES` 由 13 键扩到 **21 键**（新增 sunDrop/cardReady/shovelArm/shovelEmpty/uiClick/melonThrow/bigWaveImpact/loseClimax）；顺手补 fake gain 的 ramp 方法（消除 `音效异常(sun)` 噪声日志）
- **`index.js` 探针**补 `sirenLoopOn`

### 门控（三件套全绿）
- 烟雾 **23/23** · 回归 **30/30**（`--all` **53/53**）· 总线 **55/55**
- **变异测试 12/12 全部精准抓到**（去 B1 噪声 / 去 B2 转点 / 去 B3 两条路径分别 / B4 退回无参 / 去 B5 / 去 B6 / B8 退回误用 sun / 去 B9 / 去 B10 / §C 退回一次性 / §C 结束不停）
  - **第一轮 M-C（去 B3 掉落音）未抓到** → 原因是测试只覆盖了自然掉落、没覆盖向日葵路径 → 补断言后重跑 12/12 全抓。**"变异没抓到"暴露的是测试盲区，不是变异无效**
- **真 loop 帧集成验证**（临时脚本，非用例）：递增时钟 + console.error 监视网跑 660 真实帧（含完整预警窗口）→ 0 帧异常 / 0 音效异常 / 警报 loop 正确收停 / 大波已刷出
- v1.0.0 冻结基线副本哈希仍为 `713a9441…b1030deb` ✅ 未被污染

### 文档同步
- `design/audio-guide.md`：新增 **§I 实现状态**（落地对照表 + 4 处有意命名偏差 + 未实现建议 1 处 + sirenLoop 实现选择说明），摘要处加状态指向
- `tests/README.md`：21→23 条、51→53、总线 47→55、新增 SMOKE-022/023 条目、版本记录加行
- `tests/regression-plan.md`：同样口径同步 + §7「音效正确性」由「❌ 无头不可测」改为「部分（触发契约可测，听感不可测）」
- `README.md`：待办 #3（存档）改 ✅、#8（音效补齐）改 ✅
- `production/v1.1-plan.md`：S2 状态改 ✅ + 执行结果表
- `docs/code-map.md` 重生成：**1959 行 / 68 函数 / 44 常量**（wc = awk = code-map 三方对齐）

### ★ 本次踩到的工具坑（重要，已写入长期记忆与技能）
**对同一个文件并行发两个 Edit 调用，会互相覆盖**——两次都报 "Successfully edited"，实际只落一半。本次 6 组并行编辑里有 6 处静默丢失（`SFX.death(z.type)`、cardCD 转点、两处 uiClick、onKey 的 shovelArm、loseClimax），靠 `grep -n` 逐条核对才发现。
→ 规则：**同一文件一次只发一个 Edit；改完必须 grep 核对落点。**

### 用户待办
1. ~~**授权提交**~~ ✅ **已授权并完成（2026-09-17 下午）**，见下节
2. ~~**真机听感确认**~~ ✅ **用户验收通过**：P0 三项**均能听出**；警报 loop 2s 4 轮**不吵**
3. v1.0.0 遗留：推送 commit 与 tag 到远端（仍仅本地）

---

## S2 收尾：听感验收 + 提交（2026-09-17 下午）

### 用户验收结论
- P0 三项（种植"噗" / 卡片就绪"叮" / 阳光掉落提示）**能听得出来** → 可闻性达标
- 警报 loop 2s 内 4 轮**不吵** → §I.3 里标注的降级风险「偏吵」未出现，`INTERVAL 0.62` 标定有效
- 已写入 `design/audio-guide.md` **§I.4 验收记录**表 + `production/v1.1-plan.md` S2 执行结果表

### 提交
| commit | 内容 |
|---|---|
| `c38662b` | `feat(V11-06): S2 音频补齐 B1-B10 + 大波警报 loop` —— 源码 + code-map + harness 护栏（7 文件，+475/-127） |
| `3011dba` | `docs(V11-06): 音频规格实现状态与验收记录 + 测试口径同步` —— README / audio-guide / v1.1-plan / tests 两份文档 / 记忆（7 文件，+206/-35） |

> **S2 提交全部落袋**：`c38662b`（源码）→ `3011dba`（文档）。工作树干净。
- 提交前复跑三件套门控：**23/23 · 30/30 · 55/55 全绿**
- 提交信息沿用仓库既有风格：`type(scope): 描述` 一行主题 + 空行 + 要点正文
- 拆两个 commit 的分界：**代码与它的机械派生物（code-map、用例、harness 探针）同一提交**，叙述性文档与记忆归档另起

### 下一步
S3 内容扩展（第三关 GDD `design/gdd/level-3.md` → `LEVELS[3]` 实现）→ S4 Playtest R2/R3 + 性能基准 + v1.1 发布包
> S2 已提交落袋，S3 可以开工（单文件需错开编辑窗口的约束已解除）

---

## 推送 GitHub（13:18–13:35）

远端：**https://github.com/ysyonline/PvZlite**（public，2026-09-17 新建，推送前为空仓库 / 默认分支 `main`）

### 结果
| 项 | 值 |
|---|---|
| 分支 | 本地 `master` → **重命名为 `main`**（对齐远端默认分支，避免默认分支空着） |
| 推送 | `refs/heads/main` = `17cfdea` ✅ 与本地 HEAD 一致（49 commit） |
| 标签 | `v1.0.0` → `f6c81a5` ✅（含解引用标签对象 `5f704a7`） |
| 远端复核 | API 回读：分支 `main` / 标签 `v1.0.0` / 最新提交 `17cfdea` / 根目录 9 项齐全 |
| 上传耗时 | 5 秒（`.git` 仅 588K） |

### ★ 排障：git push 挂死 → 根因是 `helper-selector`
**症状**：`git push` 零输出挂到超时；`git credential fill` 也挂；但 `git ls-remote` 正常（2.8s）。

**排查路径**（这个分步隔离法值得复用）：
1. `git ls-remote origin` → 通 ⇒ 网络/URL 都没问题，问题在**写鉴权**环节
2. `git -c credential.helper= credential fill` → 0.5s 秒回错 ⇒ git 本体正常
3. `git -c credential.helper='!"…/git-credential-manager.exe"' credential fill` → **14.8s 拿到凭据** ⇒ **GCM 本身好的**

⇒ 定位到**系统级配置 `credential.helper = helper-selector`**（PortableGit 自带 `git-credential-helper-selector.exe`）。无交互终端时它会去弹**图形选择框**，永远等不到用户点击 → 永久挂起。它是列表里第一个 helper，所以先于 GCM 执行并卡死。

**解法**：命令行覆盖 helper 列表（`credential.helper=` 空值会**清空**列表）：
```bash
git -c credential.helper= \
    -c credential.helper='!"C:/Users/user3667/.workbuddy/binaries/PortableGit/versions/1.2.0/mingw64/bin/git-credential-manager.exe"' \
    push -u origin main
```
**没有动全局/系统配置**（那是用户的既有设置，只做单次覆盖）。

**顺带清掉的干扰项**：`PortableGit/versions/1.2.0/etc/gitconfig.lock`（0 字节陈旧锁，13:25 由首次取凭据时留下，无进程持有）。已删除——**但它不是根因**，删完照样挂。

**其他确认**：GitHub 凭据此前已存在（GCM 里 `username=ysyonline`），无需重新授权；直连与 7890 代理**两条通道都通**，本次推送没配任何代理。

### 发布前隐私筛查（结论：可公开）
推送前扫了全部被跟踪文件：**无任何硬凭据**（api key / token / password / 私钥均为 0 命中），无代理端口、无 UAC/系统环境描述。唯一痕迹是 `docs` 外 1 处：`.workbuddy/memory/MEMORY.md` 里的 Node 绝对路径（含 Windows 用户名 `user3667`）——判定为低敏感，未处理。

## 仓库清理（13:40–13:45，commit `bce0ecc`）

上一节「遗留待办」三条的处理结果：
- ~~**`.tmp/pvs-check.js` 被跟踪并已公开**~~ ✅ **已清理**：`git rm --cached` + 删本地 + 新增 `.gitignore`。文件仍可从历史恢复：`git log --follow -- .tmp/pvs-check.js`（引入于 `efa9cab`）
- ~~仓库无 `.gitignore`~~ ✅ 已补（忽略 `.tmp/`、`.mutcheck/`、`node_modules/`、`dist/`、`build/` 等）
- 若希望后续 push 不再需要那串 helper 覆盖，可选：把 `helper-selector` 从系统配置摘掉，或在仓库级设 `credential.helper`（**未做**，属用户环境决定）

### ★ 本次第二个工具坑：`git add` 带不存在的 pathspec 会**整体失败**
删掉 `.tmp/pvs-check.js` 后又去 `git add .gitignore .tmp/pvs-check.js` —— 后一个路径已不在磁盘，**git 直接放弃整个 add**，`.gitignore` 静默漏加；因为当时加了 `2>/dev/null`，报错被吞，提交信息写着"新增 .gitignore"而内容里根本没有。
**靠 `git show --stat HEAD` 核对才发现**（提交信息 ≠ 实际内容）。
→ 规则：**`git add` 别带可能不存在的路径**；别用 `2>/dev/null` 吞 git 报错；**提交后必须看一次 `git show --stat`**。
→ 补救：该提交未推送，用 `git commit --amend` 修正（已推送的提交不要 amend）。

---

# 📌 新会话接手指南（2026-09-17 14:05 版 · ⚠️ 已被 L254 起的 15:40 版取代，仅存档）

## 一、当前状态速览
- **项目**：PvZ Lite，`D:/code/zw`，纯前端单文件 HTML（`plants-vs-zombies.html`，**1959 行 / 68 函数 / 44 常量**，规模一律以 `docs/code-map.md` 头部为准），零依赖零构建，仅 PC Chrome/Edge
- **远端（新）**：`origin` = **https://github.com/ysyonline/PvZlite**（public）。本地分支已由 `master` 改名 **`main`**，跟踪 `origin/main`
- **v1.0.0**：已封存 → commit `f6c81a5` / tag `v1.0.0`（annotated 对象 `5f704a7`）；**标签已推远端**；发布包在 `production/release/v1.0.0/`
- **v1.1**：`production/v1.1-plan.md`（方案 B）；**S1 ✅ + S2 ✅ 已完成并验收**，**S3（第三关）待开工**，S4 最后
- **HEAD** = `907201c`（memory 归档），已推远端；工作树干净，**52 个提交**

## 二、测试基线（三件套，必须全绿）
Node 绝对路径（**目录名带 `-3` 后缀**，写成 `22.22.2` 会 No such file）：
`C:/Users/user3667/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`

| 门控 | 命令 | 基线 |
|---|---|---|
| 烟雾 | `<node> tests/harness/run-smoke.js` | **23/23** |
| 回归 | `<node> tests/harness/run-all.js` | **30/30**（`--all` 53/53，`--smoke` 只跑烟雾） |
| 音频总线 | `<node> tests/harness/verify-bus.js` | **55/55** |

Bash 每命令开头统一：
`export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY`

## 三、改代码的硬规则
1. **先查 `docs/code-map.md` 定位 → 只读目标区块 → 精确编辑**，禁止全文通读
2. 改完重跑 `node tools/gen-code-map.mjs` 刷新行号；行数三方对齐（`wc -l` = `awk NR` = code-map）
3. **render 层改动必带真 loop 帧 + `console.error` 监视网**（逻辑层烟雾抓不到 render bug；曾有 `lastDt` 导致画面永久冻结的 S0）
4. **禁止对同一文件并行发多个 Edit**：并发写入互相覆盖、部分改动静默丢失，而每个调用都报"成功"。改完必须 `grep -n` 逐条核对落点
5. **`git add` 别带不存在的路径**（会整体失败且可能被 `2>/dev/null` 吞掉）；**提交后必看 `git show --stat`**
6. **无用户指令不提交代码**；提交走「代码+机械派生物」/「文档」两刀切（详见技能 `pvz-lite-verify`）
7. 引用关卡数值要按源码 `LEVELS[].waves` **逐波求和**核对，别信摘要（历史"28→25"是笔误，实际 24）

## 四、变异测试配方（证明测试有效）
```bash
cd "D:/code/zw"; NODE="C:/Users/user3667/.workbuddy/binaries/node/versions/22.22.2-3/node.exe"
mkdir -p .mutcheck && cp plants-vs-zombies.html .mutcheck/mutant.html
sed -i '<行号>s/<原式>/<改坏>/' .mutcheck/mutant.html
PVZ_HTML_PATH=".mutcheck/mutant.html" "$NODE" tests/harness/run-all.js | grep -E "<目标用例>|结果:"
rm -rf .mutcheck
```
标准变异：热区 `CARD_X0+i*CARD_W`→`170+i*CARD_W` 应挂 `REG-TRAP-04`；`window.addEventListener('keydown')`→`canvas.` 应挂 `REG-TRAP-06`
**判读**：变异后仍全绿 = 用例是空断言，必须重写；"没抓到"暴露的是**测试盲区**（S2 就靠这条查出 B3 只覆盖了自然掉落、漏了向日葵路径）

## 五、推送命令（★ 必须照抄，直接 `git push` 会挂死）
```bash
export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY
cd /d/code/zw
GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=Never git -c credential.helper= \
  -c credential.helper='!"C:/Users/user3667/.workbuddy/binaries/PortableGit/versions/1.2.0/mingw64/bin/git-credential-manager.exe"' \
  push -u origin main
```
- **根因**：PortableGit 系统配置里 `credential.helper = helper-selector` 排在 GCM 之前，无终端时弹图形框 → 永久挂起。`credential.helper=`（空值）作用是**清空**列表再只挂 GCM
- **不要改全局/系统配置**；GitHub 凭据已在 GCM（`username=ysyonline`），无需重新授权
- 无鉴权操作（`ls-remote` / fetch 公开仓库）不需要覆盖，**直连可用**（实测 2.8s；旧记的"直连必被 SNI 重置"已不成立）
- **推送后必须从远端回读校验**：`git ls-remote origin refs/heads/main` 比对 `git rev-parse HEAD`；tag 要比对 `refs/tags/v1.0.0^{}`（**annotated tag 用 `git rev-parse v1.0.0` 拿到的是标签对象，不是提交**）
- 若看到 `could not lock config file .../etc/gitconfig: File exists`：陈旧 0 字节锁，可删但**不是根因**，别被带偏

## 六、下一步（v1.1 剩余冲刺）
| 冲刺 | 内容 | 关键输入 |
|---|---|---|
| **S3 内容扩展**（下一个就做它） | 第三关 GDD → `LEVELS[3]` 实现；可选新植物/僵尸 | `production/v1.1-plan.md` §S3、`README.md` 待办 |
| **S4 验证发布** | Playtest Round 2（新手上手）/ Round 3（压力边界）+ 补测 T2/T3/T5/T7/T8、性能基准、v1.1 发布包 | `tests/playtest-plan.md` |

S2 已提交落袋，**"单文件需错开编辑窗口"的约束已解除，S3 可直接开工**。S3 与 S2 改的是文件内不同分区（`LEVELS`/植物僵尸表 vs `SFX` 表），但仍要一次只改一处。

## 七、用户未办事项
1. 签 `production/release/v1.0.0/RELEASE-CHECKLIST.md` I 节**用户审批栏**（QA/发布人栏已签，用户栏故意留空未代签）
2. 决定分发动作（默认单文件直接分发）
3. ~~推送提交与标签到远端~~ ✅ **已办（2026-09-17）**：`main` + `v1.0.0` 均已推 → https://github.com/ysyonline/PvZlite
4. 可选补跑 E5（Edge），把"接受未验证风险"转为已验证

## 八、SESSION 教训（本会话新增）
- **「并行编辑同一文件」是本会话最大坑**：6 组并行 Edit 静默丢了 6 处改动，每个调用却都报 "Successfully edited"。**grep 核对落点只信 grep，不信成功提示**
- **`git add` 带不存在的 pathspec 会整体失败**，且 `2>/dev/null` 会吞掉报错 → 提交信息与内容不符。**提交后必看 `git show --stat`**
- **凭据 helper 挂死要看"哪个 helper 在跑"**：分步隔离（`ls-remote` → 无 helper → 只挂 GCM）三步就能定位，别一上来怀疑网络
- **搜索结果/摘要里的数字不可信**：源码行数、用例数、关卡数值一律自己复算（本次 3 处摘要数字与源码不符）
- **成员/工具自述 ≠ 事实**：本会话两次靠"独立回读"抓到问题（远端 ref 回读、`git show --stat`）
- **调度专家团成员必须先 `TeamCreate`**，否则 Agent 报 `No active team found`

---

## v1.1 S3 第三关「月夜草坪」完成（14:05–15:00，未提交待授权）

### 流程
- 三件套起点全绿（23/30/55）→ TeamCreate `pvz-v11-s3` → V11-07 GDD（design-strategist）→ 用户拍板 4 项 → V11-08 实现（engineering-lead）→ 主理人独立核验 + 补强
- **用户拍板记录**：A 档轻量版「月夜草坪」/ startSun=100 / 波次表照抄 GDD §4.1 / bucket 2 只（W6+W7）
- V11-09（新植物/僵尸，方案 C 范围）**本次不做**，v1.2 候选

### 交付物
- `design/gdd/level-3.md`：371 行八节 GDD。文策渊初稿数值漂移（总量写 34 实为 **33**、L2 基线写 21 实为 **24**、§4.2 四处 hp 错）已退回修正，波次表代码块本身自洽
- `plants-vs-zombies.html` 1959→**1985 行**：`LEVELS[3]`（L85–L101，7 波 33 只，构成 normal15/cone8/fast8/bucket2，interval≥6，big=W3/W5/W7，night:true）；菜单选关循环上界与 x 基准两处（L671–672 onClickMenu、L1844–1845 drawMenu，`i<=3` + `bx=270`）；night 冷蓝滤镜分支（L1276–1283，复用 dusk 模式）
- `tests/harness/cases/SMOKE-024.js`：新增，26 条断言（T1–T10/T12/T13 + night/dusk 互斥 + **render 层真帧验证**）
- `tests/harness/cases/REG-END-02.js`：**有意的过时假设修正**（原断言 LEVELS[3]===undefined；L3 上线后平移为 setLevel(3)+锁 LEVELS[4]===undefined，用例本意不变）
- README / tests/README 口径同步；code-map 重生成
- **bucket 零成本红利**：STATS/绘制/尸体零件/音效/计分全链路早已就绪但 L1/L2 未配——L3 配上即得新单位，零新代码
- 关键行号：`LEVELS[3]` L85、night 分支 L1276、菜单循环 L671/L1844

### 门控终态（主理人独立复跑）
- 烟雾 **24/24** · REG **30/30**（--all 54/54）· 总线 **55/55** 全绿
- **变异测试 2/2 精准抓到**：① W3 fast 2→3 → SMOKE-024 T2 挂（总量=34≠33）② night 分支 GRID_X→未定义变量 → SMOKE-024 render 断言挂（ReferenceError 进监视网）
- 程基岩笔误 `2026-09-18`（6 处注释/文档）已由主理人 sed 修正为 09-17

### 本会话新教训
- **harness 的 ctx 是 Proxy 桩：改 ctx 方法名类变异测不出来**（调用被容错吞掉，全绿假象）。render 变异必须用「未定义变量 ReferenceError」这类 Proxy 拦不住的写法
- SMOKE-024 render 断言套路：console.error 监视网 + `g.rafQueue.shift()` 手动消费真帧（loop 内 try/catch 把帧异常打 console.error），比 `__stepFrame` 探针更直接
- GDD 文字口径必须与波次表代码块互证：逐波求和别信正文叙述（本次又见"34 vs 33"同类笔误）

### 下一步（S3 收尾 + S4）
1. **等用户授权提交**：建议两刀切——① `feat(V11-07)` GDD 文档 ② `feat(V11-08)` 源码+用例+文档同步（代码+机械派生物一刀）
2. 用户实机验收（GDD §8.2 M1–M5：菜单三钮点击、?level=3 直进、夜幕观感、W6 bucket 首秀、3 次大波横幅）+ 真机听感（bucket 金属碎裂音是 S2 能力首次实战发声）
3. 验收过 → S4（Playtest Round 2/3 + 性能基准 + v1.1 发布包）
4. 团队 `pvz-v11-s3` 未拆除（design-strategist / engineering-lead 已 idle），提交授权后可直接复用

### 工程化评估结论（15:30 用户问「要不要 Vite/React」，已口头答复，未写 ADR）
- **结论：v1.1/v1.2 维持单文件零构建，明确不上 React**（Canvas 命令式渲染与组件模型错位，54 条用例会全部作废，双击即玩分发特性丢失）
- **触发重评信号**：代码超 4000 行 / 泳池屋顶地形关 / 移动端触屏 / 服务端依赖（多人/云存档）
- 重评时首选路线：**Vite-only（ESM 拆模块 + 构建静态产物），React 仍不推荐**
- 用户未要求落 ADR；若日后要正式留档 → 派 engineering-lead 写 `docs/architecture/` ADR

---

## 📌 S3 提交完成（2026-09-17 15:52，新会话续）→ ✅ 验收通过（16:24），S3 全部完结

- **用户授权后两刀提交完成**：`da04d3a` = GDD 单文件（design/gdd/level-3.md，372 行）；`3cc65fb` = 源码+SMOKE-024+REG-END-02 平移+三份文档同步（6 files, +322/-163）；追加 `892217d` = v1.1-plan 归档 S3 执行结果。提交后 `git show --stat` 逐一核对，内容与信息完全一致
- **✅ 用户实机验收通过（16:24）**：M1–M6 全过，`e6345d3` 落档验收记录（GDD §8.2 + v1.1-plan）。**V11-08 闭合，S3 冲刺完结**
- 验收环境：本地静态服务器 `python -m http.server 8931 --bind 127.0.0.1`（后台任务 XpAp0U，仍在运行，S4 Playtest 可复用）；地址 `http://127.0.0.1:8931/plants-vs-zombies.html?level=3`
- **下一步 = S4**（V11-10/11/12）：Playtest Round 2/3（含补测 T2/T3/T5/T7/T8）+ 性能基准实测回填 perf-profile.md + v1.1 发布包（沿用 v1.0.0 模板）
- **✅ 已推送（16:28）**：`3ab6b1e..e6345d3 main -> main`，4 提交上远端。小坑：推送后本地 `origin/main` 引用短暂 `[gone]`（远端跟踪引用状态异常），`git fetch origin` 后恢复且确认同步——推送本身是成功的，遇到 `[gone]` 先 fetch 再判断，别慌
- **HEAD = origin/main = `e6345d3`，完全同步**
- **提交后工作树**：干净（仅剩 `.workbuddy/memory/` 两个会话记录文件，不进仓库提交范围——注意 `.workbuddy/memory/` 实际上是被 git 跟踪的，这两个文件的改动留在工作树属正常，下次 memory 归档时一并处理）
- `production/v1.1-plan.md` 已补 S3 执行结果节（对称 S2 格式），头部状态行已更新；**验收记录**在 GDD §8.2 与 v1.1-plan S3 节（`e6345d3`）
- ~~待用户：S3 实机验收~~ ✅ 16:24 全过
- 接手指南以**下方 16:30 版（⓪ 起）为最新**，15:40 版仅剩变异配方/推送命令/教训节有效

---
## 📌 新会话接手指南（2026-09-17 16:30 S3 完结时点，更新于同日 15:40 版之上——15:40 版的状态/下一步节已就地更新，其余节仍有效）

## ⓪ S4 开工入口（新会话第一件事）
- **任务**：S4 验证与发布 = V11-10 Playtest Round 2/3 + V11-11 性能基准 + V11-12 v1.1 发布包
- **开工动作**：先出 Playtest Round 2 测试清单。参考物：Round 1 报告在 `tests/playtests/`（若目录为空则查 `tests/` 下 playtest 相关文件）；补测项 T2/T3/T5/T7/T8 出处见 `tests/regression-plan.md` 与 `production/release/v1.0.0/KNOWN-ISSUES.md` #5
- **实机环境**：`python -m http.server 8931 --bind 127.0.0.1`（上一会话的服务器不跨会话存活，**新会话需重起**）；直通 `http://127.0.0.1:8931/plants-vs-zombies.html?level=3`
- **发布包**：沿用 `production/release/v1.0.0/` 模板结构，产出放 `production/release/v1.1/`


## 一、当前状态速览
- **项目**：PvZ Lite，`D:/code/zw`，纯前端单文件 `plants-vs-zombies.html` **1985 行**（规模以 `docs/code-map.md` 头部为准），零依赖零构建，仅 PC Chrome/Edge
- **v1.1 进度**：S1 ✅ / S2 ✅（`c38662b`）/ **S3 ✅ 全部完结（提交 `da04d3a`+`3cc65fb`+`892217d`，实机验收 16:24 全过 `e6345d3` 落档）** / S4 验证发布（**当前任务**）
- ~~工作树改动~~（已随 S3 提交清空；`.workbuddy/memory/` 两文件的会话记录改动常驻，不必处理）
- **S3 交付内容**：第三关「月夜草坪」= `LEVELS[3]`（L85–101，7 波 33 只，bucket 2 只 W6/W7，startSun=100，night 冷蓝滤镜 L1276–1283）+ 菜单三钮（L671–672、L1844–1845）+ `SMOKE-024`（26 断言含 render 真帧）+ GDD `design/gdd/level-3.md`（371 行，数值经主理人复算修正：总量 33 非 34）
- **工作树**：✅ 已随 S3 两刀提交清空（`da04d3a` / `3cc65fb`）；`.workbuddy/memory/` 两文件为会话常驻改动，不算脏
- **HEAD** = `e6345d3`，本地领先远端 4 个提交（**尚未推送**，推不推等用户指示）
- 用户拍板记录（S3）：A 档轻量版 / startSun=100 / 波次表照抄 GDD §4.1 / bucket 2 只
- 工程化已评估：维持单文件，触发条件见上文「工程化评估结论」

## 二、测试基线（三件套，S3 后新数字，必须全绿）
Node 绝对路径（**目录名带 `-3` 后缀**）：
`C:/Users/user3667/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`

| 门控 | 命令 | 基线 |
|---|---|---|
| 烟雾 | `tests/harness/run-smoke.js` | **24/24**（含 SMOKE-024） |
| 回归 | `tests/harness/run-all.js` | **30/30**（`--all` = 54/54） |
| 音频总线 | `tests/harness/verify-bus.js` | **55/55** |

Bash 每命令开头统一：
`export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY`

## 三、硬规则（不变，含新增）
1. 先查 `docs/code-map.md` 定位 → 只读目标区块 → 精确编辑；改完重跑 `node tools/gen-code-map.mjs`
2. **禁止对同一文件并行 Edit**；改完 `grep -n` 核对落点
3. render 层改动必带真 loop 帧 + console.error 监视网（SMOKE-024 已示范套路）
4. `git add` 别带不存在路径；提交后必看 `git show --stat`
5. 无用户指令不提交；推送用第五节 helper 覆盖命令
6. 关卡数值按源码逐波求和复算，别信 GDD 正文叙述（"34 vs 33" 又现一次）
7. **变异测试新知**：harness ctx 是 Proxy 桩，改 ctx 方法名的变异测不出；render 层要用「未定义变量 ReferenceError」类变异（本会话实证）

## 四、变异测试配方（同前 + S3 变异点）
- 波次数据变异：改 L95 `['fast',2]`→`['fast',3]` 应挂 SMOKE-024 T2
- render 变异：L1282 `GRID_X`→`UNDEFINED_VAR_XYZ` 应挂 SMOKE-024 render 断言
- 旧两条（热区 L40、keydown 绑定）照旧，命令模板见 14:05 版第四节

## 五、推送命令（不变，照抄）
```bash
export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY
cd /d/code/zw
GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=Never git -c credential.helper= \
  -c credential.helper='!"C:/Users/user3667/.workbuddy/binaries/PortableGit/versions/1.2.0/mingw64/bin/git-credential-manager.exe"' \
  push -u origin main
```

## 六、下一步（按序）
| # | 事项 | 说明 |
|---|---|---|
| 1 | ~~S3 提交~~ | ✅ 已完成（`da04d3a`/`3cc65fb`/`892217d`） |
| 2 | ~~S3 实机验收~~ | ✅ **用户验收全过（16:24）**，`e6345d3` 落档 |
| 3 | ~~推送~~ | ✅ 已推送（16:28），本地与 origin/main 同步于 `e6345d3` |
| 4 | **S4 验证发布（当前任务）** | Playtest Round 2/3 + 补测 T2/T3/T5/T7/T8 + 性能基准回填 `docs/architecture/perf-profile.md` + v1.1 发布包（沿用 v1.0.0 模板） |
| 5 | v1.2 候选 | 地瓜 AOE / 移动端 / 铲子退款 / 工程化重评（Vite-only 路线） |

## 七、用户未办事项（延续）
1. v1.0.0 发布清单「用户审批栏」签字（`production/release/v1.0.0/RELEASE-CHECKLIST.md` I 节）
2. 决定分发动作（默认单文件直发）
3. 可选补跑 E5（Edge）
4. ~~S3 验收 + 提交授权~~ ✅ 全部完成
5. ~~是否推送~~ ✅ 已推送（16:28）

## 八、SESSION 教训（本会话新增 3 条，全量见上文 S3 节）
- **harness Proxy 桩吞方法名变异**：变异测试也要先验证「变异能被测出」，方法名类变异对 Proxy ctx 无效
- **成员自述数值三连漂**：GDD 34→33、hp 四处、日期 09-18，主理人复算是最后防线，别省
- **bash 偶发 cygwin 崩溃**（exit 3221225477）：重跑同命令即可，非项目问题

---

# 16:35+ S4 开工（团队 pvz-v11-s4 · 三成员并行）

## 一、S4 执行结果（V11-10 / V11-11 / V11-12 可完成部分全部交付）
| Task | 成员 | 产出 | 状态 |
|---|---|---|---|
| V11-10 Playtest 执行包 | 严守真 | `tests/playtests/round-2-execution-pack.md`（17 任务）+ `round-3-execution-pack.md`（17 任务） | ✅ 清单就绪，**实机执行待用户**（主测本人+亲友副测） |
| V11-11 性能基准 | 程基岩 | `tests/perf/bench.js`（可复现：`node tests/perf/bench.js`，seed=20260918）+ 回填 `docs/architecture/perf-profile.md` §7（自动段，幂等） | ✅ **PASS（无头口径）**：三场景 p95 = 0.13/0.33/0.47ms（预算 1%/2%/3%）；draw calls 估算口径 249/637/1138 vs 静态估算 155/605/1009（B/C 吻合 +5.3%/+12.8%，A +60.6% 系空场 HUD 占比，无工程意义）；0 帧异常 |
| V11-12 发布包 | 路远行 | `production/release/v1.1/` 三件：RELEASE-NOTES（19 commits 实证）/ CHECKLIST（A9 性能已回填）/ KNOWN-ISSUES（9 条主体闭合 / #5 待用户 / 2 观察项） | ✅ 草案 v2，定稿被 Playtest 实机执行阻塞 |

- R1 五项补测分配：T7/T8（下一关直进、快捷键）→ Round 2；T2/T3/T5（只用豌豆/向日葵+铲子/不点阳光）→ Round 3（需控局技巧）；结果执行后回填 `round-1-report.md` 闭合 KNOWN-ISSUES #5
- R3-T8 期望已按 v1.1 语义迁移（通 L2 → 解锁 L3；"已通关全部"只在通 L3 后出现），计划书原文未改、执行包内有专项说明
- #8 口径：主体闭合（无头实测已回填），残留「真机 DevTools Performance 复核」列发布后跟踪
- **版本定版点**：源码 L42 `VERSION='v1.1.0-dev'` → 冻结前须定版 `'v1.1.0'`（源码改动需用户授权，改后重跑 A1–A4；SMOKE-021 不锁具体值，测试零改动）
- 核实修正：S3 实为 4 commits（非 3）；v1.0.0 标签已在远端（#10 完全闭合）；README 教学示例「第三关·泳池」占位名与已上线「月夜草坪」重名 → N2 观察项（P3，v1.2 候选）；行数口径 1985 行 / 81,095 字节

## 二、S4 剩余待用户事项（定稿路径，按序）
1. **实机 Playtest R2/R3**（用户本人+亲友；最后一个外部依赖）
2. **版本定版授权**（②→重跑门控）
3. **冻结三件套批准**：G1 固化副本 + G2 SHA-256 + G3 打 `v1.1.0` 标签（前提 1+2 完成）
4. A8 门控独立复跑（quality-lead 承接，与 3 同批）→ 5. C13 已知问题确认 + I 节用户签字

## 三、汇编核验记录（主理人独立做过，防自报失实）
- git 范围：perf-profile.md 仅文末 +35 行；新文件仅 tests/perf/bench.js + round-2/3 执行包 + production/release/v1.1/ 三件；游戏源码 / tests/harness / v1.0.0/ 零触碰 ✅
- 抽查：§7 实测段带 PERF-BENCH:AUTO 标记与双口径/局限披露；R3 执行包任务表 T1-T8+R1 三条+T9-T14 完整、结果栏全空、判定公式未被改 ✅
- 三成员交叉数值一致：LEVELS[3] 波次 2/3/4/5/4/7/8=33、sirenLoop 0.62s/轮、门控 24/24·30/30·55/55 ✅
- 团队 pvz-v11-s4 成员已全部完工待命（shutdown 未发，等 Playtest 排期决策后一并处理）

## 四、18:19 用户验收 + 版本定版（已执行）
- **用户口径**：门控 + **完整 Round 2/3**（含亲友新手场次 + 地狱 4× 压力）**无异常**；但**未提交书面逐项记录** → 证据等级记为「用户口述验收」，报告禁止编造逐项 ✅/耗时/观察数据（已明确交待 quality-lead）
- **定版已执行**（用户授权）：`plants-vs-zombies.html` L42 `'v1.1.0-dev'` → `'v1.1.0'`；引用点 L1897 画布角落 + L1980 控制台日志同步生效（同一常量）——**这是本冲刺唯一一处源码改动，尚未 commit**
- **定版后门控重跑全绿**：24/24 · 30/30 · 54/54 · 55/55（主理人执行，quality-lead 另做 A8 独立复跑）
- 在办：quality-lead 汇编 round-2/3-report.md + 回填 round-1-report.md 五项补测 + A8；release-ops-lead 刷新 A7/B/C5-C6-C11/D7(需重测字节数)/H（草案 v3）
- **待用户批准**：G1–G3 冻结三件套（前提：定版改动需先 commit——提交同样需用户授权）+ 最终签字 + 分发动作
- 教训增量：用户口头验收 → 报告必须如实标注证据等级，不能把「口述」写成逐项实测；宁可显式记录局限

## 五、18:2x 用户三项拍板（S4 收尾决策包）
1. **授权提交并推送**（定版构建 + S4 全部产出）→ 用 helper 覆盖命令推送
2. **批准冻结三件套**（G1 固化副本 / G2 SHA-256 / G3 打 `v1.1.0` 标签）→ 三项全执行
3. **选择补做一场带书面记录的新手试玩**（把 Round 2 教学维证据等级从「口述」提档）→ 记录表＝`round-2-execution-pack.md` 第六节；记录回来后由 quality-lead 升级报告与判定；若查出阻断问题走热修路径、冻结副本重做
- 执行顺序：release-ops 收口清单（草案 v4 终版）→ commit + push → G1/G2/G3 冻结 → 用户签 I 节
- 遗留待用户一句话：E5（Edge 浏览器）仍为 v1.0.0 延续的未验证风险，需豁免或补测
- quality-lead 已授权：`round-1-report.md` 补一条带日期的「REG 缺口已闭合」注记（原文保留不改）

## 六、18:4x 提交 + 冻结执行结果
- **5 个提交（已推送远端 `e6345d3..0fae781`）**：`6b733be` 定版（源码 1 行）/ `7db9edb` 性能基准 bench.js+perf-profile §7 / `37141f4` Playtest 执行包与报告+R1 补测回填 / `1c12afe` 发布包三件+记忆 / `0fae781` 冻结记录（清单 18 行增删 + 固化副本 1985 行）
- **G1 固化副本**：`production/release/v1.1/artifacts/plants-vs-zombies.v1.1.html`（81,091 字节）
- **G2 哈希**：母本 = 副本 = `29bc5a364a1bc41162f0fc4993f570d89cd0dd8cd362107284f612b69de7d6f1`（前后 8 位 `29bc5a36` ↔ `9de7d6f1` 一致）；清单 D8/G1/G2/H 节/文档状态 v4 均已回填
- **G3 打 `v1.1.0` 标签有意暂缓**：待新手场次记录入库、round-2-report.md 定稿后再打，避免标签指向旧版文档
- 发布包终态：综合判定「**可放行（待用户签字）**」；#5 已闭合（证据等级据实写为用户口述）；新增 N3（教学维零书面样本）/N4（4× 末波无量化 profile，建议并 #8）；E5（Edge）仍未验证待豁免
- **剩余待用户**：① C15 已知问题全量确认+豁免 ② 残留风险接受（N3/N4/E5）③ 新手场次记录（已选补做）④ G3 标签执行 ⑤ 三方签字（I 节）⑥ 分发动作
- 冻结后源码**不得再改**；若改须重走门控并重做 G1/G2（清单 G.1 已写明）

---

# 📌 新会话接手指南（v1.1 收尾版 · 2026-09-17 18:45 归档）

> 本文件上方 L269 的「⓪ S4 开工入口」是**今日早些时候**写的，内容已过期（S4 机器侧已跑完），**以本指南为准**。

## 一、当前状态（一句话）
**v1.1 机器侧全部完成并推送远端**（HEAD `0fae781`，与 origin/main 同步）；**源码已冻结**（哈希 `29bc5a364a1bc41162f0fc4993f570d89cd0dd8cd362107284f612b69de7d6f1`，81,091 字节）；**发布包综合判定「可放行（待用户签字）」**；**距 GO 只剩用户动作**。

## 二、明天要做什么（按序，全部是用户/收尾动作）
| # | 事项 | 谁做 | 说明 |
|---|---|---|---|
| 1 | **新手场次记录入库** | 用户采集 → `quality-lead` | 用户已选「补做一场带书面记录的新手试玩」；记录表＝`tests/playtests/round-2-execution-pack.md` **第六节总表**，必需 5 项：真新手画像 / T3·T4·T5 逐项 / T6 波次+是否 3 分钟内 / **T8 自述机制原话** / 卡壳点。记录到手 → 更新 `round-2-report.md`（教学维提档「书面」+ 注明单样本）→ 重出 Round 2 判定 |
| 2 | **G3 打 `v1.1.0` 标签** | 主理人 | 有意押后到第 1 项定稿后（避免标签指向旧版文档）；命令 `git tag -a v1.1.0 -m "PvZ Lite v1.1.0"`，再用第五节 helper 覆盖命令推标签 |
| 3 | **C15 已知问题全量确认 + 豁免** | 用户 | 含 N3（教学维，若第 1 项完成则被覆盖）、N4（4× 末波无量化帧率，建议并入 #8 真机复核）、**E5（Edge）仍未验证**——需用户再书面豁免一次或补测 |
| 4 | **I 节三方签字** | QA/发布人/用户 | `production/release/v1.1/RELEASE-CHECKLIST.md` I 节；**用户栏必须本人签，不得代签预填**（v1.0.0 教训） |
| 5 | **分发动作** | 用户 | 单文件直接分发，渠道由用户定；分发件即冻结副本（哈希见上） |

**发布后跟踪项**：KNOWN-ISSUES #8 真机 DevTools Performance 复核一次（同时闭合 N4）。

## 三、硬规则（不变 + 本次新增）
1. **v1.1 源码已冻结**：`plants-vs-zombies.html` **不许再改**。确需改 → 重跑全套门控 + **重做 G1/G2**（清单 G.1 写明）→ 之前的哈希作废
2. 改代码仍是：查 `docs/code-map.md` → 只读目标区块 → 精确编辑 → 重跑 `node tools/gen-code-map.mjs`
3. `v1.0.0/` 与 `v1.1/artifacts/` 两处冻结产物**只读**，一字节不可动
4. 提交前 `git add` 别带不存在的路径；提交后必看 `git show --stat`
5. 高影响动作（打标签 / 分发 / 撤回 / 热修）一律人工审批

## 四、测试基线（本日实测，全绿）
Node 绝对路径：`C:/Users/user3667/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`（**带 `-3` 后缀**）
| 门控 | 命令 | 基线 |
|---|---|---|
| 烟雾 | `tests/harness/run-smoke.js` | **24/24** |
| 回归 | `tests/harness/run-all.js` | **30/30**（`--all` = **54/54**） |
| 音频总线 | `tests/harness/verify-bus.js` | **55/55** |
| 性能基准 | `tests/perf/bench.js` | PASS（三场景 p95 ≤3% 帧预算，幂等回填 `perf-profile.md` §7） |
Bash 每命令开头：`export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY`

## 五、推送/打标签命令（照抄，否则挂死）
```bash
export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows"; unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY
cd /d/code/zw
GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=Never git -c credential.helper= \
  -c credential.helper='!"C:/Users/user3667/.workbuddy/binaries/PortableGit/versions/1.2.0/mingw64/bin/git-credential-manager.exe"' \
  push origin main
# 打标签：git tag -a v1.1.0 -m "PvZ Lite v1.1.0"   然后 push origin v1.1.0
```

## 六、本次会话教训（新增 4 条）
- **口头验收必须如实降级证据等级**：用户口述"没问题"不等于书面逐项记录；报告写「用户口述验收 + 自动化独立复核」，**不得写「逐项书面验收通过」**。这条写进了发布说明的措辞约束
- **主理人口径也会错**：我把 S3 说成 3 个提交，成员 git 实证是 4 个；数值一律以 git/源码实证为准
- **定版/冻结要讲顺序**：先 commit → 再 G1/G2 → 标签等到文档定稿；顺序反了会出现"标签指向旧版文档"
- **成员独立性有效**：A8 独立复跑（quality-lead 另跑一遍）与主理人自查数字一致，是可信度的来源，别省

## 七、团队状态
本会话团队 `pvz-v11-s4`（quality-lead / engineering-lead / release-ops-lead）**已全部完工并 shutdown**。明天需要协作时重新 `TeamCreate` 再 spawn（成员 Agent ID：`quality-lead` / `release-ops-lead` / `engineering-lead` / `design-strategist` / `art-director` / `audio-director`）。
