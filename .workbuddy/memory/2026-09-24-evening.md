# PvZ Lite 2026-09-24（晚班 · 家庭机）

## 20:2x–20:4x 家庭机 · v2.0.1 施工+核验+提交推送+发布包（用户令「开工」→ 三项 AskUserQuestion 授权）

- **开工核验（T-601）**：main=`4f533c6` 与远端一致（ls-remote 实测，非 fetch 缓存）；本机 Node 实际版本目录=**22.22.2-3**（记忆中 22.22.2 不带后缀，已修正认知）；源码 190,847B/3618 行=v2.0.0 冻结指纹对齐
- **施工（程基岩执行，T-602~T-604）**：v2.0.1 三处 `!testMode` 前置（onClickSelect L1272 解锁检查 / onClickEnd L1319 / drawSelect L3441 locked 视觉）+ VERSION L61→v2.0.1；占位 toast 两处（L1271/L1318）按拍板未动；行数不变 3618
- **自证 v20-test-unlock.js（新建）**：A 段新源 14/14（U3 解锁定闸★/U5 占位 toast 保留★toastT>0 正判据/U7 play+sun=9999）；B 段 v2.0.0 旧源判别 3 红（预期≥2；用例 5 占位拦截绿=与版本无关，预期绿）；harness `loadGame({search:'?test=1'})` 走 L509 location 桩，零 harness 改动
- **定版通扫（T-604）**：5 处活默认值改 v2.0.1（prelude.js L24/L52·get VERSION()、v17-acc L119 VER_EXP、v18-acc L26 注释/L53 expectVer、r9a-metric-spec.md L95/L97）+ 复扫零残留（SMOKE-021 版本形断言与版本无关，无需改）
- **门控回归**：run-gates.js 被 spawnSync EBUSY 阻塞（**主理人会话同样复现**=会话级环境问题非机器级）；直跑三门控等效验证全绿：FULL 87/87 · BUS 56/56 · BENCH PASS（i5-7500T 数值高于上版 i5-12500 记录=环境差异，DC 结构量五场景一致零漂移；perf-profile §7 幂等回填）
- **迁移复核**：v20-menu-nav 20/20 · v20-select-draw PASS（零迁移拍板成立）
- **★ EBUSY 环境异常（新坑）**：本会话 spawnSync/execFileSync 对任意 .exe 恒 EBUSY（node/git/cmd 全灭，复跑无效），异步 spawn 正常——影响 run-gates.js 一键入口与 git show 导出；workaround=逐门直跑 + 异步 spawn 封装 git show（程基岩 v20-test-unlock B 段先例）；已记入 v2.0.1 KNOWN-ISSUES 工具链节
- **版本口径变化**：v2.0.1 工作区字节数 **191,131B**（v2.0.0=190,847B，+284B 来自注释扩充），LF SHA-256 `371d07d0…5eeef`；行数不变 3618
- **发布包（T-606）**：production/release/v2.0.1/ 四件套（RELEASE-NOTES/CHECKLIST/KNOWN-ISSUES/冻结副本），对齐 v1.2.1 补丁版粒度（变更表+零其他变更声明+回滚=v2.0.0 冻结副本）；冻结副本 Node 异步 spawn git show 导出（EBUSY 规避）
- **授权与执行**：用户三项拍板（提交并推送 / 打发布包+tag 并推送 / EBUSY 记 KNOWN-ISSUES+日志）；tag v2.0.1 显式推送
