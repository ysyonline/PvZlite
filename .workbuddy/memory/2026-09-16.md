# 2026-09-16

## 会话归档（16:07，用户开新窗口；上午段归档见文件后半部分）

### 本次会话完成（14:04-16:07，5 个 commit，最新 `887a288`）

#### 1. 音频无声问题根治（14:04-14:45，commit 76ed6cb）★ 本日最重要成果
- **现象**：开局点阳光/种植无声，♪ running；战斗音效响过之后一切正常。用户反馈了 3 轮才定位
- **诊断过程**（5 个诊断页入库 `tools/audio-diag*.html`，可复用）：
  - diag1：A(audio 元素)✓ / B(WebAudio 长音 0.5)✓ / C(游戏同款短音)**时有时无** → 排除系统与浏览器
  - diag2 二分：D 原样/E 加音量/F 加长/G 提前排程 **全部 4/5** → 音量/时长/排程全排除
  - 决定性线索（用户自报）：战斗音效响过后 UI 音效就正常 → **管线空闲→唤醒过渡期吞开头音频**
  - diag4：K 可闻长音预热 5/5 vs L 静音保活 4/5 → **数字静音被驱动功率门控，只有真实非零样本能撑住设备**
  - diag5：M 17.5kHz 超声保活 5/5 且不可闻（胜出）；N 周期微热可闻（淘汰）
- **修复**：`initAudioBus` 启动常驻振荡器 17500Hz@0.005 **直连 destination 不走 master**（静音主闸不切断样本流）
- 顺带移除 ♪ 临时诊断徽标 + audioResumeTries；SMOKE-014/016 断言改基线差值式；新增 SMOKE-017（保活存在/参数/幂等）
- **教训（重要，可迁移）**：WebAudio「有声但听不到」类问题，先分清逻辑层/浏览器层/驱动层——本例逻辑层完全正常，是 **USB 声卡驱动的静音功率门控**，代码怎么改排程都没用

#### 2. 覆盖种植 bug 修复（14:43-15:00，commit 0390a2d）
- onClick 种植分支缺格子占用检查 → 补 `plants.some(col,row)`，命中 deny + toast「这格已经有植物了」，**保留选中**方便改点空格
- SMOKE-018（4 断言）+ 旧版对照验证能抓出 bug

#### 3. BGM 上线 + 可闻性重做（14:43-15:52，commit 0390a2d / 0c228b0）
- 初版听不到（用户反馈）：①低音线 130-196Hz 小音箱放不出 ②0.045×env0.5≈0.022 太轻
- **重做**：32 步循环 C-G-Am-F 和声；旋律 triangle 523-1047Hz vol 0.07 + 低音 sine 根音连音 vol 0.11，走 env 总线
- `setState` 驱动 updateBGM（play 起/menu·end 停/mute 停+续），loop 每帧兜底启动
- **教训**：合成 BGM 可闻性要按「总线衰减后 × 设备频响」标定，不能只看节点音量
- **BGM 是否可闻尚未获用户确认**（重做后未回报）

#### 4. harness/工具链升级（随上面两项落地）
- vm 沙箱补 `window.setInterval/clearInterval` stub（BGM 定时器必须 window 前缀）
- `__probe` 补 `bgmOn`/`audioKeepAlive`（对缺失全局做 typeof 守卫，旧版文件不炸）
- 新增 `setStateMenu`/`setMuted` API；run-smoke 全局支持 `PVZ_HTML_PATH`（原仅 SMOKE-013 单例支持）

### 当前基线与状态
- 烟雾 **19/19**（`...node.exe tests/harness/run-smoke.js`）+ 总线 **47/47**（`.../verify-bus.js`）
- git 干净，最新 commit `887a288`；代码地图 1844 行/64 函数（改完代码重跑 `node tools/gen-code-map.mjs`）
- 代码导航用 `docs/code-map.md`，禁止全文通读（README 硬规则）

### 下一步（新会话从这里接）
1. **等用户确认**：开局音效是否正常、BGM 是否可闻（重做后未回报）
2. **Playtest Round 1**：执行包 `tests/playtests/round-1-execution-pack.md`（V1-V8 专项 + T1-T9 任务 + O1-O6 观察），用户试玩 + 回填
3. Step 5 回归 26 条 → Phase 7 发布
4. 遗留 P1：音效 B1-B4 分层（种植落地/卡片就绪/阳光分层/僵尸死亡分层）、sirenLoop
5. B/C 方案（配置外置/拆文件）：等要加第三关/新植物时再评估

---

## 以下为上午段归档（14:00 前的会话，原文保留）

### 当日完成总览（10:00-14:00，共 20+ commit，最新 `00051da`）
1. **Step 3 手感七项落地**（F-04 震动 / F-01 种植动画 / F-02 死亡零件 / F-03 爆炸五层 / F-06 大波预警 / D-11 圆角化 / D-14 已验）+ 修复 F-01 引入的 `lastDt` S0
2. **用户手感调优 6 轮**：结束画面去震动 → L2 温和削峰（startSun 150 / 总量 28→25 / fast 大波 3→2）→ 波次清场硬门槛（上一波清完才开新波，25s 兜底）→ 大波预警提前（剩 ≤2 只即报）→ 预警时长 4s→2s → SMOKE-012 清场门槛回归
3. **S1 静默缺陷**：`SFX.bigWaveImpact` 被调用但未定义 → 大波刷怪帧必炸（被整帧隔离吞掉）→ 按 audio-guide §B7 补实现 + SMOKE-013 双层防护
4. **L2 直进通道**：`?level=2` 免解锁 + `pvz_unlocked` localStorage 持久化 + SMOKE-011 平衡契约
5. **省 token 工具化（方案 A）**：`tools/gen-code-map.mjs` → `docs/code-map.md`（分区/函数→绝对行号 + 高频速查表）；README 写入「先查表定位、禁止全文读」规则。**B/C 方案待用户要加新内容时再评估**
6. **Step 4 Playtest 启动**：执行包落盘 `tests/playtests/round-1-execution-pack.md`（V1-V8 专项验收点 + T1-T9 任务表 + O1-O6 观察 + 判定标准）
7. **音频三连修**（用户报告 3 次）：预热 → 未就绪补播队列（`audioQueue` + `flushAudioQueue` 每帧冲洗）→ **仍未获用户确认**

### 挂起点（新会话从这里接）
- **用户已回报（14:10 新会话）**：仍然无声 + ♪ **running** + 双击本地文件玩 → 自动播放策略嫌疑排除，代码链路复查正常（tone→ui 0.6→master→destination，无静音态）→ 嫌疑收窄到**系统音量/输出设备**或 **WebAudio 输出层**
- 已产出 `tools/audio-diag.html`（与游戏代码无关的三层测试：A=audio 元素系统通道 / B=WebAudio 直连 0.5 / C=游戏同款音量链路）+ 环境设备枚举
- **用户首轮回报（14:15）**：A 有声、B 有声、**C 时有时无**（连点 C 一会儿有声一会儿没声）；默认输出设备 = USB Pro Audio，与实际聆听设备一致；诊断页跑在 WorkBuddy Electron 预览里（UA 已确认）
- **关键推断**：B(0.8s/0.5音量) 稳定有声 vs C(0.09s/0.12) 间歇 → 嫌疑锁定**短音被吞 / 音量过小 / 排程贴帧**三选一
- 已产出 `tools/audio-diag2.html` 二分版：D=游戏原样 / E=只加音量0.5 / F=只加长0.4s / G=只提前400ms 排程，各连播 5 声数个数
- **二轮回报（14:20）**：D/E/F/G **全部 4/5 声** → 音量/时长/排程全部排除，是播放层面**周期性掉一声**（G 有 400ms 提前排程也丢，排除排程截断）
- 已产出 `tools/audio-diag3.html`：H=6s 连续长音（数中断次数，掉帧会显形）+ J=连播 10 声（记缺第几声，规律=系统性 / 随机=设备级）
- **决定性线索（14:25，用户自报）**：游戏里开局点阳光/种植无声，但**豌豆子弹命中怪物的战斗音效响过之后，所有 UI 音效就正常了** → 根因模型锁定：**音频管线空闲→激活的过渡期会丢弃开头音频**；管线被连续播放撑热后就一切正常。完美解释 diag2 四组全部 4/5（每轮第一声前管线已静默数秒）和游戏开局现象（ctx 在首次 pointerdown 创建，开局几秒内的音效全在「设备开启窗口期」被吞）
- 已产出 `tools/audio-diag4.html` 修复方案验证：K=2s 可闻长音预热→5 短音（对照）；L=**静音循环 buffer 常驻保活**（gain 0.0001，听不见）→3s 后 5 短音
- **三轮回报（14:30）**：**K=5/5，L=4/5** → 决定性结论：**真实有声样本流动才能撑住管线，数字静音会被驱动功率门控**（空闲→唤醒过渡期吞开头音频）。游戏现象完全吻合：战斗音效连续响→管线热→之后全正常；开局冷管线→点阳光/种植被吞
## 音频无声根治：超声保活音源（14:35-14:45，commit 76ed6cb）
- **方案验证收敛**：M=17.5kHz@0.005 超声保活 → 短音 5/5 且人耳不可闻（胜出）；N=周期微热 → 5/5 但「啵啵声」可闻（淘汰）
- **游戏修复**：`initAudioBus` 末尾启动常驻振荡器 17500Hz@0.005，**直连 destination 不走 master**（静音主闸归零也不切断保活样本流）；`AudioBus.keepAlive` 标志
- 移除 ♪ 诊断徽标 + audioResumeTries 计数（使命完成）
- **测试适配**：保活振荡器使所有音频用例的 oscStarted 计数 +1 → SMOKE-014 改为 expect 1、SMOKE-016 改为**基线差值式**（对初始化音源免疫，防再次加常驻音源时连锁改断言）
- 新增 **SMOKE-017**：保活存在性（keepAlive 置位）/ 参数（freq 17500 / gain 0.005 / 直连 destination）/ 幂等（重复 startGame 不重复启动）
- 基线 **17/17** + 总线 47/47；tests/README 16→17 同步；代码地图刷新（1782 行/62 函数）
- 诊断页 5 个已入库 tools/audio-diag*.html（保留供日后同类问题复用）
- **待用户真机确认**：开局点阳光/种植应有声；若仍有问题再约诊断
- 下一步：Playtest Round 1（执行包在 tests/playtests/round-1-execution-pack.md）→ Step 5 回归 26 条 → Phase 7

## 覆盖种植修复 + BGM 上线（14:43-15:00，commit 0390a2d，用户反馈两项）
- **Bug：同格覆盖种植**：onClick 种植分支只查阳光+冷却，没查格子占用 → 修复为 `plants.some(col,row)` 命中则 deny + toast「这格已经有植物了」，**保留选中**方便改点空格
- **BGM**：WebAudio 合成 8 小节低音回环（bass sine 0.045 + 五度 triangle 0.018，走 env 总线 0.5），`setState` 驱动 `updateBGM()`（play 起播 / menu·end 停），loop 每帧兜底启动（覆盖上下文晚就绪），mute 切换同步停/续；时间轴自续 window.setInterval 120ms 补齐未来 0.3s 拍点（标签页休眠回来对齐）
- **harness 三处升级**：①vm 沙箱补 `window.setInterval/clearInterval` stub（BGM 定时器必须用 window 前缀，裸 setInterval 在沙箱里 ReferenceError）②`__probe` 补 `bgmOn` 并对 AudioBus/BGM 做 typeof 守卫（旧版文件 probe 不炸）③新增 `setStateMenu`/`setMuted` API + run-smoke 全局支持 `PVZ_HTML_PATH`（原先只有 SMOKE-013 单例支持）
- **SMOKE-018**（覆盖种植 4 断言：拒绝/不扣阳光/选中保留/铲后可再种）+ **SMOKE-019**（BGM 生命周期 4 断言：menu 不启/play 起/menu 停/静音停+续）
- **SMOKE-018 旧版对照验证通过**：490d4e7 版本跑出 14/19，018 按设计失败（能抓 bug），14/16/17/19 因新特性不存在按设计失败
- 坑：向日葵卡片冷却 ~7.5s，用例里铲后重种要先 `g.tick(10)` 走完冷却
- 基线 **19/19** + 总线 47/47；代码地图 1837 行/64 函数

## 下一步
1. 用户真机验证：开局音效（超声保活）+ BGM + 覆盖种植拒绝
2. Playtest Round 1（执行包 tests/playtests/round-1-execution-pack.md，V1-V8 + T1-T9）
3. Step 5 回归 26 条 → Phase 7 发布
4. 遗留 P1：音效 B1-B4 分层、sirenLoop

## BGM 可闻性重做（15:52，commit 0c228b0，用户反馈「没有听到背景音乐」）
- 代码链路排查无误（BGM 确实在启动）→ 问题在**声学层**：①低音线 130-196Hz 小音箱/笔记本无法重放 ②音量 0.045×env0.5≈0.022 太轻（仅为收阳光音效的 1/3）
- **重做**：32 步循环 C-G-Am-F 和声进行；旋律层 triangle 523-1047Hz（中高频任何设备可放）vol 0.07 + 低音层 sine 根音每 4 步连音铺底 vol 0.11
- **教训：合成 BGM 的可闻性要按「总线衰减后 × 设备频响」标定，不能只看节点音量数值**
- 双门控 19/19 + 47/47；**等用户真机确认 BGM 可闻**
- 徽标是**临时的**（drawStatus 内，gt<20 显示），确认后移除：位置在 `drawStatus` 开头，grep「音频诊断徽标」
- 若 ♪ suspended 持续 → 自动播放策略拦截（预览 iframe 嫌疑最大）→ 建议双击本地文件玩
- 若 ♪ running 但无声 → 排查系统音量/输出设备；补播机制已保证不丢事件

### 新会话接手指南
1. 先读本文件全文（当日全部记录都在这里）
2. 测试基线：**烟雾 16/16**（`...node.exe tests/harness/run-smoke.js`）+ **总线 47/47**（`.../verify-bus.js`），git 干净，最新 commit `00051da`
3. 代码导航用 `docs/code-map.md`（改完代码重跑 `node tools/gen-code-map.mjs` 刷新行号）
4. 下一步路线：等音频确认 → 完成 Playtest Round 1（用户试玩 + 回填执行包）→ Step 5 回归 26 条 → Phase 7 发布
5. 遗留 P1：音效 B1-B4（种植落地/卡片就绪/阳光分层/僵尸死亡分层）、sirenLoop；团队成员需重建（session 已失效）

---

## 音频未就绪补播机制（13:48，commit 490d4e7，用户二次反馈「开局点阳光/种植仍无声」）
- 上一版预热+8ms 前瞻没解决 → 换根治方案：**tone/noise 不再把音效排进未 running 的上下文**（排进挂起上下文的事件会被丢弃）
- 机制：`scheduleOrDefer(fn)` 守卫——非 running 入 `audioQueue`（上限 32 防堆积），`loop()` 每帧 `flushAudioQueue()` 冲洗；`startGame` 清空队列防跨局爆音
- **临时诊断徽标**：drawStatus 右上角，开局前 20s 显示 `♪ running|suspended r=N q=M`（绿=正常/红=被挂起）。**等用户回报徽标内容后可移除**
  - ♪ running 但仍无声 → 排查音量/输出设备
  - ♪ suspended 持续 → 浏览器自动播放策略拦截（工作台预览 iframe 无 allow=autoplay 时会这样）→ 让用户双击本地文件玩
- SMOKE-016：suspended 起步 → 种植 2 次入队不丢弃 → 手动切 running + 跑 1 帧真 loop → 补播 2 个振荡器 → 后续直排。**基线 16/16**
- 坑：测试桩 fake ctx 的 `resume()` 别同步改 state，否则 defer 分支永远走不到（SMOKE-016 首跑就栽这）

## 用户报告的 2 个 bug 修复（13:28，commit 8203f58）
### Bug 1：开局种植没有音效
- **先排除逻辑层**：注入 FakeAudioContext 验证 —— 种植确实创建并 start 了振荡器，说明代码路径没问题
- **根因在浏览器音频层**：AudioContext 刚创建时音频线程未就绪，**会话首个音效被整段吞掉**（之后的音效正常）
- **修复三件套**：①`primeAudio(c)` 上下文创建即播 1 帧静音 buffer 预热管线 ②`tone/noise` 的 t0 加 **8ms 前瞻**（避免排到"刚好此刻"被丢帧）③`armAudioUnlock()` 任意首次 pointerdown/keydown 兜底 resume（带 removeEventListener 存在性守卫，兼容 sandbox）

### Bug 2：大波预警横幅倒计时结束后残留约 2s
- **根因（我自己引入的）**：上次加清场门槛时，把「横幅显示」和「等待刷怪时机」混在同一个 `warn.active` 里 —— 清场慢时横幅被迫延长
- **修复**：拆出 `warn.pending`。`active` 只管横幅（倒计时归零**立即隐藏**）；`pending` 负责等清场（或 25s 兜底）后刷怪。三个分支改成 `if(pending) / else if(active) / else if(canAdvance)` 互斥
- 顺带：预警时长 3s→**2s**（用户要求），toast 同步用 `WARN_TOTAL`
- **教训：显示状态与业务等待状态必须分开建模**，否则任何"等待条件"都会变成 UI 残留

### 测试与验证
- SMOKE-014（开局音效可听性：预热 + 种植/再种植/拒用/铲除均发声）、SMOKE-015（横幅生命周期：2s + 归零即隐 + pending 等清场 + 清空同帧刷怪），**基线 15/15**
- verify-bus 更新过时断言（createBuffer 3→4，并新增"预热启动 1 帧"断言）→ **47/47**
- 两条新用例均用 `git show HEAD:` 旧版本对照跑，**确认能抓出对应 bug**
- 代码地图已刷新（1751 行 / 58 函数）

## 代码地图工具化（13:17，commit 5825bc2，用户问「单文件迭代是否浪费 token」→ 拍板只做 A）
- **量化结论**：文件现 65 KB / 1,716 行 / 56 函数 / 39 常量 ≈ **1.9 万 token**
  - 定位式小改（Grep + 窗口读 30 行 + 精确 Edit）≈ **1.2k token，不随文件增长**
  - 全文读取 ≈ 19k（文件翻倍后 38k，占 200k 上下文 19%）
  - 今天真正的 token 大头是 **Step 3 委托成员跨全文件施工（6 特性，约 40-80k）**，不是微调
- **产出**：`tools/gen-code-map.mjs`（纯读，不改游戏文件）+ `docs/code-map.md`（11 分区 / 行号区间 / 逐区成员表 / 高频改动速查）
  - 行号为 HTML 绝对行号，可直接喂 `Read(offset,limit)`
  - 坑：分区区间不能用「下一条顶层声明」算，要用「下一分区标题前一行」
- **README 写入硬规则**：先查表定位 → 只读目标区块 → 精确编辑，禁止全文通读；改完重跑生成器刷新行号
- 用户拍板：不做 B（配置外置）/ C（全面拆分）；**等要加第三关/新植物时再考虑 C**（那时收益最大）

## Step 4 Playtest Round 1 启动（13:09，用户拍板走 Playtest 路线）
- **发现并修复 S1 静默缺陷（commit 4867902）**：`SFX.bigWaveImpact()` 被调用但 SFX 表未定义 → 每次大波真正刷怪那帧抛 TypeError，被 loop 整帧 try/catch 静默吞掉（玩家只见底部红条 5s + 丢一帧）。按 audio-guide §B7 补实现（80→45Hz sawtooth + 600Hz noise，event 总线）
- **SMOKE-013 新增**：①静态——所有 `SFX.<key>` 调用点必须有定义 ②运行——大波刷怪帧不抛异常。**支持 `PVZ_HTML_PATH` 环境变量覆盖源文件路径**（便于拿旧版本对照验证用例有效性）；基线 13/13
- **执行包产出**：`tests/playtests/round-1-execution-pack.md`——把今日 14 个 commit 的改动转成 V1-V8 专项验收点（震动/种植动画/死亡零件/爆炸五层/预警 3s/清场门槛/L2 平衡/UI 细节），每项含「怎么验 → 期望 → 异常长什么样」；任务表加 T9（专项点逐项过）；含 O1-O6 观察维度与 PASS/CONCERNS/FAIL 判定
- 修正 `tests/playtest-plan.md` 过期前提（localStorage 存档已存在 + `?level=2` 直进通道）
- **复用方法**：验证测试有效性 = 用 `git show HEAD:<file>` 导出旧版本 + 跑同一用例，确认能复现 bug（SMOKE-012/013 均如此验证过）

## 大波预警时长 4s→3s（13:02，commit 0e36962，用户要求）
- `WARN_TOTAL` 常量 4→3，`warn.t=4` 改用 `WARN_TOTAL`（原先硬编码，改时长要动两处）
- 进度条比例随常量自动同步；siren 音效总长 1.32s 仍在窗口内
- 双门控 12/12 + 46/46 全绿

## Step 3 手感七项落地完成（10:00-10:50，用户放行）

断网恢复后接力完成。团队 `pvz-lite-step3`（成员消息通道本环境不可用，spawn 直接回传，主理人代汇编）。

### 提交链（cddb53f → f22b6f4，共 9 个 commit，一项一 commit）
| commit | 内容 |
|---|---|
| c88e7ff | §8 Easing 缓动库（公共依赖） |
| 916ef2d | §1 F-04 屏幕震动（clamp 方案 + 4 触发点） |
| 0558271 | §2 F-01 种植动画 620ms 三段（**引入 S0 bug，见下**） |
| bccc739 | §3 F-02 死亡掉零件 spawnCorpseParts |
| d38c2b5 | §4 F-03 爆炸五层时间轴 + drawShockwave + drawBoom 三色段 |
| 19b86a5 | §5 F-06 大波预警增强（vignette + 倒计时数字 + 周期轻震） |
| d39b9fc | §6 D-11 圆角化 rr() helper + 三处替换 |
| 327fd24 | §P6.5-T5 SMOKE-010 暂停分支 + README 基线 10 条 |
| f22b6f4 | **收口修复 lastDt ReferenceError（S0）** |

改动规模：plants-vs-zombies.html +270/-36 行。

### 最终基线
- 烟雾 **10/10 PASS**、总线 **46/46 PASS**、工作树干净
- D-14 等宽字验收通过（数字全 Consolas，残留 5 处 sans-serif 均非数字，合规）

### S0 bug 复盘（重要，防复发）
- F-01 的 drawPlant 兜底 `lastDt`（蓝图 §2 提到"读全局 lastDt"），但**全脚本无定义**；drawGameWorld 调 drawPlant(p) 不传 dtRef → 种下第一棵植物后（plantT<620 动画窗口）render 每帧 ReferenceError，被 loop try/catch 吞掉 → **画面永久冻结但逻辑还在跑**（正是上次"偶现退出"的同类表现）
- 修复：`let lastDt=0` 声明 + loop 里 `lastT=now;lastDt=dt;`（两行收口）
- **教训：烟雾测试是逻辑层，不跑真实渲染路径，render 层 bug 要靠 SMOKE-010 这类"真 loop 帧 + console.error 监视网"才抓得到**——以后改 render 层必须带帧异常监视断言
- quality-lead 铁律起效：只记录不修，回传主理人验证后收口

### 成员偏离蓝图决策（已验收，合理）
1. F-01 旧对象兜底 plantT=620 直接落地（蓝图伪码置 0 会重播动画，与注释矛盾）
2. F-06 checkWave 两触发点未重复接线（§1 落地时已含）
3. D-11 冷却遮罩一并圆角化（不圆会露直角）+ 进度条填充宽度下限 5（arcTo 退化保护）

### 遗留（P1，未做）
- `SFX.death.cone/.bucket` 子音效（需 audio 侧先在总线注册，归阮和鸣）
- `SFX.sirenLoop`（4 秒循环 + 200ms 淡出）、`SFX.bigWaveImpact` 未实现

### 下一步选项
Playtest 手动验收（真机玩一遍七项手感）→ 回归 26 条 → Phase 7；或直接第三关/存档

## 用户手感反馈调整（11:24，commit c8c03b1）
- **结束画面去掉屏幕震动**：僵尸进屋失败（原 8px 特例）与通关（原 2px）两处触点移除；游戏内爆炸/大波预警震动保留

## 第二关直进通道 + SMOKE-011（12:27，commit 3641b62，用户要"单测第二关"）
- **直进方式**：URL 加 `?level=2` 免解锁直进第二关；另外 unlockedLevel 已持久化 localStorage(`pvz_unlocked`)（顺手把存档待办的第一块落了）
- **harness 扩展**：`game.setLevel(n)` / `game.setUnlocked(n)` / `sandbox.__LEVELS` 桥
- **SMOKE-011 平衡契约**：固化削峰数值——L2 startSun≥150、总量≤25、单波≤6、单波 fast≤2、interval≥5s、L1 5波13只不回归 + interval 下限行为学验证；基线升 **11/11**
- 坑：harness 加 __api 方法后**必须在导出区 bind**，否则 case 里 `g.setLevel is not a function`

## 波次推进清场门槛（12:49，commit 5498b8a / 3b58289，用户二次反馈"上一波还有好多就发下一波"）
**上一版 70acc34 只改了预警提前，没治根因**：波次推进只按 `gt-lastWaveT>=gap` 计时，而 `lastWaveT` 是**本波开始刷怪**的时刻 → 16s 一到，不管上一波剩几只活僵尸都开新波。
- **最终规则（checkWave）**：
  - 硬门槛：`!waveActive && zombies.length===0`（上一波队列放完 + 场上清空）才开新波
  - 最小喘息：清场快也要等够 minGap（首波 12s / 最后两波 20s / 其余 16s）
  - 兜底：`gt-waveDrainedT>=25` 仍未清场也放行 → 防「僵尸啃坚果 46s」卡死对局（坚果 dur3000 ÷ 65/s = 46s，这是必须留兜底的原因）
  - 大波预警：`已过 8s && 场上 ≤2 只` 触发；倒计时结束仍需清场才刷怪
- 新增字段 `waveDrainedT`（newWave 与队列放完时更新；startGame 重置）
- **SMOKE-012 新增**：清场门槛 4 组断言；用旧版文件对照跑**确认能抓出该 bug**（旧版失败点 `wave=2 但场上还有 1 只`）——测试有效性的验证方法值得复用
- 基线 12/12 + 46/46 全绿

## 大波预警提前（12:33，commit 70acc34，用户反馈"告警前干等上一波清场"）
- 原逻辑：大波预警挂在固定波间空档（16/20s）计时器上，上一波没清完时即使刷完队列也要干等
- 新逻辑：上一波**队列刷完（waveActive=false）且场上僵尸 ≤2 只**时，下一波是 big 就**不等空档直接触发预警**（`readyToSpawn` 加 `|| nextIsBig && zombies.length<=2` 分支）
- 预警结束仍等场上彻底清空才刷怪（`warn.t<=0` 分支不变），不会两波夹击；补了 toast 文案
- 双门控 11/11 + 46/46 全绿

## L2 难度削峰 + 全局节奏放宽（11:49，commit e42ff7e，用户拍板"温和削峰+波间空档放宽"）
用户反馈：L2 第 3 波起并发大量僵尸，体验难度过高。诊断三来源：startSun 100 太穷、interval 压到 4s/只、第 4 波大波 3 只 fast（速度 45 = 普通 2.8 倍无解）。
- **L2 削峰**：startSun 100→150（对齐 L1）；各波 interval +1s；第 3 波 4→3 只；第 4 波大波 fast 3→2（5→4 只）；终波 8→6 只。总量 28→25
- **全局放宽**（影响所有关卡）：最后两波波间空档 16→20s；刷怪间隔下限 1.5→2.5s（processSpawnQueue）；队列剩 >6 只时每放一只再 +0.5s
- 双门控 10/10 + 46/46 全绿；测试无 L2 配置硬编码（SMOKE-005 用 forceWaves 不读波次表）
- 至此蓝图 §1 的 8px 失败特例已不存在，全游戏震动上限回归 6px

## 阶段诊断 + Playtest Round 1 启动（16:15-16:32，主理人）
- 用户问「现在该走哪个阶段/下一个流程」→ 全量扫描产物后诊断：**Phase 6 打磨收尾段**（概念~制作全绿，打磨代码层完成、验证层未做）
- 用户三选一拍板：**开始 Playtest Round 1**（走 SOP 正路）
- 主理人向用户交付了 V1-V8/T1-T9 速查卡（内容源自 round-1-execution-pack.md，未额外落盘文件）
- **待用户回报**：试玩结果 + 开局音效/BGM 可闻性确认（两项 P0 前置）
- 下一步：用户回填 → 汇编 round-1-report.md + 质量门判定 → 回归 26 条 → Phase 7

## Playtest Round 1 PASS + Phase 7 启动（16:43-17:00，主理人）
### Round 1 判定 PASS（报告已落盘）
- 用户回报：**V1-V8 专项验收点全部通过** + T1/T4/T6 全通过 + 无卡死/无红条
- **两项音频待确认前置解除**：开局音效有声、BGM 可闻（解决 2026-09-16 上午两项挂起项）
- 产出 `tests/playtests/round-1-report.md`（含 V 表/T 表/O 表 + PASS 判定 + 缺口 + 下一步）
- 未单独回报项（T2/T3/T5/T7/T8、O2-O6）已在报告中如实标注，不留白不编造

### Step 5 重大发现：26 条 REG 回归从未实现
- `tests/harness/run-all.js` **不存在**；`cases/` 只有 SMOKE-001~019（.js）；第 3 节 26 条 REG-* 全是计划文档
- 文档口径三处不一致：tests/README 写「17 条」（实际 19）、regression-plan 写「≤10 条」、phase6-handoff 写「10 条」
- **用户拍板**：以现有门控（烟雾 19 + 总线 47）作发布门进 Phase 7；26 条 REG 实现列为 Phase 7 后待办
- 门控**现场实测**（不采信旧基线）：烟雾 **19/19 PASS**（62ms）、总线 **47/47 PASS**

### 阶段判定：Phase 6 完成 → Phase 7 发布评审开工
- 用户拍板：**发 v1.0.0**，P1 音频分层（B1-B4 + sirenLoop）列为已知问题留 v1.1
- 团队 `pvz-lite-phase7` 已建（主理人 TeamCreate）；任务板：T1 发布包（release-ops-lead，in progress）/ T3 最终 QA 签字（quality-lead，blockedBy T1）
- **PVZ-P7-001** 已派 release-ops-lead：产出 `production/release/v1.0.0/` 下 RELEASE-NOTES.md + RELEASE-CHECKLIST.md + KNOWN-ISSUES.md
- **关键经验**：本环境 Agent 工具带 `name`/`subagent_type` 调度专家成员时，**必须先 TeamCreate**，否则报 "No active team found"（此前 memory 记的「spawn 直接回传」需修正为「先建队再 spawn」）

## Phase 7 发布评审：QA 判定 CONCERNS + 用户决策（17:20-17:35）
### PVZ-P7-001 发布包交付（release-ops-lead）
- 产出 `production/release/v1.0.0/`：RELEASE-NOTES.md / RELEASE-CHECKLIST.md / KNOWN-ISSUES.md
- 变更日志基于真实 git 历史（36 提交 = 26 功能修复 + 10 归档），未编造；本地化结论：中文硬编码不影响发布
- 主理人独立核验：提交数 36 ✓、1843 行/71992 字节 ✓、SHA256 前缀 ✓；**抓到 2 处不一致**（G1 基线副本不存在 / G2 哈希栏空白但回传摘要称"已给出"）

### PVZ-P7-002 QA 独立复核（quality-lead，判定 **CONCERNS**）
- 记录：`tests/reports/qa-signoff-v1.0.0.md`；已在 RELEASE-CHECKLIST.md I 节 QA 栏签字
- 门控数字**全部复现**：烟雾 19/19（85ms，声明 63ms 属抖动）、总线 47/47、cases=19、run-all.js 不存在、SHA256 双工具复算一致
- B1-B6 转述**忠于** round-1 报告，无失真
- 新发现：R3 audio B5/B6/B8-B10 未入清单、R4 未跑性能基准未列、R5 README 待办#3 余项未列、R6 **发布文档未被 git 跟踪且无 v1.0.0 标签**、R7 E1-E9 全未执行
- **必闭合（不可豁免）**：B-1 固化基线副本、B-2 哈希填入 G2/D8 —— 否则回滚方案不成立
- 判定逻辑：无技术阻塞 + 硬门槛全过 + 仅有待用户签字项 → CONCERNS

### PVZ-P7-003 补做已派（release-ops-lead）：B-1 基线副本 + B-2 哈希回填 + B-3 已知问题补漏（R3-R6）

### 用户三项决策（2026-09-16 17:35）
1. **E1-E9 运行验证**：**部分豁免** —— 以 Round 1 真机结果替代（两关实测 + 音效可闻 + 零红条），残留风险 = Edge 未单独验证，书面接受
2. **git 纳管**：**打 v1.0.0 标签 + 提交**发布文档与 Playtest 报告（此前发布资料全未被跟踪）
3. **C7 已知问题**：**全部确认豁免** —— C4 文档口径不一致（README 17 vs 实际 19）、C5 未跑 Round 2/3、C6 无内嵌版本号，全部书面接受，C4 并入 v1.1 文档同步

### 下一步
P7-003 落地后：① 检查清单 B7/C7/F4/E1-E9 豁免项闭合 ② `git tag -a v1.0.0` + 提交发布包 ③ 汇总发布总结给用户

## Phase 7 完成：v1.0.0 封存（17:40-17:55）
### 版本封存事实
- commit **`f6c81a5`**（f6c81a52f5af6338f868fe468b43cbcce52182e2）= tag **`v1.0.0`**（annotated，注解「PvZ Lite v1.0.0」）；**仅本地未推送**
- 提交 8 文件全为文档/报告/归档副本，**`plants-vs-zombies.html` 未入提交、零改动**
- 产物 SHA256 `713a9441b6ddf037346d857bc0a401c15f76de8419e58d5886cdd435b1030deb`；母本与 `artifacts/plants-vs-zombies.v1.0.0.html` 逐字一致
- 门控复核：烟雾 19/19 PASS（91ms）、总线 47/47 PASS

### 主理人独立核验抓到的成员不一致（4 条，均已修复）
1. G1 基线副本宣称固化但磁盘无文件（主理人发现）
2. G2 哈希栏空白、回传摘要却称「已给出」（主理人发现，QA 复现）
3. G2 状态标记不自洽（内容已填但仍标 ⬜）
4. KNOWN-ISSUES 覆盖面不足（QA 发现 R3-R6：音频蓝图实为 B1–B10、未跑性能基准、README 待办余项、发布资料未纳管）

### 坑（重要，可迁移）
- **annotated tag 用 `git rev-parse <tag>` 取到的是标签对象 SHA，不是提交 SHA**；要比对标签指向必须用 `git rev-parse <tag>^{commit}`。本次因此一度误判「tag 未指向 HEAD」

### 交付
- 产出 `production/phase7-handoff.md`（主理人汇编：版本事实 / 产物清单 / 质量门判定链 / 跨成员一致性 / 已知风险与缓解 / 用户待办 / v1.1 候选）
- 发布包 `production/release/v1.0.0/`：RELEASE-NOTES / RELEASE-CHECKLIST（H 节 GO、I 节用户栏留空待签）/ KNOWN-ISSUES（10 条）/ artifacts/ 基线副本

### 用户待办
1. 签 RELEASE-CHECKLIST I 节用户审批栏（QA 栏、发布人栏已签，用户栏未代签）
2. 决定分发动作（默认单文件直接分发）
3. 建议推送提交与标签到远端（现仅本地）
4. 可选补跑 E5（Edge）把「接受未验证风险」转为已验证

### v1.1 候选
P1 音频分层（B1-B6/B8-B10 + sirenLoop）→ 补 26 条 REG 回归（含修 tests/README 17→19、regression-plan ≤10 口径）→ Playtest Round 2/3 → 第三关/新植物僵尸 → 存档余项 → 性能基准

## v1.1 规划 + Sprint 1 立项（18:00-18:20）
### 规划产出
- 产出 `production/v1.1-plan.md`：四冲刺（S1 护栏与收尾 / S2 音频补齐 / S3 内容扩展 / S4 验证与发布）+ 三档范围（A 轻量 / B 标准 / C 完整）+ 依赖图 + 风险表
- **用户拍板：方案 B · 标准功能**（S1+S2+S3 第三关+S4），且**立即开 S1**

### 规划期新发现的文档不一致（重要）
- `tests/regression-plan.md` 标题写「共 26 条 REG」，但第 3 节**实际枚举 30 条**（TRAP 6 + STATE 3 + CARD 4 + WAVE 4 + PLANT 4 + ZOM 3 + SUN 2 + MINE 2 + END 2 = 30）。已告知工程成员以枚举为准实现 30 条，数字由 V11-02 修正

### 团队 pvz-lite-v11 已建，S1 五条任务
| Task | 内容 | 负责人 | 依赖 | 状态 |
|---|---|---|---|---|
| V11-01 | 实现 30 条 REG + run-all.js（含 TRAP-04 热区 / TRAP-06 快捷键，闭合 #3） | engineering-lead | 无 | 🔄 进行中 |
| V11-02 | 测试文档口径同步（17→19、≤10→19、26→30） | quality-lead | V11-01 | ⏸ 阻塞 |
| V11-03 | README/phase6 数值同步（L2 阳光 100→150、预警 4s→2s、行数改由代码地图承载） | release-ops-lead | 无 | 🔄 进行中 |
| V11-04 | 静音 + 最高分持久化（try/catch 兜底，闭合 #9） | engineering-lead | V11-01 | ⏸ 阻塞 |
| V11-05 | 内嵌版本号（页面角落 + console，闭合 #6） | engineering-lead | V11-04 | ⏸ 阻塞 |

### 本轮并发策略与决策
- 并行：engineering-lead（tests/harness/ + 源码）∥ release-ops-lead（README + phase6-handoff，文件不重叠）
- **提交策略：成员只改不提交**（无用户指令不提交代码）；S1 门控通过后统一请用户授权提交
- 对 phase6-handoff.md 的行数漂移，明确要求**加历史标注而非改写历史**（保持交付记录诚实性）
- 已提醒 engineering-lead：V11-05 涉及 render 层（drawMenu/drawStatus），render bug 逻辑层烟雾抓不到，必须验真 loop 帧无异常

### 下一步
S1 门控（run-all 全绿 + 烟雾 19 + 总线 47 不回归）→ 派 V11-02 → 请用户授权提交 → 开 S2（音频 B1-B10）

## V11-03 完成 + 一处数字纠错（18:35）
- 路远行首轮「完成」但**零文件改动**（自述与产物不符），主理人用 `git diff --stat` 打回后重做，已验证落笔
- README 修正（12 行）：行数不写死（改由 code-map 承载）×2 处、L2 初始阳光 100→150、L2 僵尸总数 28→**24**、大波预警 4 秒→2 秒 ×2 处
- phase6-handoff.md（4 行）：**加历史快照说明 + 标〔当时值〕，未改写历史数值** —— 处理方式正确
- 他**正确驳回了主理人的一条错误指令**：主理人说「README 变更记录章节写 4 秒」，实际变更记录章节只列「大波预警」无时长；两处 4 秒实为 L60 内容一览与 L93 架构要点。已按实际位置改

### ★ 数字纠错（重要，已独立验证）
- **第二关僵尸总数是 24，不是 25**。按源码 `LEVELS[2].waves` 逐波求和：2+3+4+4+5+6 = **24**（主理人已用源码独立复算确认）
- 错误来源：memory 2026-09-16「削峰：总量 28→25」是**算术笔误**；实际削减 4 只（第3波-1、第4波-1、终波-2）
- **影响**：v1.0.0 已冻结文档（KNOWN-ISSUES #7 等）仍写「28→25」——冻结产物不改写，**在 v1.1 做数字更正**（列入 V11-02 文档同步范围）
- 教训：**削峰/调整类摘要里的「总量」必须按 `LEVELS[].waves` 逐波求和核对，不能信摘要**

### 当前状态
- V11-03 ✅ 完成并验证｜V11-01 🔄 进行中（engineering-lead）｜V11-02/V11-04/V11-05 ⏸ 阻塞

## V11-01/04/05 完成 + 主理人变异测试验证（18:50）
### 交付（engineering-lead）
- **V11-01**：`tests/harness/cases/REG-*.js` **30 条**（TRAP6/STATE3/CARD4/WAVE4/PLANT4/ZOM3/SUN2/MINE2/END2）+ `run-all.js`（默认跑 REG；`--all` 51 条；`--smoke`）+ `index.js` 扩 32 行（新增 `__VERSION`/`__SFX`/`__consts` 桥、`lastWaveT`/`exitArm`/`waveActive`/`muted`/`highScore` 等探针、`localStorage`/`location` 桩）
- **V11-04**：源码 +44/-5；`storageGet/storageSet` 全 try/catch；`muted←pvz_muted`、`highScore←pvz_highscore`；`updateBest` 于 setState('end') 落盘；菜单/HUD/结算三处展示最高分；新增 SMOKE-020
- **V11-05**：`const VERSION='v1.1.0-dev'` + 启动 console.log + drawMenu 右下角小字；新增 SMOKE-021（含真 loop 帧 console.error 监视网）
- `docs/code-map.md` 重生成：**1883 行 / 68 函数 / 43 常量**

### 门控（主理人独立复跑，全部复现）
- `run-all` **30/30 PASS**（64ms）｜`run-smoke` **21/21 PASS**（88ms）｜`verify-bus` **47/47 PASS**

### ★ 主理人自己做变异测试（最强验证，值得复用）
自造错误版本 → 用 `PVZ_HTML_PATH=<mutant> node tests/harness/run-all.js` 跑：
- L537 `cx=CARD_X0+i*CARD_W` → `170+i*CARD_W` ⇒ **REG-TRAP-04 FAIL**（29/30）✅ 能抓 bug
- L487 `window.addEventListener('keydown',onKey)` → `canvas.addEventListener` ⇒ **REG-TRAP-06 FAIL**（29/30）✅ 能抓 bug
**方法可复用**：cp 源文件到临时目录 → sed 单点变异 → PVZ_HTML_PATH 指向变异体跑门控 → 确认对应用例精确失败 → 清理

### ★ 地瓜 nearCell 死代码（工程发现 + 主理人复核）
- L848-850：`sameCell = z.row===p.row && dx<CELL_W*0.6`（同排 dx<54）；`nearCell = dx<CELL_W*0.5 && dy<CELL_H*0.9`（dx<45 且 dy<93.6）
- **相邻行 dy=104 > 93.6 ⇒ 永不满足 nearCell；同排 dx<45 ⊂ dx<54 ⇒ nearCell 无额外贡献 = 死代码**
- 即地瓜爆炸**只伤同排 ±54px**，注释「轻微波及相邻行」与 regression-plan §3.8「邻近行 0.9 格内伤害」均不成立
- **用户决策（2026-09-16）：保持现状，改注释与文档**（v1.0.0 已按此行为发布；改 AOE 会动平衡）。由 REG-MINE-02 锁定真实契约；「波及相邻行」登记为 **v1.2 候选**，已写入 `production/v1.1-plan.md` 第六/七节

### 用户决策
- 提交策略：**等 V11-02 完成后一次性提交，每个任务一个 commit**（V11-04/05 同改一文件，可能合并或按 hunk 拆）

### 当前 S1 状态
V11-01 ✅｜V11-03 ✅｜V11-04 ✅｜V11-05 ✅（含注释收尾）｜V11-02 🔄（quality-lead 文档口径同步中）

## 地瓜注释校准（19:05，engineering-lead）
- L840 注释重写为准确表述（仅同排 ±54px）+ 三行范围说明 + L853 行尾保留提示；**判定逻辑一行未动**（主理人已 diff 核对：`sameCell`/`nearCell`/`if(sameCell||nearCell)` 表达式与改前完全一致）
- 门控复跑：run-all 30/30（65ms）· run-smoke 21/21（77ms）· verify-bus 47/47；REG-MINE-02 仍 PASS
- code-map 刷新：1887 行 / 68 函数 / 43 常量
- **踩坑提示**：`wc -l` 报 1886、code-map 报 1887 —— 差 1 是因为**文件末行无换行符**（`wc -l` 数的是换行符个数）。引用行数时统一以 code-map 为准，避免又造出一处"文档漂移"
- engineering-lead 报告其会话中段 Bash stdout 捕获失效（连 echo 无输出），改用「重定向到文件 + Read」读取结果 —— 该问题不影响交付物

## V11-02 完成 + 两处新问题（19:20）
### V11-02 主体交付（quality-lead）
- `tests/README.md`：烟雾 17→21、26→REG 30（合计 51）、`cases/*.md`→`harness/cases/*.js`、补 SMOKE-018~021 与 REG-* / run-all.js 条目、发布流程改「烟雾 21 + REG 30 + 总线 47」三件套
- `tests/regression-plan.md`：§1 改已实现结构、§2 ≤10→21、§3 26→30、**§3.4 预警 4s→2s**、§3.8 MINE 改真实契约、§5 H1/H3/H5 标已落地、§5.4 加「状态」列、§6 10→21
- `design/audio-guide.md §D.4`：键名 `pvz.muted`→`pvz_muted` 并标已落地

### ★ 主理人抓到的两处新问题（已派回修）
1. **quality-lead 在 `tests/README.md` L128 写死了错误行数「1882 行 / 68 函数 / 43 常量」**
   - 实测 `wc -l` = **1886**、`awk NR` = **1886**；1882 是错的
   - 更严重的是**原则问题**：V11-03 刚确立「行数不写死，以 code-map 为准」，它又写死一个 → 已要求改为不写死
2. **`tools/gen-code-map.mjs` 有 off-by-one bug**（根因已定位）
   - `:23 raw.split(/\r?\n/)` + 文件末尾带 `\n` → 尾部多一个空串元素；`:91` 用 `lines.length` 报总数 → **多算 1 行**
   - 证据：wc=1886 / awk=1886 / code-map=**1887**
   - 派工程侧**只修总数那一处**，警告勿动 split 策略（否则中间空行被吃掉 → 全表行号错位）
3. regression-plan §3.8 把地瓜写成「待决项」——实际已裁决且源码注释已校准 → 已要求改为「已裁决（保留现状，仅同排 ±54px）」

### 教训
- **主理人不可只做"传话"**：成员两次在「完成」后仍引入新漂移（假完成 / 写死错误数字），必须逐项核数
- 行数三方对齐口径：`wc -l` = `awk NR` = code-map（修 bug 后应统一为 1886）

## ★ S1 收尾提交完成（19:40）· v1.1 Sprint 1 DONE
### 提交（7 个 commit，工作树干净）
| commit | 内容 |
|---|---|
| `ca8beb2` | test(V11-01) 30 条 REG + run-all.js + index.js 探针扩展 |
| `e4f6803` | feat(V11-04,V11-05) 存档持久化 + 内嵌版本号 + 地瓜注释校准（同文件故合并） |
| `cee9c4c` | fix(tools) gen-code-map 行数 off-by-one + 重生成代码地图 |
| `67cbb6b` | docs(V11-03) README / phase6 报告数值同步 |
| `78fc532` | docs(V11-02) 测试文档与音频规格口径同步 |
| `de3362b` | docs Phase 7 交接报告 + v1.1 规划 + 会话记忆 |
| `6e1fdc4` | docs v1.1 规划补记 S1 完成状态与提交记录 |

### 最终门控（主理人独立复跑）
- `run-all` **30/30**（`--all` 51/51）· `run-smoke` **21/21** · `verify-bus` **47/47**
- 变异测试 A/B 均精确 FAIL → REG-TRAP-04/06 有效
- 行数三方对齐 **1886**（wc / awk / code-map）

### 关键校验：v1.0.0 冻结产物未被污染
- `production/release/v1.0.0/artifacts/plants-vs-zombies.v1.0.0.html` 哈希仍为 `713a9441b6dd…b1030deb`，最后一次变更提交仍是 `f6c81a5` ✅
- 源码已演进（V11-04/05 后 1886 行），**基线副本保持 v1.0.0 原样** —— 这正是回滚方案的设计意图

### S1 闭环的 KNOWN-ISSUES
#3（陷阱无护栏）✅ · #4（测试文档口径）✅ · #6（无版本号）✅ · #7（README 漂移）✅ · #9（存档余项）✅ · #2（26 条 REG 未实现）✅ 已实现 **30 条**

### 下一步：S2 音频补齐（B1-B10 + sirenLoop，规格已在 design/audio-guide.md 备好）
