# SMOKE-007/008/009 修复记录（2026-09-15）

## 根因
1. **SMOKE-007**：用例读 `sb.plants`（harness sandbox 顶层 `zombies`/`plants` 没挂 globalThis，`sb.zombies` 是 undefined），改 `dur` 不生效 → 实得 1。
2. **SMOKE-008**：读 `sb.zombies.push(null)` 报错；且用 `g.step()` 而非 `g.__stepFrame()`。
3. **SMOKE-009**：读 `sb.__updateRaw`（host 侧不存在），改 `g.__updateRaw`。
4. **verify-bus.js**：读 `g.sandbox.__noise`/`g.sandbox.__routeBus`（应为 `g.__noise`/`g.__routeBus`），且 `sb.zombies.push(null)` 改 `g.pushNullZombie()`。

## 修复
- SMOKE-007：`sb.zombies` → `g.probe().zombiesArr`/`g.sandbox` 取 `plants`（加 `pushNullZombie` 风格：harness 已挂 `plants` 探针）
- SMOKE-008：`sb.zombies.push(null)` → `g.pushNullZombie()`；`g.step()` → `g.__stepFrame()`
- SMOKE-009：`sb.__updateRaw` → `g.__updateRaw`
- verify-bus.js：`g.sandbox.__noise` → `g.__noise`；`g.sandbox.__routeBus` → `g.__routeBus`；`sb.zombies.push(null)` → `g.pushNullZombie()`

## 状态
- 待跑 `node tests/harness/run-smoke.js` + `node tests/harness/verify-bus.js` 确认全绿
