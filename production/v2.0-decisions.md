# v2.0.0 用户决策记录（v2.0-decisions）

> 对应规划稿 `v2.0-plan.md` §9 / Q-1～Q-13 + 补充项 Q-6a/Q-6b。
> **全部拍板于 2026-09-23（三批），规划稿已定稿。**

## 第一批（Q-3 / Q-6 / Q-7 / Q-8）

### Q-3 占位关点击反馈 → **A. toast 提示**
- 占位关点击 → toast「该关卡即将开放」+ 轻音效（复用 SFX）。
- 备注：用户原话「点击可以没有响应」，经提示「完全无响应易被误当 bug」后选 toast。

### Q-6 昼夜是否影响玩法 → **A. 纯视觉变体 + 泳池特殊三段（用户补充）**
- 昼夜为纯视觉（出怪/阳光数值零差异）。
- **泳池世界特殊结构（用户新增，改写通用规则）**：2-1～2-2 白天；2-3～2-5 普通黑夜；2-6～2-10 **浓雾黑夜**（白色浓雾遮罩战场，仅可见最左列前半列 + 最右列半列，原版 PvZ 雾关视野效果）。
- 落点：`time` 字段值域 `'day'|'night'|'fog'`。

### Q-7 墓地/房屋机制 → **B. 墓地加墓碑机制**（用户主动升级）
- 墓地世界要有**墓碑机制**（原版向：墓碑占格/出怪点）。世界 3 本期全占位 ⇒ 本期仅做数据字段预留 + 墓碑视觉装饰，**实装随世界 3 关卡开放（v2.x）**。
- 房屋世界：复用屋顶斜坡 + 花盆税换皮。

### Q-8 选关界面形态 → **A. 分世界页签**
- 4 页签切换，每页 1 世界 × 10 格。

## 第二批（Q-1 / Q-2 / Q-4 / Q-5）

### Q-1 主菜单视觉 → **A. 纯 Canvas 静态**
- 标题大字 + 副标语 + 「进入游戏」按钮，静态为主（~60 行）；视觉打磨留后续版本。

### Q-2 旧存档迁移 → **A. 映射为世界 1**
- 旧 `unlockedLevel=N` → 已通 `'1-1'..'1-(N-1)'`、解锁 `'1-N'`；旧存档键保留可回滚，迁移幂等。

### Q-4 每关植物奖励顺序 → **C. 地形卡对位 + 强度递增**
- 世界 2 首关发 lilypad、世界 4 首关发 planter（地形卡对位）；其余按强度递增铺世界 1（1-1 double → 1-6 icemelon，承旧序列）；超出现有 12 张卡池的 28 格标 `PLACEHOLDER` 跳过不发、不发补偿。

### Q-5 世界全通金币 → **A. 300/世界**
- 沿用现 per10 数额语义平移；总增量 1200 与现口径等额，经济面零冲击。

## 第三批（Q-9 / Q-10 / Q-11 / Q-6b）

### Q-9 旧第 5 关（屋顶）处置 → **A. 整体迁入世界 4**
- 波次作 `'4-1'` 锚点；花盆税/斜坡机制成为房屋世界标配；v1.3 资产全复用。

### Q-10 占位关显示样式 → **A. 40 格全部可见**
- 可玩关亮色全彩；占位关灰调 + 「敬请期待」小字；世界结构一览无余。

### Q-11 旧 L2 黄昏 / L3 月夜视觉 → **A. dusk 退役，昼夜二分**
- dusk 滤镜退役；旧 L2 归白天变体（波次作 1-2 基底）、旧 L3 的 night 归世界 1 夜半区标准夜（波次作 1-6 基底）。

### Q-6b 浓雾实装时机 → **本期实装（M4）**
- M4 实装白色浓雾遮罩层（仅见最左列前半列 + 最右列半列）；测试模式可直跳 `?level=2-6` 做视觉验收，`time:'fog'` 端到端验证。

## 自动锁定项（依据用户原话/前置拍板推导，未单独提问）

### Q-12 本期可玩范围 → **A. 仅 1-1 可玩**
- 依据用户原话「跑通 1-1，1-2 占位」；1-2..1-10 与 2/3/4 世界同样占位。

### Q-6a 草/墓/房昼夜结构 → **维持前 5 昼后 5 夜**
- 依据用户原话「后面同理」；仅泳池特殊三段。

### Q-13 per10 金币计数器 → **A. 改语义 worldClear**
- 依据 Q-5A 拍板（沿用 per10 数额语义平移）；双轨并行会导致第 10 关重复发钱，不采纳。

## 第四批（Q-14 / Q-15 / Q-16 · T-101 盘点衍生，2026-09-23 拍板）

> 来源：T-101 旧验收脚本依赖盘点（`tests/playtests/v20-impact-inventory.md`）暴露的三个 v2 格式未定项。

### Q-14 存档复合键 `pvz_diff_clears` 格式 → **A. `hard:1-6` 同构拼接**
- 维持 `'难度:关卡键'` 单字符串复合键结构（`hard:3` → `hard:1-6`，依锚点映射）。
- 旧档迁移规则：`hard:N`（N∈1..5）→ `hard:<锚点键>`（L3→'1-6' / L4→'2-1' / L5→'4-1'）；`pvz_unlocked` 迁移同 Q-2 既有规则。迁移单向幂等，旧键保留。

### Q-15 REG-END-02 末位关锚点 → **A. 改锚 `'4-10'`**
- 「通关最后一关无下一关」用例从 L5（'4-1' 非末位，前提崩塌）改锚新体系真正末位 `'4-10'`。
- '4-10' 波次由 T-102 `materializeLevels()` 确定性模板展开自动落表，无供表时序阻塞。

### Q-16 解锁进度 `pvz_unlocked` 形态 → **A. 最高解锁键（字符串键名）**
- 解锁进度存字符串键名（如 `'1-3'`），数值比较 `unlockedLevel>=N` 改为按 `LEVEL_INDEX` 序的键序比较。
- 旧档 `unlockedLevel=N` → 最高解锁键 `'1-N'`（与 Q-2 映射一致）；T-104 harness 兼容层：`setUnlocked` 接受键名 + 数字 1..5 shim，probe 双暴露 `levelKey`（主）/ `levelNo`（派生序号，过渡）。

## 第五批（Q-17 / Q-18 · T-203 施工前设计缺口，2026-09-24 拍板）

> 来源：T-203 主菜单重绘前的旧菜单功能盘点——难度选择与选卡环节在 v2.0 流程中的位置 plan 未决议。

### Q-17 难度选择（普通/困难/地狱）入口 → **A. 选关页顶部**
- 选关页页签下方放 3 态切换钮，进对局前最后一步设置；主菜单保持纯静态（符合 Q-1=A）。
- 旧菜单难度行（y=348 三钮）随 T-203 菜单重绘退役；DIFF 全局变量与 DIFFS/DIFF_AWARD 表不动。

### Q-18 选卡界面（state='deck'）流程位置 → **A. 点关后先进选卡**
- v2.0 流程定为：menu → select → 点击具体关卡 → deck 选卡 → 开局。
- 保留现行选卡体验与默认卡组逻辑（向日葵/豌豆/坚果，可不满 ≥1 张）；改动最小，T-205 命中链路按此实现。
