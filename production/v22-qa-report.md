# v2.2 · QA 判定报告（V22-QA-03 · D4）

> **署名**：严守真（quality-lead）｜**日期**：2026-09-25｜**任务编号**：V22-QA-03（收口）
> **被测**：`plants-vs-zombies.html` v2.2.0（工作树，4170 行；含 V22-FIX-01 修复补丁，工作树 diff `+67 / −3`）
> **评审强度**：**lean** —— 只做 D3（平衡画像）+ D4（本判定），不做无关回归、不扩充范围
> **性质**：质量门是**建议性门控（advisory）**——本文件给判定，最终放行由用户决定。

---

## 0 判定

# 🟢 **PASS（建议放行，进入下一刀内容）**

| 维度 | 结果 |
|---|---|
| **门控证据** | REG **62/62** · SMOKE **29/29** · BUS **56/56** · 真机视觉 **7/7** · **全部绿** |
| **机制级回归** | REG-SQUASH-01 / REG-PEPPER-01 / REG-CHERRY-01 三条新用例全过（含旧源判别力） |
| **本轮新增·平衡画像** | **V22-BAL PASS** · 480 局 · 结论「航椒/樱桃不过强 → **维持现状**」 |
| **本轮缺陷** | V22-FIX-01 三个缺陷 **100% 已闭环**（有复验证据） |
| **新增阻塞项** | **无** |
| **待办（非阻塞）** | GDD 文档同步 **4 条**（交策划侧修订）+ 高难度平衡复核建议 1 条 |
| **遗留风险** | 全部为**继承项**，无本轮新增 |

**为什么是 PASS 而不是 CONCERNS**：功能门控四门全绿、三个缺陷全部闭环且有复验证据、平衡画像以 480 局实测数据否定了 GDD 的两条中风险。唯一的「文档与实现不一致」（GDD §3.5 的 `triggerShake(8,400)` vs 实现 `6`）**不影响任何运行时行为**（实现侧已按主理人裁决定稿，且真机已验证首/次均 6px 一致），按派单要求**只登记、转交策划侧修订**，不构成阻塞。

---

## 1 本轮 QA 覆盖总览

### 1.1 三条机制级 REG 用例（各覆盖哪些契约）

| 用例 | 覆盖契约 | 子场景 |
|---|---|---|
| **REG-SQUASH-01**（窝瓜） | ① 目标选择 = 同排前方最近（x 最小）；② 跳跃时长 **0.6s** 抛物线（落地前不结算）；③ 目标中途死亡守卫（落地时 `dead` 则不结算，窝瓜仍消失）；④ 无前方僵尸 → 待机（不跳、不掉耐久）；⑤ 落地**单体**秒杀；⑥ 一次性 `_dying` 后移除 | S1–S4 |
| **REG-PEPPER-01**（航椒） | ① **armT=0 即种即生效**（种下当帧即可触发）；② 触发判据**二分实测锁定 49.5px**；③ 引爆范围 = **同排全部**（不限数量/不限列，含身后与他列）；④ **无差别秒杀**（铁桶 560 / hard×1.35 / expert×1.80 一碰即死，不参与耐久结算）；⑤ 他排不受影响（跨行=否）；⑥ 一次性 | S1–S5 |
| **REG-CHERRY-01**（樱桃） | ① **3×3 格判定 `dc≤1 && dr≤1` + 跨行秒杀**（本作首个跨行秒杀）；② 触发**只认同排**（相邻行同列不引爆，但被引爆后会被命中）；③ 棋盘边缘 col=0/row=0（左上）自然截断；④ 边缘 col=8/row=4（右下）截断；⑤ 一次性 | S1–S5 |

> **三用例均带旧源判别力**：在 v2.1.0（`a3ca5f6`）上复跑**必红**（无卡片 ⇒ 走注入兜底 ⇒ `updatePlant` 无对应分支 ⇒ 永不引爆）。红点落在**机制**而非「卡片不存在」，可证缺陷真实存在、避免「修了不存在的 bug」。
> **口径留痕**：REG-PEPPER-01 实测锁定触发阈值 **49.5px**（`CELL_W*0.55`），与 GDD §2.3 标注的「≈54px」不符 —— 详见 §3 文档待同步项 **DS-2**。

### 1.2 真机视觉 7 判据（`tests/playtests/v22-newplant-visual.js`）

| # | 判据 | 关键读数 | 结果 |
|---|---|---|---|
| 1 | **VIS-SQUASH-01** 窝瓜跳跃三阶段 + 抛物线几何 | 质心 x 单调 218.9→489.1（期望 190→490）；y 抛物线顶点 idx=2（升 79.1px / 落 115.6px）；压缩帧缩放比 0.57（<0.85）；噪声基线 n=0 | PASS |
| 2 | **VIS-HPBAR-01** 一次性植物不画血条 | 对照 pea/nut black=96（有血条）；mine/squash/pepper/cherry **均 0** | PASS |
| 3 | **VIS-PEPPER-01** 航椒全排火焰波 | 同排 4 只（含铁桶 560）清零；他排 row3 存活 1（跨行=否）；火焰粒子 30 枚；双向 vx>0=12 / vx<0=18；冲击波 rStart→rEnd 10→200；**爆炸帧 render 无异常** | PASS |
| 4 | **VIS-CHERRY-01** 樱桃 3×3 冲击波 + 跨行秒杀 | 3×3 内 9 只清零；幸存恰为 dc=2 / dr=2 四只；**跨行秒杀成立**（row1 与 row3 被清）；冲击波 8→135 | PASS |
| 5 | **VIS-CARD-01** 三新卡面专属美术 | art 区（80×36 纯美术区）squash/pepper/cherry **互异 hash = 3/3** | PASS |
| 6 | **VIS-BOOM-01** 爆炸特效渲染零帧异常 | 真机主循环（RAF 实跑 1.2s）逐类型读 `frameErr`：**mine / pepper / cherry / squash 全为「无异常」· frameErrT=0** | PASS |
| 7 | **VIS-SHAKE-01** 樱桃震屏强度一致性 | 首次 **6** · 第二次 **6** · 地雷 6 · 航椒 6；`loseShakeUsed=false`（8px 名额未被吃掉） | PASS |

**7/7 子项 PASS，总判定 PASS。**

### 1.3 本次平衡画像（D3）

`production/v22-balance-profile.md` —— 四臂 × 20 关 × 6 种子 = **480 局**，`V22-BAL PASS`。三道校准门（G0 复现 / G1 通关 / G2 生效）全过。核心读数：

- **20 关聚合失守率：基线 3/120（2.50%）→ 压测臂 3/120（2.50%），差 0.00pp**
- **加权通关时长：331.18s → 330.43s，差 −0.75s（−0.23%）**
- **对照臂（v2.2 不用新卡）与基线逐关完全相同** ⇒ v2.2 无附带难度漂移
- ⇒ **航椒 / 樱桃均不过强，不触发 GDD §7.1 缓解抓手，维持现状**

---

## 2 缺陷登记表（V22-FIX-01 · 三缺陷全部「已闭环」）

| ID | 级别 | 缺陷 | 根因 | 修复 | 修复后复验证据 | 状态 |
|---|---|---|---|---|---|---|
| **BUG-01** | **Major** | 三新植物卡面无专属绘制：squash/pepper/cherry 卡面像素雷同，只能靠卡名区分 | `drawCardFace`（源码 L3706 起）的 `if/else` 链**终止于 cabbage**，三新植物（及同缺口的 `snowpea`）落到**公共绿底兜底** | 补 4 个专属分支（squash / pepper / cherry / snowpea），与 `drawPlantInner` 本体同构等比缩小（源码 **L3826–L3883**） | 真机 `VIS-CARD-01`：art 区（80×36）三卡 **互异 hash 2/3 → 3/3**；`nz/colors` 三值互异（2872/2880/2880 · 112/177/217） | ✅ **已闭环** |
| **BUG-02** | **Minor** | 樱桃 `triggerShake(8,400)` 吃掉「失败进屋特写」的一次性 8px 名额，之后所有震屏永久退化 6px | `triggerShake` 内部 `if(intensity===8 && !loseShakeUsed){loseShakeUsed=true}` —— 樱桃抢先消费了为失败特写保留的一次性名额，导致首爆 8px、此后樱桃与航椒（请求 7）恒被 clamp 到 6px，手感不一致 | 按主理人裁决改为 **`triggerShake(6,400)`**（源码 L2126），8px 名额保留给失败进屋特写 | 真机 `VIS-SHAKE-01`：首次 **6** · 第二次 **6** · 地雷 6 · 航椒 6，**首/次一致**，`loseShakeUsed=false` | ✅ **已闭环** |
| **BUG-03** | 待定 → **Major（渲染中断）** | 航椒爆炸时 `createRadialGradient` 抛 `IndexSizeError`，帧渲染中断 + 屏幕底部红条 | `drawBoom`（源码 L3589）用**固定分母 0.45** 归一 `e.life`，且 `k=Math.max(0, life/0.45)` **只夹下界**：航椒 `life=0.5` ⇒ `k=1.111` ⇒ `tn=−0.111` ⇒ `easeOutCubic` 返回负 ⇒ **半径 r<0** ⇒ 抛异常 | **双侧夹紧** `k=Math.max(0, Math.min(1, e.life/0.45))`，并给 `r` 加 `Math.max(0, ·)` 二次兜底 | 真机 `VIS-BOOM-01`：主循环四类爆炸（mine / pepper / cherry / squash）**frameErr 全为 null**，`frameErrT=0` | ✅ **已闭环** |

**工程侧改动面**：`plants-vs-zombies.html` 工作树 diff **`+67 / −3`**（纯插补 + 一处常量 `8→6` + 一处夹紧式），机制代码零改动，无删除逻辑分支。
**回归影响**：对照臂（v2.2 × 不用新卡）与 v2.1.1 基线**逐关逐值相同**（失守率差 0.00pp、加权时长差 0.00s）⇒ 三处修复对玩法数值**零影响**。

---

## 3 文档待同步项（★ 必须登记 · 交策划侧修订，QA 不改 GDD）

> **处理方式**：以下 4 条为**文档与实现的不一致**。按派单边界，QA **不直接修改 `design/gdd/v22-new-plants.md`**，仅登记需修订的章节与建议表述，由主理人**转交策划侧（文策渊）**修订。

### DS-1（P1 · 必改）· GDD §3.5 震屏强度与实现不一致

| 项 | 内容 |
|---|---|
| **位置** | `design/gdd/v22-new-plants.md` **§3.5 视觉效果建议**（末段） |
| **现状表述** | 「……全屏红光（`triggerFlash('#f44', 0.12)`）+ 强震屏（**`triggerShake(8, 400)`**）+ `SFX.boom()`。」 |
| **实现现状** | 源码 L2126 = **`triggerShake(6, 400)`**（V22-FIX-01 · BUG-02，主理人裁决） |
| **不一致后果** | 若按文档回改 8，会**再次吃掉「失败进屋特写」的一次性 8px 名额**（`loseShakeUsed` 永久置位），使后续所有震屏退化为 6px —— 即 BUG-02 复发 |
| **建议表述** | 「……全屏红光（`triggerFlash('#f44', 0.12)`）+ 强震屏（**`triggerShake(6, 400)`**）+ `SFX.boom()`。」<br>并补注：「**8px 为「失败进屋特写」的一次性名额（`loseShakeUsed`），全局单次震屏 ≤6px（§G 手感红线）；樱桃与航椒均取 6，保证首/次一致。**」 |

### DS-2（Minor）· GDD §2.1 / §2.3 触发阈值「≈54px」与实现 49.5px 不符

| 项 | 内容 |
|---|---|
| **位置** | §2.1 属性表「触发机制」行 + §2.3「引爆判据」 |
| **现状表述** | 「`\|z.x - pos.x\| < CELL_W*0.55`（**≈54px**）」 |
| **实现现状** | `CELL_W*0.55 = 90*0.55 = ` **49.5px**（REG-PEPPER-01 二分实测锁定；源码 L2028 / L2034 / 地雷 L1993 同源） |
| **说明** | **54px 是地雷的「杀伤」半径**（`CELL_W*0.6`，源码 explodeMine L2042 注释），与「触发判据」是两个不同语义；GDD 把两者混写了 |
| **建议表述** | 改为「`\|z.x - pos.x\| < CELL_W*0.55`（**49.5px**）」，并加注：「与地雷**检测**判据同源；地雷的 **54px 是杀伤半径**（`CELL_W*0.6`），勿与触发阈值混淆。」 |

### DS-3（Minor）· GDD §4.3「跳跃中不被啃」仅在绘制层成立

| 项 | 内容 |
|---|---|
| **位置** | §4.3 行为细节第 5 条 |
| **现状表述** | 「跳跃过程中窝瓜自身不参与碰撞（不被啃）」 |
| **实现现状** | 仅在**绘制层**成立（`drawSquashJump` 接管、不绘制在格内）；`updateZombies` 的啃食目标选择（源码 L2233）**未排除 `p.jumping`**，跳跃中仍可被啃 |
| **影响** | 实测 0.6s 窗口最多掉 **39/400** 耐久，**不可致死**，无玩法影响（REG-SQUASH-01 已留痕、不对其作断言） |
| **建议表述** | 「跳跃期间不绘制在格内（由 `drawSquashJump` 接管）；啃食判定未排除 `jumping`，0.6s 窗口最多损失 39/400 耐久，不可致死。」 |

### DS-4（信息 · 无需改文档）· 视觉脚本 detail 文案残留旧值

| 项 | 内容 |
|---|---|
| **位置** | `tests/playtests/v22-newplant-visual.js` 的 **VIS-CHERRY-01** detail 文案中仍出现「请求 8」字样 |
| **性质** | 该文案是**源码机制说明文字**，非判据；实测读数 `intensity=6`，判定 PASS |
| **处理** | **不修改已交付的测试文件**（红线）。仅登记为信息项，避免后人误读为「实现仍是 8」。若后续要清理，需另开一刀并重新复验 |

---

## 4 门控证据

| 门 | 命令/产物 | 读数 | 判定 |
|---|---|---|---|
| **REG**（机制级回归全量） | `tests/harness/run-all.js`（62 个 REG 用例） | **62/62 PASS** | 🟢 |
| **SMOKE**（烟雾） | `tests/harness/run-smoke.js`（29 个 SMOKE 用例） | **29/29 PASS** | 🟢 |
| **BUS**（总线/音频） | `tests/harness/verify-bus.js` | **56/56 PASS** | 🟢 |
| **真机视觉** | `tests/playtests/v22-newplant-visual.js` + `v22-newplant-visual-results.json` | **7/7 子项 PASS · overall=PASS** | 🟢 |
| **平衡画像（本轮新增）** | `tests/playtests/v22-balance-profile.js` + `-results.json` | **V22-BAL PASS**（G0 20/20 · G1 4/4 · G2 双卡达标；480 局；353s） | 🟢 |
| **前置门（画像用）** | prelude 七道门，四臂各跑一遍 | gate1–6 全过（含 v2.2.0 版本自证 / 常量快照 / mulberry32 同种子一致） | 🟢 |

> 说明：REG/SMOKE/BUS 三色门控已由主理人在工程侧修复后复验通过，本轮按派单**引用结论、未重跑**（评审强度 lean）。真机视觉 7/7 亦为主理人复验结论，本轮**读取其 `results.json` 原始读数**核对后引用。
> 本轮**唯一新增执行**的是平衡画像（`V22-BAL`），其自身带三道校准门 + 可复现性自检（`2-1#9300` 两次 run 深度相等）+ 卫生检查（跑后工作树无任务外脏文件）。

---

## 5 遗留与已知风险

### 5.1 继承项（v2.1.1 及更早，状态不变，**本轮无新增**）

| # | 项 | 级别 | 状态 |
|---|---|---|---|
| **R5** | 弹道手感人工复核 | 低 | ⏳ 建议（非阻塞） |
| **R2** | 整局 playthrough 未做 | 低 | 已披露 |
| **R3** | 仅验 Edge / Chromium 系 | 低 | 已披露 |
| R4 | testMode 沙盒边界（新增存档键须回头查守卫） | 低 | 已披露（v2.2 **未新增任何存档键**） |
| **N-13** | 多语言 | 最低 | 🧊 冻结至 v3.0.0 |
| **N-14** | 移动端 | 最低 | 🧊 冻结至 v3.0.0 |
| — | 世界 3/4 除锚点外全占位（Q-12） | 观察项 | 📌 v2.x 补全 |
| — | spawnSync/execFileSync 会话级 EBUSY | P3 | ⚠️ 工具链问题，有 workaround（异步 spawn / 逐门直跑） |

### 5.2 本轮新识别的观察项（非缺陷，登记备查）

| # | 项 | 级别 | 说明 |
|---|---|---|---|
| **O-1** | **未测高难度档** | 观察 | 平衡画像只跑 `DIFF=normal`。三张新卡是**无差别秒杀**（不参与耐久结算），在 `hard×1.35` / `expert×1.80` 下相对强度会系统性上升。**建议 v2.3 或下一刀补 `hard` 档同口径跑分**（同脚本改 `g.setDiff('hard')`，约 6 分钟）。**不阻塞本轮放行** |
| **O-2** | **航椒/樱桃「使用率偏低」** | 观察 | 应急型打法下两者 **0 次购买**；压测臂才用到（航椒 117 株 / 樱桃 71 株）。提示 125/150 定价在真实经济里**偏高**（偏冷而非过强）。属**策划议题**，本轮不提数值建议 |
| **O-3** | 窝瓜在 1-5/1-6 段呈轻微负收益 | 观察 | 压测臂种下 87/85 株（≈14 株/局，逼近 CD 15s 上限），通关时长**变慢 2.50/2.25s**。判定为**健康定价**（便宜但不无脑），不建议调整 |
| **O-4** | 单关样本分辨率 1/6 ≈ 16.7pp | 局限 | 平衡画像单关结论只作参考；**不支持任何单关数值回调**。聚合 120 局 95%CI ±2.8pp |

---

## 6 放行建议与阻塞项清单

### 6.1 阻塞项

> **无阻塞项。**

### 6.2 放行建议（advisory）

| # | 建议 | 是否阻塞 |
|---|---|---|
| 1 | **放行 v2.2 进入下一刀内容**：功能门控四门全绿、三缺陷闭环、平衡画像否定两条中风险 | — |
| 2 | **立即启动 GDD 文档同步**：`DS-1`（§3.5 震屏 8→6）**必须改**，否则有 BUG-02 复发风险；`DS-2`（49.5px）、`DS-3`（跳跃啃食）建议同批改 | 否（但 DS-1 建议在下次触碰该文档前完成） |
| 3 | **下一刀补 `hard` 档平衡复核**（O-1） | 否 |
| 4 | 继承项 R5/R2/R3 按原计划推进；N-13/N-14 维持冻结至 v3.0.0 | 否 |
| 5 | 本轮产物（`production/v22-balance-profile.md`、本报告、`tests/playtests/v22-balance-profile*.js/json`）**尚未 commit** —— 按派单红线，提交与否由用户/主理人决定 | — |

### 6.3 边界遵守声明

- ✅ **未修改** `plants-vs-zombies.html`（源码）
- ✅ **未修改** 已交付测试文件（`tests/harness/cases/REG-*.js`、`tests/playtests/v22-newplant-visual.js`）
- ✅ **未修改** `design/gdd/v22-new-plants.md`（文档待同步项仅登记在 §3）
- ✅ **未 git commit / 未 push**
- ✅ 新增临时脚本仅落 `tests/` 下两件（`v22-balance-profile.js` / `-results.json`），已在本报告 §1.3 与平衡报告「附」披露；**未污染 `production/` 与源码**

---

—— 严守真 · V22-QA-03 D4 判定完（**判定：PASS · 建议放行 · 无阻塞项**）——
