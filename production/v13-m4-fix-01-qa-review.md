# V13-M4-FIX-01 v2 独立复核报告

- **复核人**：严守真（QA · quality-lead）
- **复核时间**：2026-09-19 14:01–14:06 UTC（无头 harness，seed:42）
- **复核对象**：`plants-vs-zombies.html` 工作树（FIX-01 v2：直线弹贴坡 + L1/L2 出怪串行门）
- **派单**：team-lead Task #4（V13-M4-FIX-01 v2 独立复核）
- **判定（advisory）**：**机器侧 PASS**，附 3 项非阻塞备注；真机签收项单列（见 §7）

## 0. 锚点与红线遵守

| 版本 | md5（前8位） | 行数 | 说明 |
|---|---|---|---|
| git HEAD | `79d4c275` | 2346 | M3 平面版（基线来源，blob 直读） |
| M4 备份 `/tmp/pvz-baseline.html` | `354b3f63` | 2408 | M4-QA-03 签收态 |
| **FIX-01 v2 工作树（复核对象）** | `6b178be4` | **2431** | 开工/终验一致 |

- 红线核查：✅ 未改源码终态（三组破坏均精确还原，md5 逐一留痕见 §2）；✅ 全程无 git 提交/推送（numstat=142/57 相对 HEAD 不变）；✅ 临时变体均写入 TEMP，未触碰工作树。
- 工程自述 vs 实测：自述 2430 行，实测 `wc -l` = **2431**（疑为末行换行计数口径差），非阻塞，记录在案。`REG-GATE-01` 存在=true、`REG-ROOF-02` 存在=false，与自述"替代"关系一致 ✅。
- 工程自述三处关键改动独立定位：L912 `if(levelNo===1||levelNo===2){`（门 gating，1-based）；L913 `gt-spawnGateT<10` 阈值；L1104 `pr.y=pr.flatY-liftX(pr.x)-pr.yOff`（贴坡重算）；L1000–1007 发射快照 `flatY/yOff`；L840 startGame 复位；L863–864 状态声明。均与自述相符 ✅。

## 1. 四门控独立复跑（各 ×3）

| 门控 | run1 | run2 | run3 | 判定 |
|---|---|---|---|---|
| SMOKE | 28/28 | 28/28 | 28/28 | PASS |
| REG（含 REG-GATE-01） | 35/35 | 35/35 | 35/35 | PASS |
| ALL | 63/63 | 63/63 | 63/63 | PASS |
| BUS（T1 核验） | 56/56 | 56/56 | 56/56 | PASS |

- 单用例行运行 ×3：SMOKE-028（L5 屋顶 C2 坡上弹道命中）与 REG-GATE-01（L1/L2 全波次出怪串行门）逐次均绿，耗时 4–10ms，无 flaky 迹象。
- bench（×1）：场景 E（L5 末波地狱）p50 **0.81ms**（5% 预算）、p95 1.55ms；A/B/C/D p50 0.11/0.34/0.45/0.56ms，与 M4-QA-03 阶段记录（E 0.77–1.17ms 带）同量级，无性能退化。bench 后源码 md5 复核不变（`6b178be4`）✅。

## 2. 对抗性破坏验证（破坏 → 红 → 精确还原，md5 留痕）

| # | 破坏 | 破坏态 md5 | 结果 | 还原后 md5 | 还原后 |
|---|---|---|---|---|---|
| D1 | 注释 L1104 贴坡重算行 | `4fcb807b` | **SMOKE-028 红 27/28**：Part A① col4 捕获 `pr.y=278 @ x=507, lift=60, flatY2=340`（期望 flat−lift−8=272，超出 ±单tick滞后 2.5 容差） | `6b178be4` | 28/28 绿 |
| D2 | 阈值 `10`→`0`（L913） | `1a6e0ad5` | **REG-GATE-01 红 34/35**：A1 失败 `[2,0,false]`（3.05s 时 #1 存活应拦 #2，实测 2 只在场、队列耗尽、波推进） | `6b178be4` | 35/35 绿 |
| D3 | gating 摘除 L2（`levelNo===1`） | `7208c3a0` | **REG-GATE-01 仍绿 35/35（未变红）** → 见 §2.1 判别性分析 | `6b178be4` | 35/35 绿 |

**结论**：D1/D2 判别力实证成立——套件能抓住贴坡与阈值两类破坏。D3 未变红，经专探针（§2.1）查明为**用例判别力缺口**而非门实现缺陷。

### 2.1 D3 深挖：A5（L2 生效）非判别性 —— 机理与建议

- 摘除 L2 门后 A5 仍绿的机理：L2 W1 波配置 `interval=10`，出怪节奏 `[normal×2]`。无门时 #2 也在 @10.05 放出；有门时前尸 #1 存活、`gt−spawnGateT<10` 恰在 @10.05 失效放行——**两态行为巧合等价**。A5 的断言（9.05 拦 / 10.05 放）对两种态都成立，故不构成判别列。
- 探针实测（d3-probe）：`queue after newWave(1): 2` → `#1 @0.05` → `@9.05 zombies=1 queue=1` → `@10.05 zombies=2 queue=0`，与推演一致。
- **建议（测试增强，非阻塞）**：为 REG-GATE-01 增加 A6——选 L2 **W2**（cfg `interval=9`，`[normal×2, fast×1]`）：有门且前尸存活时断言 @9.05 未放（门拦至 10.0s），无门时 @9.05 已放（9.05<10.0）。该列在摘门破坏下必红，可补齐 L2 判别力。
- 说明：工程自述"门误套 L5 必红"由 A3（L5 旁路）承担，A3 判别性不受 D3 影响；本次发现仅限 A5 列。

## 3. 出怪时刻表基线对照（基线：HEAD blob 直读 `v13-m4-fix-01-baseline.json`，同 seed:42 + 同清场策略）

| 关卡 | identical | 只数 | 关键差异 |
|---|---|---|---|
| L1 | **false** | 6=6 | gaps `[16, 9.05, 7, 8.05, 8.05]` → `[16, 0.05, 16, 0.05, 0.05]`；#3/#5/#6 提前 9s/8s/16s（60.2s→44.2s 收尾） |
| L2 | **false** | 9=9 | gaps 8 项中 6 项变 0.05（串行签名）；#2 提前 22.05→12.1；末 4 只压缩至 44.1–44.25s |
| L3 | **true** | 9=9 | 逐事件零差异 |
| L4 | **true** | 9=9 | 逐事件零差异 |
| L5 | **true** | 9=9 | 逐事件零差异 |

- **方向核验 ✅**：L1/L2 异于基线 = 门生效（死亡立即放行 → 0.05 串行间隔是门的签名形态）；L3/L4/L5 逐事件一致 = 零漂移。
- **发现（advisory，非阻塞）**：L2 类型序列发生置换——基线 `[…, normal, cone, normal, normal]` → FIX `[…, normal, normal, normal, cone]`（i=6 cone→normal、i=8 normal→cone）。**类型多重集守恒**（3 normal + 1 cone 不变），仅顺序变化。机理推断：僵尸类型在出怪时刻经 PRNG 抽取，门改变出怪时刻 → PRNG 消费顺序移动 → 类型归属重排；seed:42 下完全确定、可复现。此为改变出怪时序的伴生效应，建议知会文策/用户确认"类型顺序变化是否可接受"（L1 类型序列未变）。

## 4. 贴坡几何声明独立核实（含恒定y退化对照变体）

几何常量：GRID_X=55、CELL_W=90、CELL_H=104、GRID_Y=80、flatY(row2)=340、ROOF_LIFT_H=60、ROOF_COLS=5；命中窗口纵向 `|zy−pr.y|<32`、横向 `|z.x−pr.x|<42`（L1111）。

| 探针 | 场景 | FIX（贴坡） | 恒定y变体 | 判定 |
|---|---|---|---|---|
| P1 | col6 植物对左侧僵尸 x=145 | fired=false | — | ✅ 几何约束成立（hasZombieAhead 只认右侧，字面 col6→col1 场景不可构造） |
| P2 | col0 植物 → col8 平台僵尸 x=755 | hit=true（hp 180→160） | **hit=false** | ✅ 判别力充分：变体恒 y=326 vs zy=280，偏差 **46>32** 必 miss；FIX 贴坡 y=272，偏差 8<32 命中 |
| P3 | col1 植物 → col4 斜坡僵尸 x=435 | hit=true | **hit=true（未判别）** | ⚠️ 见下 |

- **P3 机理复算（已钉死，非缺陷）**：plant col1 格心 x=190、liftX=18，发射 y=340−18−8=314（恒定）；僵尸 zy=340−liftX(x)，命中窗 x∈(393,477) 内 zy∈(283.7, 294.9)，恒定y偏差 ∈ **(19.1, 30.3)**，全程 <32 容差 → 旧语义也命中。col1→col4 的 liftX 差（50.7−18≈32.7）被发射偏移 −8 与命中窗左缘几何吃掉，**该镜像场景对恒定y判别力客观不足**。非源码缺陷、非探针缺陷。
- **套件判别力兜底 ✅**：镜像判别主力 P2（col0→col8）成立；SMOKE-028 Part A① 为逐帧不变量 `|pr.y−(flat−liftX(pr.x)−8)|≤2.5`，对"是否贴坡"是全列强判别（D1 实证捕获 col4 y=278 vs 期望 272）。P3 教训记入 §6：镜像 hit/miss 对照列应选 liftX 差 >40px 的列对（如 col0→col8），col1→col4 仅作补覆盖。
- 另核查：cabbage 抛物分支（L1012 起）不经过 `flatY` 路径（L1101 `else if`），本次未触碰，红线遵守 ✅。

## 5. L1–L4 零漂移三重独立验证

1. **代码审计**：`liftX` 单点门 `if(!level.roof)return 0`（L237–240）沿用 M4 审计结论，L1–L4 全部 C2 触点经其归零。FIX-01 新增贴坡分支在 L1–L4 下 `pr.y=flatY−0−yOff`，与发射常量逐帧等价（仅计算时机从发射一次变为每帧，bench A–D 无退化佐证）。
2. **基线 diff**：L3/L4/L5 `identical=true`、逐事件零差异（§3）。
3. **时序用例**：SMOKE-024、REG-WAVE-02/03 等 L1–L4 时序关联用例含于 28/35 全绿，×3 复跑无漂移。

## 6. 用例断言强度评估（证据评审口径）

| 用例/列 | 强度 | 依据 |
|---|---|---|
| SMOKE-028 Part A① 贴坡不变量 | **强** | 逐帧全列判别 + D1 实证 |
| SMOKE-028 Part B/C/D 命中窗 | 强（B/C）/ 中（D 镜像 col0→col8） | P2 判别成立 |
| SMOKE-028 D 之 col1→col4 补覆盖 | 弱（对恒定y不判别） | P3 复算 §4；建议保留但标注定位 |
| REG-GATE-01 A1 死亡即放行/A4 反转 | **强** | D2 实证（A1 先红） |
| REG-GATE-01 A2 精确 10.0 阈值 | 强 | 边界值断言 |
| REG-GATE-01 A3 L5 旁路 | 强 | gating 显式排除 L5 |
| REG-GATE-01 A5 L2 生效 | **弱（非判别列）** | D3 实证 + §2.1 机理；建议补 A6（L2 W2 interval=9 @9.05 拦截断言） |

综合：**ADEQUATE（附 2 项增强建议）**——关键路径均有强判别列覆盖，弱列为补覆盖性质，不构成签收阻塞。

## 7. 签收判定与真机项

- **机器侧判定：PASS**。四门控 ×3 全绿、破坏 D1/D2 判别力实证且精确还原、L1/L2 门生效方向正确、L3–L5 零漂移、几何声明核实、性能无退化、源码终态未变（`6b178be4`/2431 行）。
- **Advisory 备注（均非阻塞）**：
  1. REG-GATE-01 建议补 A6 强判别列（§2.1）；
  2. 镜像 hit/miss 对照列选择原则：liftX 差 >40px（§4 P3 教训）；
  3. L2 类型顺序置换建议知会文策/用户确认（§3，多重集守恒、确定可复现）。
- **真机签收项（单列，须用户/真机完成后方可关单）**：
  - 贴坡观感：直线弹沿坡连续、无跳变（对应阶段 A M 系列屋顶观感项）；
  - L1/L2 节奏手感：前期"同一时刻场上僵尸 ≤1 只（存活期内）"的压迫感是否符合用户原始反馈预期——本修复的最终验收方是用户真机体验；
  - L2 类型顺序变化真机可感知度确认（如需）。

## 附录：证据文件（TEMP，均只读探针/变体，未触碰工作树）

- `fix01-qa-gates.js/.txt` — 四门控 ×3 + bench + md5 前后核验
- `fix01-qa-adversarial.js/.txt` — D1/D2/D3 破坏-还原全留痕
- `fix01-qa-d3-probe.js/.txt` — A5 非判别性机理探针
- `fix01-qa-diff.js/.json/-cmp.json/-DONE.txt` — 基线对照（L1–L5 逐事件）
- `fix01-qa-geometry.js/.txt` + `fix01-qa-consty-variant.html` — 几何 P1/P2/P3（变体文件可清理）
- `fix01-qa-final.js/.txt` — 终验：md5/numstat/用例数/门控全绿
- 基线：`production/v13-m4-fix-01-baseline.json`（HEAD blob 直读生成）
