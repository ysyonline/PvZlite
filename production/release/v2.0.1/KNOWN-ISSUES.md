# PvZ Lite v2.0.1 · 已知问题清单（Known Issues）

> **文档状态：定稿 v1.0**（2026-09-24 · 技术门全绿，发布执行）。
> **版本**：v2.0.1 ｜ **日期**：2026-09-24 ｜ **发布门控判定**：✅ 全门 PASS（见 RELEASE-CHECKLIST §F）
> v2.0.1 为 v2.0.0 的补丁版本（唯一变更 = `?test=1` 解锁定闸）。v2.0.0 周期的变更、闭环与遗留见 `production/release/v2.0/KNOWN-ISSUES.md`（全部继承，本文件只记增量）。

---

## 汇总（本版增量）

| # | 项 | 级别 | 状态 | 说明 |
|---|---|---|---|---|
| — | `?test=1` 解「锁定」闸 | 中 | ✅ **已闭环（v2.0.1 · T-602）** | 三处 `!testMode` 前置（选关页点关 L1272 / 结算屏下一关 L1319 / 选关页锁定视觉 L3441）；**占位关拦截保留**（Q-3/Q-12：占位关永远不可玩，与解锁闸无关）；testMode 纯沙盒零写守卫不变 |
| — | spawnSync EBUSY（会话级环境异常） | P3 | ⚠️ 已知缺口（有 workaround） | **工具链问题，非游戏代码问题**：WorkBuddy 会话内 `spawnSync`/`execFileSync` 对任意 .exe 恒 EBUSY（node/git/cmd 全灭，复跑无效），主理人与工程子会话均复现；**异步 spawn 正常**。影响面 = `run-gates.js` 一键入口与 `git show` 导出脚本；workaround = 逐门直跑等效验证 + bash 重定向/异步封装导出。若后续会话复现，按此径直走 workaround，勿反复重试 |
| — | i5-7500T bench 绝对耗时抬升 | 低 | 已披露（环境差异） | 家庭机 i5-7500T 无头 bench 帧耗时整体高于上版 i5-12500 记录；**DC 结构量五场景逐场景一致零漂移**（判级 PASS）；perf-profile §7 已按本机口径幂等回填 |

## 继承项（v2.0.0 及更早，状态不变）

| # | 项 | 级别 | 状态 |
|---|---|---|---|
| R5 | 弹道手感人工复核 | 低 | ⏳ 建议（非阻塞） |
| R2 | 整局 playthrough 未做 | 低 | 已披露 |
| R3 | 仅验 Edge / Chromium 系 | 低 | 已披露 |
| R4 | testMode 沙盒边界（新增存档键须回头查守卫） | 低 | 已披露（本版未新增任何存档键） |
| N-13 | 多语言 | 最低 | 🧊 冻结（补丁版无需复核，下一大版本 v3.0.0 再核） |
| N-14 | 移动端 | 最低 | 🧊 冻结（同上） |
| — | 世界 2/3/4 除锚点外全占位（Q-12） | 观察项 | 📌 v2.x 补全 |
| O1/O2/O4/积分商城 | 优化池冻结项 | P3 | 🧊 冻结（继承） |

---

## 方法论沉淀（本版新增）

1. **spawnSync EBUSY 判定流程** —— 子进程启动恒 EBUSY 且复跑无效时，先切异步 spawn 验证（正常 ⇒ 会话级环境问题；仍 EBUSY ⇒ 机器级），**不要反复重试同步调用**；门控直跑清单以 `run-gates.js` 源码内部列表为准逐门等效。
2. **补丁版发布包粒度对齐 v1.2.1 先例** —— 变更表（单行）+ 零其他变更声明 + 回滚 = 上一版冻结副本；不重复大版本的完整章节。
3. **跨会话核验口径** —— 冻结副本导出后必做三重校验（sha256 / cmp 工作区 / CR 计数），本版 CR=0 属 LF 基线环境，换机后工作区直算可能 CRLF 虚警，仍以 `git show <commit>` 管道口径为权威。
