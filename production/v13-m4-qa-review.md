# v1.3-M4 · L5 屋顶 C2 五列坡面 · 独立 QA 验收复核

> 任务 **V13-M4-QA-03**（第二层 QA · 独立验收复核）· 作者：严守真（quality-lead）· 2026-09-19
> 性质：**独立验证报告（advisory）**。不接受工程自报数字，全部自行复跑；对「看起来没问题」主动打破。
> 复核对象：`V13-M4-ENG-03`（`plants-vs-zombies.html` 2408 行·未提交）· `SMOKE-028` / `REG-SLOPE-01`
> 权威依据：`tests/v13-m4-slope-acceptance.md`（§1 门控 / §2 断言口径 / §3 真机项 / §6 签收）；`production/v13-m4-c2-5col-impl-spec.md`（§3 BLOCKING）
> 线索（非证据）：`production/v13-m4-impl-report.md`
> **红线遵守**：未提交/推送 git；未新增或删除用例；源码**最终状态逐字节未变**（全程「临时破坏→验证→精确还原」，还原证据见 §2）。

---

## 0. 一句话结论

**机器侧复核 = PASS；整体签收 = CONCERNS（待真机 M1–M8）。** 四门控我独立复跑数字与工程自报**一致**（28/34/62 + 总线 56，且新用例连跑 3 次无抖动）；两条新用例经我**亲手破坏**证明**具判别力**（去掉关键抬升 → 对应用例必红，还原后必绿，md5 逐字节核验）；L1–L4 零漂移经代码+运行时双重独立验证成立。**但无头不可替代的 M1/M3（一票否决）等真机项未做 ⇒ 依 §6 不得判整体 PASS**，须用户真机签收。

| 维度 | 判定 |
|---|---|
| 四门控（独立性复跑） | **PASS**（28/28 · 34/34 · 62/62 · 56/56，各 3 次一致） |
| 用例判别性（对抗验证） | **PASS**（两条均「破则红 / 还原则绿」） |
| L1–L4 零漂移 | **PASS**（byte-exact + roof-gate 双证） |
| 待定项 (a) 檐沟超规格 | **PASS（可接受）** · 附登记建议 |
| 待定项 (b) bench p50 | **PASS（预算内）** · 附单列 issue 建议 |
| 用例断言强度 | **PASS**（无过宽漏洞；有 1 处低风险覆盖盲区，见 §5） |
| **整体签收（advisory）** | **CONCERNS** — 机器全绿，但 M1/M3 一票否决项须真机 |

---

## 1. 四门控独立复跑（原始输出 + 连跑 3 次）

Node：`C:\Users\weixufeng\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`

### ① 烟雾 `node tests/harness/run-smoke.js`
```text
 ✅ SMOKE-027  L5 屋顶契约（波次平衡 + 屋顶轴校验）  (7ms)
 ✅ SMOKE-028  L5 屋顶 C2 坡上弹道命中（平台 col6 + 斜坡 col4 双列）  (7ms)
 结果: 28/28 PASS  ·  总耗时 202ms
 门控: PASS（全绿，允许 commit）
```
- 3 次结果：**28/28 · 28/28 · 28/28**（耗时 202 / 197 / 203 ms，SMOKE-028 稳定 4–7ms）。

### ② 回归（默认 REG）`node tests/harness/run-all.js`
```text
 结果: 34/34 PASS  ·  总耗时 150ms
```
- 3 次结果：**34/34 · 34/34 · 34/34**。

### ③ 回归（全量）`node tests/harness/run-all.js --all`
```text
 结果: 62/62 PASS  ·  总耗时 303ms
```
- 3 次结果：**62/62 · 62/62 · 62/62**。增量口径核对：28（烟雾）+ 34（REG）= 62 ✓。

### ④ 音频总线 `node tests/harness/verify-bus.js`
```text
  T1 核验：56/56 PASS
  门控：PASS（幂等安全 / 路由完整 / 隔离生效）
```
- **56/56 不变**（本轮零音频改动），✓。

### ⑤ 性能基准 `node tests/perf/bench.js`
```text
 场景 E · L5 末波地狱（15 植物 + 30 僵尸含双桶，屋顶）
   帧耗时  p50 0.77ms (5% 预算) · p95 1.44ms (9%) · max 2.65ms · mean 0.88ms
   draw calls(帧中位) 全口径 2764 · 估算口径(§1.1) 1448
 判级（无头口径）: PASS
```
- 五场景全 PASS。**稳定性结论**：`run-smoke` / `run-all` 各连跑 3 次结果完全一致（`seed:42`），SMOKE-028 / REG-SLOPE-01 **无 Pass/Fail 抖动** ⇒ 非 flaky，无需按 `bug-taxonomy.md` §5.3 隔离。
- **与自报对照**：工程自报 28/34/62 + 56 + bench PASS —— **我复跑数字逐项一致**（唯 bench 绝对耗时不同，见 §4(b)）。

---

## 2. ★ 对抗性反向验证（用例有效性证明 · 亲手重做）

> 方法：对源码做**单点破坏** → 立即用 `md5sum` + `grep` 核验「改坏已落盘」→ 跑对应用例 → 从基线备份**精确还原** → 再核验 `md5` 回到基线。**破解项目「同文件编辑静默未落盘」历史坑**：每次破坏都与还原均留哈希证据。
> 基线：`plants-vs-zombies.html` md5 = `354b3f63c7a644dcf5fa3c1c01646b47` · `git diff --numstat` = `112 49` · 2408 行 · 备份 `/tmp/pvz-baseline.html`（md5 同）。

### 破坏① —— 去掉 `posToGrid` 的 `+liftX(x)`（L247）
- 破坏后：`md5 = 75eb66bf2b35fba6ac9e0f17e7169c7e`（≠ 基线，**已落盘**）；`L247` 实为 `y:Math.floor((y-GRID_Y)/CELL_H)`。
- 跑 `run-all.js`：
```text
 ❌ REG-SLOPE-01  L5 屋顶 C2 坡面几何契约
   └─ Error: 断言失败: B：L5 gridToPos↔posToGrid 往返应精确 == (4,0)
        ::  [{"x":460,"y":78},{"x":4,"y":-1}]
 结果: 33/34 PASS · 1 FAIL  ·  门控: FAIL（有阻塞项）
```
- **结论：REG-SLOPE-01 变红 ✓（判别有效）**，且失败列正是判别列 **col4**（反解得 `y:-1`），与验收 §2.1 预期一致。

### 破坏② —— 去掉 `updateProjectiles` 命中基线 `-liftX(z.x)`（L1088，B1）
- 破坏后：`md5 = 2c8123c5a4e0a9c081f253ad0b800e8b`（≠ 基线，**已落盘**）；`L1088` 实为 `...CELL_H/2;`（无 `-liftX`）。
- 跑 `run-smoke.js`：
```text
 ❌ SMOKE-028  L5 屋顶 C2 坡上弹道命中（平台 col6 + 斜坡 col4 双列）
   └─ Error: 断言失败: SMOKE-028 Part B：col6 坡上豌豆应命中扣 20（180→160）  ::  180
 结果: 27/28 PASS · 1 FAIL  ·  门控: FAIL（有阻塞项，未达 QA）
```
- **结论：SMOKE-028 变红 ✓（判别有效）**，僵尸 `hp` 停在 180（未命中），Part B 失效被精确捕获。

### 还原核验证据（逐字节）
| 动作 | `md5sum plants-vs-zombies.html` | `git diff --numstat` | 用例 |
|---|---|---|---|
| 破坏①后 | `75eb66bf…`（≠基线） | — | REG-SLOPE-01 **RED** |
| **还原①后** | **`354b3f63…`（=基线）** ✓ | **`112 49`** ✓ | REG **34/34** ✓ |
| 破坏②后 | `2c8123c5…`（≠基线） | — | SMOKE-028 **RED** |
| **还原②后** | **`354b3f63…`（=基线）** ✓ | **`112 49`** ✓ | SMOKE **28/28** ✓ |

> **两条用例均具判别力，无「去掉仍绿」的无效用例**；还原后源码与破坏前**逐字节一致**。此结论**独立于工程自报**成立。

---

## 3. L1–L4 零漂移 · 独立抽查

**（a）代码层**（`git diff` + 逐行读）：
- `liftX`（L237–240）**单点 gate**：`if(!level.roof)return 0;` ⇒ 非屋顶关恒 0。
- `gridToPos`（L241–244）：`y:GRID_Y+row*CELL_H+CELL_H/2-liftX(x)`；liftX=0 时**逐字节等于**旧式 `GRID_Y+row*CELL_H+CELL_H/2`。
- `posToGrid`（L245–248）：`y:Math.floor((y-GRID_Y+liftX(x))/CELL_H)`；liftX=0 时**逐字节等于**旧式 `Math.floor((y-GRID_Y)/CELL_H)`。

**（b）运行时层**（独立探针，**非 harness 用例**，置于 OS 临时目录，不污染 `tests/`）：对 L1–L4 遍历全部 45 格，把 `gridToPos`/`posToGrid` 与**平面闭式原式**逐字节比对，并对 x∈[-200,1200] 每 1px 抽验 `liftX===0`：
```text
L1 roof-gate OK, 45-cell byte-exact OK
L2 roof-gate OK, 45-cell byte-exact OK
L3 roof-gate OK, 45-cell byte-exact OK
L4 roof-gate OK, 45-cell byte-exact OK
PROBE RESULT: ALL OK (0 fails)
```
**结论：L1–L4 零漂移成立**（结构免疫 + 运行时 byte-exact 双证）。

---

## 4. 待定项裁决

### (a) 超规格改动：右侧檐沟顶随平台抬升到 `GRID_Y−H`（L1545–1549）→ **PASS（可接受）**
- 源码 L1547：`const gutterTop=level.roof?GRID_Y-ROOF_LIFT_H:GRID_Y;`
- **roof-gated 核实 ✓**：三元显式判 `level.roof`；非屋顶关取 `GRID_Y`。
- **老关逐字节等价 ✓**：旧式 `fillRect(GRID_X+COLS*CELL_W,GRID_Y,ROAD_W,ROWS*CELL_H)`；新式 `height=GRID_Y+ROWS*CELL_H-gutterTop`，老关 `gutterTop=GRID_Y` ⇒ `height=ROWS*CELL_H`，**完全一致**。（其上 `fillStyle=level.roof?'#5a5248':'#5a3a22'` 为 M3 既有行、未变。）
- **可单点回退 ✓**：把 L1547 改回 `const gutterTop=GRID_Y;` 即恢复原行为（1 行）。
- **裁决**：工程理由（不抬升会在平台右上 `x∈[865,955]×y∈[20,80]` 露 60px 天空缝）**成立且自洽**；改动**最小、门控干净、可回退**。**可接受**。建议：**在发布说明/回退文档登记该 2 行**（规格 §2.E 表未列，属已披露的超规格项）。
- ⚠️ 相邻既有瑕疵（**非本轮引入**）：画布右缘 `x∈[955,1000]` 露 CSS 绿底，改动前后截图均存在（`ROAD_W=90` 全关卡既有）—— 工程已诚实披露并建议单列 issue，**我确认其不在本轮范围**。

### (b) bench 场景 E p50 变化 → **PASS（帧预算内）** · 建议单列低优先级 issue
我在**本机**独立做了 before/after（临时切 `git show HEAD:` 版本 → 跑 → 精确还原，md5 已核验回基线）：

| 场景 E | 改动前(HEAD) | 改动后(M4) | 备注 |
|---|---|---|---|
| p50 | **0.48ms** | **0.77–1.17ms**（3 次） | 绝对增量 **+0.29~0.69ms**；相对 +60%~+144% |
| p95 | 0.93ms | 1.44–2.51ms | 均 ≪ 16.67ms |
| draw calls 估算口径 | 1424 | 1448 | **+1.7%**（与自报一致） |
| draw calls 全口径 | 2429 | 2764 | **+13.8%** |

- **判定**：p50/p95 均**远在 16.67ms 帧预算内**（p50 占预算 5%）⇒ **可接受，PASS**。
- **关于自报 3.34ms（+46%）**：该**绝对数字我无法复现**（本机 p50 仅 0.77–1.17ms）。无头 bench 帧耗时报 **受 JIT 冷热/机器负载影响的强噪声量**，跨机器不可比；但**结构量（draw calls）逐项吻合**，说明工程自报数据**非虚构**，仅绝对耗时随环境漂移。
- **★ headless 乐观下限（必须写进结论）**：计数 `ctx` **不做光栅化/文本布局**，测得仅为「JS 逻辑 + 调用编排」。C2 把斜坡 45 格由 `fillRect` 改为**平行四边形 path**、并新增半缝/檐板/压条描边 ⇒ **光栅化工作量与真实帧耗增加被 headless 完全遮蔽**。故：**本 bench 的 p50 只能作为「乐观下限」，不能作为「真机帧率达标」的依据**；真实 GPU/Canvas 光栅开销须真机 DevTools 复核。
- **建议**：**单列低优先级性能 issue（随 `KNOWN-ISSUES`，如 #8）**跟踪「C2 斜坡平行四边形绘制的真机帧耗」，作为发布后专项；**不阻塞**本轮（无头判级 PASS + 绝对量极小）。

---

## 5. 新用例断言强度评估

### 5.1 Part A 期望值自洽性（自行按公式推算）→ **自洽 ✓**
`flatY(row2)=GRID_Y+2·CELL_H+CELL_H/2=80+208+52=340`（探针实测 =340 ✓）
- col6 格心 x=640 ⇒ `liftX=60` ⇒ `pr.y = 340−60−8 = **272**` ✓（= 用例断言）
- col4 格心 x=460 ⇒ `liftX=54` ⇒ `pr.y = 340−54−8 = **278**` ✓（= 用例断言）
> 发射偏移 −8 已计入（`projectiles.push({...,y:pos.y-8,...})`，L989）；`pos.y=gridToPos(col,2).y` 已自动抬升。**口径与 H=60 完全自洽。**

### 5.2 覆盖广度
- **Part B 双列覆盖 ✓**：`samples=[{col:6},{col:4}]` 并跑 —— 覆盖**平台（col6，lift=60）** + **斜坡末列（col4，lift=54，判别最灵敏列）**，僵尸置植物**右侧**（α/β 均命中）。
- **Part C 抛物 cabbage ✓**：col6 投手发射 `type==='cabbage'` 弹、命中 `hp 180→140`（dmg 40），对应 spec §3-**B2**（cabbage 瞄准基线 `−liftX(target.x)`）。
- **REG-SLOPE-01**：Part A `===` 逐字节（45 格 ×4 关）· Part B 全 45 格往返 + col4<col5 判别 + 平台恒定 · Part C 单调/恒定/clamp 且 **H-无关**（抗未来微调）—— 结构完整。

### 5.3 是否存在「断言过宽」漏洞 → **未发现**
- Part B 断言 **`hp===160`（精确值）**，非「hp 有变化」：其它掉血路径（地瓜 500 秒杀、cabbage 40）均**不产生恰好 −20**，故 `hp===160` **严格蕴含「恰好一次豌豆命中」**，不会被别的路径伪造通过。
- Part A 断言 `|shot.y−expY|<0.01`：**双向守护**，既拦「未抬升」也拦 B4「双重抬升」。
- Part C 断言 `type==='cabbage'` 存在 + `hp===140`。
- 循环均带**有界 guard**（100/200/400）且不改变断言（超时仍走最终断言，失败可见）⇒ 无「静默超时假绿」。
- **未发现**过宽导致测不出错的漏洞。

### 5.4 覆盖盲区（低风险 · 供登记，不阻塞）
1. **B3 `explodeMine` 坡上无直接用例**（`SMOKE-028` 只覆盖 B1/B2；REG-SLOPE-01 只验映射）。B3 为 `nearCell` 惰性分支、影响小，且已按同口径 `−liftX(z.x)` 改（L1039）。建议后续补一条 L5 地瓜断言（可选）。
2. **实体 y（`drawZombie`/`killZombie`/`spawnCorpseParts`，L1160/L1188/L1879）无头不可测** —— `probe()` 不吐实体 y（`harness/index.js` L128–134），故**只能真机看**（→ M1）。
3. Part C 仅覆盖 col6（平台），未覆盖 col4 抛物——影响极小。

---

## 6. 签收判定（advisory）与阻塞项

依 `tests/v13-m4-slope-acceptance.md` §6：

> **PASS** = 门控 28/34/56 全绿 + 两条新用例判别性自检通过 + **M1/M3 真机通过**。
> **CONCERNS** = 门控全绿但 M2/M4/M5/M6 有观感偏差 → 交用户裁决。
> **FAIL** = 任一门控红 / M1/M3 不过。

**本报告判定：机器侧 PASS；整体签收 = CONCERNS（待真机）。**
- 机器侧三要素**全部满足**：门控 28/34/56（+62）全绿 · 判别性自检**我独立重做且通过** · L1–L4 零漂移成立。
- **但 §6 的 PASS 还要求 M1/M3 真机通过 —— 该项无头不可替代，尚未执行**。故**不得**把机器全绿等同于整体 PASS。**最终放行权在用户**。

### 阻塞项清单（须真机人工，见 §7）
| 优先级 | 项 | 说明 |
|---|---|---|
| **一票否决** | **M1** | L5 全场实体/弹道 y 平移 60px 后的「手感」与命中一致性 |
| **一票否决** | **M3** | 斜坡段落格直觉（尤 col4/col5 交界、col0 左缘），无「点这格中那格」 |
| 硬指标 | M2 / M4 / M5 | 沿坡行走连续 / 顶部不裁底边不露底 / L1–L4 目视零变化 |
| 稳健性 | M6 / M7 / M8 | 屋顶玩法原意 / 菜单进 L5 无帧异常 / 难度×关卡遍历 |

### 非阻塞登记项（建议）
1. **待定项 (a)**：檐沟抬升 2 行（L1547）入「回退文档/发布说明」。
2. **待定项 (b)**：单列低优先级 perf issue（C2 斜坡真机帧耗，随 KNOWN-ISSUES）。
3. **§5.4**：可选补 L5 地瓜（B3）断言；实体 y 归 M1。
4. **既有瑕疵**（非本轮）：画布右缘绿条 `x∈[955,1000]`，建议单列 issue。
5. **`perf-profile.md` 提示**：`bench.js` 每次运行会**自动回填 §7**（含运行时间戳/耗时），故其内容随运行漂移（本次复核期间被重写，diff 体量不变 33/31）。建议**最终提交前重跑一次 bench** 使其与定版代码一致，或将其视为生成物。

---

## 7. 须真机人工验证清单（无头不可替代）

> 环境：**1000×680 画布 · Chrome/Edge 实机**。`probe()` 不吐实体 y ⇒ 以下**只能真机看**，**机器门控全绿不等于整体通过**。

- [ ] **M1（一票否决）**：L5 全场实体/弹道 y 平移 60px 后手感 —— 种植落点、僵尸位置、子弹轨迹**视觉与命中一致**；无「打中不掉血 / 没打中掉血」。
- [ ] **M2**：僵尸沿坡行走**连续**（无 20px「电梯」跳档）；col4→col5 仅 1 次可见落差（**预期**）。
- [ ] **M3（一票否决）**：斜坡落格直觉 —— **点哪格中哪格**，尤 col4/col5 交界与 col0 左缘。
- [ ] **M4**：城垛（平台顶 y=20、城垛顶 y≈6）**不被上沿裁切**；檐板补厚使**屋面底边停 y=600 不露 CSS 绿底**。
- [ ] **M5**：L1–L4 逐关与改造前**肉眼无差异**。
- [ ] **M6**：屋顶玩法原意（直线弹弱/易失手、投手是解法）。
- [ ] **M7**：菜单→L5 入口/渲染各态无帧异常/残影。
- [ ] **M8**：普通/困难/地狱 × L5 各 ≥1 波，坡上命中与行走不劣化。

---

## 8. 工作树 / 证据留存

- **源码最终状态逐字节未变**：`md5 = 354b3f63c7a644dcf5fa3c1c01646b47`，`git diff --numstat = 112 49`，2408 行（与复核开始时一致）。
- **未提交/未推送**；**未新增或删除任何 harness 用例**；独立探针置于 OS 临时目录（`%TEMP%\qa-probe.js`），**不在 `tests/` 内**。
- 复核前后 `git status` 一致：`M perf-profile.md` · `M code-map.md` · `M plants-vs-zombies.html` · `?? v13-m4-evidence/` · `?? v13-m4-impl-report.md` · `?? REG-SLOPE-01.js` · `?? SMOKE-028.js`。唯一被本次复核「副作用改写」的是生成物 `perf-profile.md §7`（见 §6 登记项 5）。
- 备份：`/tmp/pvz-baseline.html`（= 复核基线）。

---

*本报告为**第二层 QA 独立验证**，全部数字由复跑/亲证获得；门控为 advisory，**发布放行由用户人工审批**。*
